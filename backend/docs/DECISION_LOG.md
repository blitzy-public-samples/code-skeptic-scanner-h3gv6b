# Decision Log

This log is the single source of truth for *why* the Code Skeptic Scanner backend is built the way it
is. Each entry records one decision that a competent engineer could reasonably have made
differently, together with the alternatives that were available, the reasoning that selected this
option and the risks the option carries.

Code in this repository never carries rationale. A comment states provenance facts — the source file
and line range it was ported from, or that the construct is net-new — and points here with
`see docs/DECISION_LOG.md`, optionally naming the entry that governs it. Reasoning, alternatives and
risks live in this file only.

**Completeness invariant.** Every `DL-` identifier cited by any artifact in this repository has a
complete four-column row in the table below. A citation is never satisfied by an entry that lives
somewhere else.

**Revision.** When a decision changes, its entry is rewritten in place under the same identifier so
that a citation always resolves to the behaviour the code has now; the earlier form survives in
version history only — see DL-099.

**Identifier allocation.** `DL-001` … `DL-060` are the identifiers of the decision inventory
committed in the Agent Action Plan §0.7.2, and each one whose decision is embodied by a delivered
artifact is populated below. Identifiers from `DL-061` onward are allocated by this log for
decisions the committed inventory does not cover.

## Entries

| ID | Decision | Alternatives considered | Rationale | Risks |
|----|----------|-------------------------|-----------|-------|
| DL-001 | The Java module replaces `backend/` in place: `backend/pom.xml` and `backend/src/**` are created and the twenty Python files under `backend/app/**` and `backend/tests/**` are deleted in the same change. | (a) A new sibling `backend-java/` module beside a retained `backend/`. (b) A nested `backend/java/` subtree. (c) A separate repository or a submodule. | The prompt names the artifact `backend/pom.xml` and instructs "From `backend/`, run `mvn clean install`", which fixes the module root. Every operational file already addresses `backend/` by that exact path — `COPY ./backend .`, `docker build … ./backend`, `cd backend`, `working-directory: ./backend` — so keeping the path makes every operational edit content-only instead of a path rewrite. (a) and (b) would leave twenty files of non-importable Python beside the Java tree, which contradicts "do not interleave Java and Python source" and would keep failing the pipeline's lint, type-check and test steps. (c) was excluded by the prompt. | A side-by-side diff of the two implementations is not available in the working tree; it is available through git history and through `docs/TRACEABILITY_MATRIX.md`. Any external tooling that still expects Python under `backend/` breaks at once rather than degrading quietly. |
| DL-002 | Maven coordinates are `com.codeskeptic:code-skeptic-scanner-backend`, and the base package is `com.codeskeptic.scanner`. | (a) `com.codeskeptic.scanner:backend`. (b) A reverse-DNS group derived from a hosting domain, e.g. `io.github.…`. (c) `com.codeskeptic:backend`. | Both names are derived from identifiers already in the repository rather than invented: the Celery application name `'code_skeptic_scanner'` at `backend/app/tasks/response_generation.py:L8` and the container image `code-skeptic-backend` at `.github/workflows/cd.yml:L12`. (b) asserts ownership of a domain the repository does not evidence. (c) is ambiguous in a repository that also contains a frontend. | The artifactId is long, so it appears in full in the jar name and in `spring-boot:repackage` output. Renaming later would touch the Dockerfile, the CD workflow and the deploy script. |
| DL-003 | The Maven parent is `org.springframework.boot:spring-boot-starter-parent:3.5.16`. | (a) Spring Boot 4.1.0, the newest release overall. (b) An earlier 3.x patch such as 3.3.x. (c) Import `spring-boot-dependencies` as a BOM instead of inheriting the parent. | The prompt specifies "latest stable 3.x", and 3.5.16 is the newest stable 3.x on Maven Central; 4.1.0 would contradict an explicit instruction and additionally pull in Spring Framework 7. (b) forgoes fixes for no stated benefit. (c) loses the parent's plugin management, which is what allows every plugin version in this POM to stay unpinned. | The 3.x line has a finite support window, so a future move to 4.x is inevitable and will carry a Spring Framework 7 upgrade with it. Inheriting the parent means a Boot version bump silently moves every BOM-managed transitive version at once. |
| DL-004 | The build tool is Maven, driven from `backend/pom.xml`, and every operational file invokes it: `.github/workflows/ci.yml` runs `mvn -B clean verify` and `scripts/deploy.sh` runs `mvn clean package`. | (a) Gradle with a Kotlin or Groovy DSL. (b) Maven with a wrapper (`mvnw`) committed alongside. | The prompt names Maven as the primary build tool and states the definition of done as `mvn clean verify`. (a) would contradict that. (b) is a reasonable convenience, but the CI job pins the JDK through `actions/setup-java` with `cache: maven` and the deploy script runs on a host that already has Maven, so a wrapper would add two committed binaries and a `.mvn/` directory without changing any outcome. | Without a committed wrapper, the Maven version is whatever the executing environment provides, so a build could behave differently across machines. The parent POM pins every plugin version, which keeps that exposure to Maven core itself. |
| DL-005 | Java 21 is the language level and the runtime: `<java.version>21</java.version>` in the POM, `actions/setup-java` with the Temurin distribution and `java-version: '21'` in CI, and an `eclipse-temurin:21-jre` runtime image. | (a) Java 17, the previous LTS. (b) Java 23 or another non-LTS release. (c) A different vendor build such as Zulu or Corretto. | The prompt specifies Java 21 (LTS), which Spring Boot 3.5.x fully supports, and records were compiled and run on OpenJDK 21 during analysis. (a) forgoes records-adjacent language features already relied on and contradicts the instruction. (b) has no LTS support window. (c) is functionally equivalent; Temurin was chosen because the prompt names `eclipse-temurin:21-jre` as the runtime base image, so using Temurin in CI keeps the build and runtime JDKs from the same vendor. | Pinning a major version in three places means an upgrade has to change all three together; a mismatch between the CI JDK and the image JRE would only surface at runtime. |
| DL-006 | Lombok is not a dependency. Constructors, getters, setters, `equals`, `hashCode` and any `toString` override are written by hand. | (a) Add `org.projectlombok:lombok` and annotate entities and DTOs. (b) Use Lombok on entities only. | Java 21 records already remove the boilerplate from every DTO in this project, and JPA entities need mutable fields and a no-argument constructor regardless, so Lombok would save little. Skipping it removes an annotation processor that has to stay aligned across JDK upgrades and keeps the generated bytecode readable in a stack trace. The prompt explicitly invited this decision to be flagged. | The four entity classes carry visible accessor boilerplate, and each new column means another getter and setter pair by hand. Hand-written `equals`/`hashCode` can drift from the field set if a column is ever added without updating them. |
| DL-007 | JaCoCo is not configured; no coverage report is produced or enforced. | (a) Bind `jacoco-maven-plugin` to `prepare-agent` and `report`. (b) Add it with a coverage threshold that fails the build. | Coverage reporting is a new capability the prompt does not request, and the retired Python project had none, so adding it would be scope creep rather than translation. The definition of done is a green `mvn clean verify`, which JaCoCo does not contribute to. | Test coverage is not measured, so a gap in the suite is invisible to the build. Nothing prevents adding the plugin later; it needs no source change. |
| DL-008 | `maven-failsafe-plugin` is not configured; every test runs under Surefire in the `test` phase. | (a) Add Failsafe and move `@SpringBootTest` and `@DataJpaTest` classes to an `*IT` naming convention bound to `integration-test`. (b) Add Failsafe and keep Surefire for unit tests only. | Spring Boot's test slices run in-process against H2 and need no external service, so they complete under Surefire inside the same `mvn clean verify` that the prompt names as the definition of done. A separate integration-test phase would add a plugin and a naming convention without adding a single assertion. | Slow context-loading tests share the `test` phase with fast unit tests, so there is no way to run only the fast ones. If a future test ever needs a real external service, the phase split will have to be introduced then. |
| DL-009 | The test-time database is `com.h2database:h2` at `runtime` scope, resolved by the parent BOM to 2.3.232. Testcontainers is not used. | (a) `org.testcontainers:junit-jupiter` plus `org.testcontainers:postgresql`, running a real PostgreSQL per test class. (b) A developer-supplied external database for tests. (c) H2 at `test` scope only. | Testcontainers' `junit-jupiter` and `postgresql` modules publish only up to 1.21.4 while the standalone artifact has moved to 2.0.5, so mixing them would break resolution; Testcontainers also needs a working Docker daemon, which the prompt's "no manual intervention" requirement cannot assume. (b) contradicts the same requirement. (c) was rejected because the same in-memory database is useful for a local `spring-boot:run` against `jdbc:h2:mem:…`, and `runtime` scope keeps it available for both. | H2 is not PostgreSQL or MySQL: DDL and dialect differences mean a mapping can pass on H2 and behave differently in production. The entity mappings are therefore verified against generated DDL for every supported dialect, not against H2 alone — see DL-068 and DL-069. Shipping H2 at `runtime` scope puts it on the production classpath, where it is unused. |
| DL-010 | Sentiment analysis uses the official Google client `com.google.cloud:google-cloud-language`, pinned explicitly to 2.96.0, and `LanguageServiceClient` is obtained with Application Default Credentials. | (a) Import `com.google.cloud:libraries-bom` and leave the artifact unversioned. (b) Call the REST API directly through `WebClient` or `RestClient`. (c) A third-party wrapper. | The artifact is actively maintained and is the same client family the retired `backend/app/services/sentiment_analysis.py:L1` used, so the port stays a substitution rather than a redesign; Application Default Credentials reproduce `LanguageServiceClient()` at `:L8` with no key property, which is why no Google credential appears in `application.yml`. (a) hides the resolved version behind a chain of imported BOMs, so the explicit pin is more auditable. (b) would hand-roll authentication that the client already implements. | An explicit pin has to be advanced by hand and can fall behind the BOM's own alignment. Application Default Credentials mean the service needs an ambient credential in every environment it runs in, and a missing credential surfaces only when the first analysis is attempted. |
| DL-011 | Response generation uses the official `com.openai:openai-java`, pinned explicitly to 4.49.0. | (a) `com.theokanning.openai-gpt3-java:service`, the long-standing community client. (b) Call the REST API directly through `WebClient`. | The official SDK is actively published — 4.49.0 dates from within days of the analysis — and supersedes the community wrapper, whose name still carries `gpt3`. Its Chat Completions builder was compiled during analysis and accepts `model`, a user message, `maxCompletionTokens`, `temperature` and `n`, so every parameter the retired `backend/app/services/llm_service.py:L19-26` passed maps across exactly. (b) would hand-roll request shapes that change with the API. | The SDK brings OkHttp and, through it, the Kotlin standard library onto the classpath. A major-version bump of a fast-moving SDK can change builder shapes, which is why every call site is confined behind the `LlmService` adapter. |
| DL-012 | The X (Twitter) integration is hand-rolled on `WebClient` from `spring-boot-starter-webflux`; no Java X client is used. | (a) `org.twitter4j:twitter4j-core` plus `twitter4j-stream`. (b) `com.twitter:twitter-api-java-sdk`. (c) `io.github.redouane59:twittered`. | No actively maintained Java client for X exists: twitter4j-core was last published in 2022 and twitter4j-stream targets the retired v1.1 `statuses` API, `twitter-api-java-sdk` was last published in 2022, and `twittered` is not on Maven Central at all. Adopting a four-year-stale client that targets an API version which no longer exists would be worse than a small hand-rolled client, and the prompt explicitly sanctions "a plain REST client via WebClient/RestClient" for this situation. `WebClient` is also the right shape for a long-lived chunked NDJSON stream. | The request shapes, the rules syntax and the reconnection behaviour are this project's responsibility and will need maintenance when the API changes. The exposure is bounded because every call lives behind the `TwitterService` and `TweetStreamClient` adapter boundary, so adopting a maintained client later is a local change. |
| DL-013 | The Notion integration is hand-rolled on `org.springframework.web.client.RestClient`, which ships with `spring-boot-starter-web`, so it adds no dependency. | (a) `com.github.seratch:notion-sdk-jvm-core`. (b) `tw.yukina.notion.sdk:notion-sdk-java`. (c) `WebClient` instead of `RestClient`. | The retired source made exactly two Notion calls — `pages.create` and `databases.query` at `backend/app/services/notion_service.py:L23-26,L32-38` — so an SDK would buy very little. (a) was last published more than two years ago and would add a stale transitive graph. (b) has negligible adoption. (c) is available but blocking calls are what these two operations are, and `RestClient` expresses them without a reactive type; it was compile-verified during analysis to need no additional coordinate. | The Notion API version header and the two request bodies are this project's responsibility. Notion is a secondary mirror and the relational database remains the system of record — see DL-052 for what is logged when a mirror call fails — so a Notion outage degrades a side effect rather than the service. |
| DL-014 | JWTs are minted and parsed with `io.jsonwebtoken` jjwt 0.13.0, split across `jjwt-api` at compile scope and `jjwt-impl` and `jjwt-jackson` at runtime scope. | (a) `spring-boot-starter-oauth2-resource-server` with a `NimbusJwtDecoder` over `MacAlgorithm.HS256`. (b) Nimbus JOSE + JWT directly. (c) Declare all three jjwt modules at compile scope. | This service mints its own tokens; it is not a resource server consuming a third-party issuer's tokens. (a) is built around decoding tokens issued elsewhere and would still need a separate minting library, so it adds a starter without removing work. The prompt names jjwt first. (c) would put implementation classes on the compile classpath, where application code could bind to them instead of the API. | A hand-written authentication filter of roughly thirty lines replaces framework-provided decoding, so its correctness is this project's responsibility; `security/JwtServiceTest` and `api/AuthControllerTest` exist to hold it. Splitting the modules by scope means a mistaken compile-scope import of an `impl` class fails the build, which is the intent, but the failure message is not obvious. |
| DL-015 | `scanner.jwt.algorithm` is a declared property bound from `${ALGORITHM:HS256}`, and the only value it accepts is `HS256`. The accepted string is read from the signer the implementation applies — `Jwts.SIG.HS256.getId()` — and any other non-blank value fails startup. An absent or blank value leaves HS256 in effect. | (a) Omit the property and hardcode HS256. (b) Support the full JWS algorithm set the property could name, including the RS and ES families. (c) Accept the whole HS family — HS256, HS384 and HS512 — while continuing to sign with HS256. (d) Accept the HS family and select the signer from the configured value. | `backend/app/core/security.py:L11` read `settings.ALGORITHM`, which `backend/app/core/config.py` never declared, so the key must exist for the G6 configuration drift to be closed and (a) does not close it. HS256 is `flask_jwt_extended`'s default and PyJWT's typical symmetric use, and AAP 0.6.1.2 fixes it, so the token format is not a deployment choice. (c) was delivered first and is a silent contradiction: a deployment configuring HS384 started successfully and then minted HS256 tokens, so the property described one thing and the service did another. (d) would honour the value but contradicts 0.6.1.2 and turns the accepted set into a real branch to key-manage and test on every path. (b) additionally needs asymmetric key material, a key store and a rotation story the source has nowhere. Deriving the accepted string from the signer rather than writing the literal twice means the two cannot drift apart in a later change. | A deployment carrying `ALGORITHM=HS384` — a value the property's type permits and the previous behaviour accepted — now fails to start rather than being quietly ignored. That is the intent. No shipped configuration sets anything but HS256, and `ALGORITHM` is not among the environment keys the deployment supplies, so the `HS256` default applies everywhere. |
| DL-016 | `scanner.jwt.secret` is bound from `${SECRET_KEY}` with **no default**, so the application fails to start when the variable is absent. | (a) Ship a development default such as `changeme`. (b) Generate a random secret at startup when none is supplied. (c) Bind an empty default and fail later, at first use. | A committed development secret is the kind of value that reaches production, and jjwt 0.13.0 additionally rejects an HS256 key shorter than 256 bits, so a short placeholder would fail anyway. (b) silently invalidates every previously issued token on restart and breaks any multi-instance deployment. (c) turns a configuration error into a request-time failure far from its cause. Failing fast at boot is the behaviour a missing mandatory secret should have. | Every environment, including a bare local run, must supply `SECRET_KEY` before the context will refresh. The test profile supplies its own key so that `mvn clean verify` still needs no manual step. |
| DL-017 | The access-token lifetime is `scanner.jwt.expiration-minutes`, defaulting to 60, and the value is validated when `security/JwtService` is constructed: below 1 minute or above 60 fails startup. Minutes are converted to the seconds the token endpoint reports with a checked multiplication. | (a) A shorter default such as 15 minutes. (b) A longer default such as 24 hours. (c) Access token plus refresh token. (d) Declare the bound with Bean Validation `@Min` and `@Max` on the properties record. (e) Clamp an out-of-range value into the range instead of failing. | `documentation/Technical Specifications.md` §API Security states a one-hour maximum JWT lifespan, which makes 60 minutes a documented ceiling rather than a preference — and therefore something to enforce. The first delivery accepted any `long`, including zero, negative values and `Long.MAX_VALUE`, the last of which overflows the seconds conversion. 60 is the largest value honouring the maximum and mirrors `exp = datetime.utcnow() + expires_delta` at `backend/app/core/security.py:L9-10`. (a) is safer but forces re-authentication in a system with no refresh flow. (b) exceeds the documented maximum. (c) introduces a token type, a store and a revocation surface the source does not have. (d) would add a Bean Validation constraint, and IR9 limits validation to the two the source declares — so the guard sits beside the secret-length and algorithm guards already in the same constructor, which also keeps every startup failure in one place. (e) hides a misconfiguration: an operator asking for a day would silently get an hour. | A deployment configured above 60 minutes, or at zero, now fails to start rather than running with a lifetime nobody intended. A leaked token still lives for up to an hour and there is no revocation path short of rotating `SECRET_KEY`, which invalidates every token at once. Clients must handle a 401 by re-authenticating. |
| DL-018 | Every minted token carries exactly three claims — `sub` (the principal name), `iat` and `exp`. No role, scope, permission or caller-supplied claim is copied in. | (a) Copy an arbitrary caller-supplied claim map, as `create_access_token(data: dict)` did at `backend/app/core/security.py:L7-8`. (b) Add role or authority claims. (c) Add the registered `jti`, `iss` and `aud` claims. | The source signature accepted a dictionary and copied it wholesale into the claim set, so any caller could place anything — including contradictory or privileged values — into a signed token; it had zero call sites, so nothing depended on that freedom. This service authenticates exactly one configuration-backed principal (DL-020) and RBAC is out of scope, so the subject is the only claim any code path reads. `iat` and `exp` are derived from a single captured instant, so the pair cannot disagree. (c) would add registered claims that no validator in this application checks, which is ceremony rather than security. | A future multi-principal or authorization model needs an additive change to both the claim set and the filter that reads it. Because no `jti` is issued, an individual token cannot be revoked before it expires; the exposure is bounded by the 60-minute lifetime of DL-017 instead. |
| DL-019 | A net-new endpoint `POST /auth/token` accepts `{"username", "password"}` and returns `{"access_token", "token_type", "expires_in"}`. It is the only unauthenticated route; all eleven pre-existing routes require an authenticated principal. | (a) `POST /login` returning a bare token string. (b) `POST /auth/login` with a nested envelope. (c) HTTP Basic authentication on every route and no token endpoint at all. (d) No token endpoint, leaving tokens to be minted out of band. | The retired tree registered no authentication blueprint and `create_access_token` had zero call sites, so there is nothing to port and the shape is a choice that must be flagged rather than slipped in. The OAuth 2 conventional body is what a future `frontend/src/services/authService.ts` would most naturally consume, and `frontend/src/utils/api.ts:L13-16` already sends `Authorization: Bearer ${token}`, so a bearer token is the expected currency. (a) and (b) are equally workable but neither matches the convention the frontend's header already implies. (c) would send credentials on every request. (d) leaves the prompt's requirement for a working token-issuance path unmet. | This is a genuinely new public route, so it is a new attack surface: it is unauthenticated by necessity and has no rate limiting, because new rate limiting is out of scope. Its path and body shape are now a contract that a client can depend on. |
| DL-020 | The credential store is configuration-backed: a single principal registered through an `InMemoryUserDetailsManager` from `scanner.auth.username` (default `admin`) and `scanner.auth.password-hash` (a bcrypt hash, no default). No `app_users` table is created. | (a) A JPA `app_users` entity with a `UserDetailsService` over it. (b) A plaintext password property compared directly. (c) An external identity provider. | Adding a table would break the schema-parity mandate that the target has exactly the same four tables as the source, and there is no user-management requirement anywhere: no registration route, no user CRUD, no password reset, and RBAC is explicitly out of scope, so the table would be an unused capability. The only `User` entity the project documents describes a Twitter account, not an application principal. A hash property still exercises `BCryptPasswordEncoder` for real, which is what makes the passlib translation live code instead of dead code. (b) would put a plaintext credential in configuration. (c) is explicitly out of scope. | One shared credential, no per-user accounts, and no rotation without a redeploy. The extension path is a JPA-backed `UserDetailsService` behind the same interface, which is a localised change. `AUTH_PASSWORD_HASH` has no default, so it must be supplied or the context will not refresh. |
| DL-021 | Authentication is genuinely enforced in the Java service, on all eleven pre-existing routes, where the Python decorators enforced nothing. | (a) Reproduce the source's effective behaviour by leaving every route open. (b) Enforce on write routes only. (c) Enforce, but behind a flag defaulting to off. | `@jwt_required` was applied bare, without parentheses, at all eleven sites, which in `flask-jwt-extended` 4.x registers the decorator factory instead of the guard — so the source's *intent* was unambiguous and only its wiring was broken. The prompt orders a working token-issuance path, and the frontend already sends a bearer header. (a) would preserve a defect as a feature. (b) is arbitrary in a system whose read data is the product. (c) ships a switch whose off position is a vulnerability. | This is a deliberate behaviour change: any client that reached these routes unauthenticated now receives 401. It is called out here because a reader could otherwise mistake it for a regression. Every controller test therefore supplies authentication. |
| DL-022 | JSON member names stay `snake_case`, declared per component with `@JsonProperty`. `spring.jackson.property-naming-strategy` is deliberately not set. | (a) Set a global `SNAKE_CASE` naming strategy and drop the per-component annotations. (b) Switch the wire to `camelCase` to match the frontend's Zod contracts. | The wire contract is defined by the retired Flask handlers, which emitted `snake_case` keys, and IR1 requires it to survive verbatim. (a) is tempting but would also rename members this project does not intend to expose implicitly, and it makes the wire name invisible at the declaration site, where a reviewer checks it. (b) would be an improvement to the frontend's benefit, but the prompt forbids fixing the frontend and forbids changing the contract; the divergence is left in place — see DL-059. | Every DTO component carries an explicit annotation, so a new component silently defaults to the Java member name if the annotation is forgotten. The frontend's `camelCase` contracts still do not match, which is a known, deliberate divergence. |
| DL-023 | Identifiers serialise as `String` on the wire while the persistence types stay numeric: `TweetDto.id`, `ResponseDto.id`, `ResponseDto.tweetId` and `AiToolDto.id` are `String`, and conversion happens only at the DTO boundary. `equals`/`hashCode` on an entity compare the persistent identifier and are net-new Java persistence mechanics with no Python counterpart. | (a) Serialise identifiers as JSON numbers, matching the column types. (b) Make the persistence types `String` as well. (c) Overload the X post identifier onto `tweets.id`. | `backend/app/schema/tweet.py:L6` and `frontend/src/schema/tweet.ts:L4` both type `id` as a string, and `frontend/src/schema/aiTool.ts` does the same, so a string identifier is the only typed contract the repository actually contains; JSON numbers also lose precision past 2^53 in a JavaScript client. (b) would change the column types and break schema parity. (c) would change the meaning of a generated surrogate key and risk collisions with the generator — see DL-049. The entity `equals`/`hashCode` form yields `false` whenever either identifier is `null`, so two unpersisted instances never compare equal, and `hashCode` is derived from the entity type so it is stable across the transient-to-persistent transition. | Two conversions exist per identifier — entity to wire and wire back to a lookup — and a malformed inbound identifier is a not-found rather than a bad-request condition, which is what DL-048 records. A type-constant `hashCode` puts every instance of an entity in one hash bucket, which is irrelevant at the collection sizes this service handles. |
| DL-024 | `tweets.media` and `tweets.ai_tools_mentioned` remain single delimited `String` columns and are exposed on the wire as JSON arrays. `util/DelimitedStringListConverter` performs the conversion with a comma delimiter, is applied explicitly per attribute and is not auto-applied. | (a) An `@ElementCollection` side table per list. (b) A JSON column type. (c) A foreign key or join table from `ai_tools_mentioned` to `ai_tools`. (d) A different delimiter, such as a vertical bar or the ASCII unit separator. (e) `@Converter(autoApply = true)`. | `backend/app/db/models.py:L15,L18` declare both columns as a plain `String`, while `backend/app/schema/tweet.py:L11,L14` and `frontend/src/schema/tweet.ts:L9,L12` both declare `List[str]`, so the conversion is required and the column definition must not change. (a) and (b) would add a table or change a column type, breaking schema parity. (c) is explicitly forbidden by the prompt. (d) is safer against embedded delimiters but a comma is what a human editing the column would write, and the source stored no structured value at all. (e) would silently apply the conversion to every `List<String>` attribute in the model, including any future one that should not be delimited. | The conversion is lossy: an element that itself contains a comma is split into two on the way back, and a `null` or blank column normalises to an empty list rather than to `null`. Both are accepted because the source stored free text with no escaping either. Because the converter is not auto-applied, a future entity attribute that needs it must name it explicitly or it will silently persist as `[a, b]` through the default rendering. |
| DL-025 | The JPA entity for the `responses` table keeps the source class name `Response`, with `@Table(name = "responses")`, and its identifier is typed `Long`, as `entity/Tweet.id` and `entity/AiTool.id` are. | (a) Rename the type to `GeneratedResponse` so it does not read like an HTTP type. (b) Type the identifier `Integer`, the one-to-one mapping of `Column(Integer, primary_key=True)`. (c) Map `tweet_id` as a scalar column alongside a separate association attribute, mirroring the two source declarations literally. | `backend/app/db/models.py:L20` declares `class Response`, and name parity is precisely what this refactor preserves; the name does not collide with `org.springframework.http.ResponseEntity`, which is a different type, and the entity is referenced only from the persistence layer. On the identifier: `responses` is one half of the schema's single association, so `Long` keeps the primary key and the `tweet_id` foreign key the same width as `tweets.id`, and it matches the `JpaRepository<Response, Long>` that AAP §0.4.1.4 specifies — the same clause that fixes `entity/AiTool` at `Long` (DL-070), so all three identifiers are uniform. (b) would make the foreign key narrower than the key it references. (c) would map one column twice, which Hibernate rejects unless one side is read-only, so the single `@ManyToOne @JoinColumn(name = "tweet_id")` carries both the column and the relationship, as TR-4 directs. | `responses.id`, `tweets.id` and `ai_tools.id` all generate `bigint` where the source declared a 32-bit `Column(Integer)`. That is the schema's only divergence in column type from the source, it is mandated by AAP §0.4.1.4 and recorded for `ai_tools` under DL-070, no stored value can fail the widening, and it is invisible to clients because identifiers serialise as `String` (DL-023). |
| DL-026 | Schema bootstrap is `spring.jpa.hibernate.ddl-auto=update`, with `spring.jpa.open-in-view=false`. There is no migration tool. | (a) Flyway. (b) Liquibase. (c) `ddl-auto=validate` plus a hand-written DDL script. (d) `ddl-auto=none` and an out-of-band schema. (e) Leave `open-in-view` at its default of `true`. | Nothing in the retired tree ever created a table: there is no `Base.metadata.create_all()` call, no migrations directory and no CLI, so the implicit expectation was that the schema simply exists. `update` is the minimal equivalent of that expectation and satisfies IR3 without introducing a capability the source lacked; introducing Flyway or Liquibase would be a new capability, not a translation. (c) and (d) would leave a clean checkout unable to serve a request, which contradicts "no manual intervention". (e) keeps a persistence context open for the whole request, which hides lazy-loading mistakes behind working code and holds a connection longer than the transaction needs. | `update` never drops or narrows anything and never widens an existing column, so a database created before a mapping change keeps the old physical type — the divergence that DL-069 weighs. It also means the annotations *are* the DDL, so a mapping error becomes a schema error, which is why the mappings are verified against generated DDL for every supported dialect. There is no versioned migration history, so a schema change cannot be reviewed as a diff. |
| DL-027 | `DATABASE_URL` binds to `scanner.database-url`, and `config/DatabaseUrlTranslator` converts it into a JDBC URL plus separate credentials. `spring.datasource.url`, `.username`, `.password`, `.driver-class-name` and `spring.jpa.database-platform` are all deliberately absent. | (a) Bind `DATABASE_URL` straight to `spring.datasource.url`. (b) Require operators to supply JDBC-form settings and drop `DATABASE_URL`. (c) Set `spring.jpa.database-platform` to a dialect. (d) Set `spring.datasource.driver-class-name`. | A SQLAlchemy URL such as `postgresql://user:pass@host/db` is not a JDBC URL, so (a) fails at connection time; the source passed exactly that string to `create_engine` at `backend/app/db/database.py:L7`. (b) would change the operational contract the prompt preserves. (c) and (d) would hardcode a vendor, which IR4 forbids — Hibernate detects the dialect from JDBC connection metadata and the driver is resolved from the URL, which is precisely how "dialect-agnostic" is honoured with both drivers on the classpath. | A translation step is a piece of security-relevant parsing this project owns; its behaviour is pinned by an exhaustive case set covering the AAP's six cases plus every credential-bearing and no-normalisation shape. Because no dialect is declared, a wrong `DATABASE_URL` surfaces as a driver-level failure at first connection rather than as a configuration error at binding time. |
| DL-028 | Both `org.postgresql:postgresql` and `com.mysql:mysql-connector-j` are declared at `runtime` scope, versions managed by the parent BOM. | (a) Declare only the driver a given deployment needs, selected by a Maven profile. (b) Declare them at `compile` scope. (c) Pin both versions explicitly. | `DATABASE_URL` is a single opaque string resolved at runtime, so the artifact cannot know at build time which vendor it will face; a single jar that serves either URL is what IR4 requires. `runtime` scope keeps both drivers off the compile classpath, so no application class can import a vendor type by accident. (a) would produce vendor-specific artifacts and a build-time decision the configuration is meant to defer. (c) would create drift the moment the Boot version moves, since the BOM already aligns them. | Two drivers ship in every image, one of which is always unused, which adds a few megabytes and two more CVE surfaces to track. A `DATABASE_URL` naming a third vendor is rejected at startup by the translator rather than silently unsupported. |
| DL-029 | `src/main/resources/application.yml` sets `server.port: ${PORT:5000}`, so the service listens on 5000 unless the environment injects `PORT`. | (a) Leave Spring Boot's default of 8080. (b) Hardcode 5000 with no override. (c) Set 8080 and change the Dockerfile's `EXPOSE`. | Flask defaulted to 5000 at `backend/app/main.py:L53` and the container exposes 5000 at `infrastructure/docker/Dockerfile.backend:L17`, so 5000 is the observable contract; without an explicit setting the port silently changes and the exposed port stops matching the listener. Honouring an injected `PORT` is what Cloud Run requires, and because 5000 is preserved no Terraform or Cloud Run change is triggered. (b) would break on a platform that injects `PORT`. (c) would ripple into the Dockerfile and the deployment for no gain. | Two mechanisms can set the port, so a `PORT` variable left over in an environment silently overrides the default. Port 5000 collides with some desktop services, which is a local-development annoyance rather than a deployment issue. |
| DL-030 | `spring.main.web-application-type` is set explicitly to `servlet`. | (a) Omit it and let Spring resolve the type. (b) Set `reactive` and run the whole application on WebFlux. | Both `spring-boot-starter-web` and `spring-boot-starter-webflux` are on the classpath, because `WebClient` is needed for the long-lived X stream while the eleven routes are ordinary blocking handlers. Spring already resolves that combination to `SERVLET`, so this setting changes nothing — it makes an otherwise implicit and surprising resolution explicit, which is what IR12 asks for. (b) would rewrite every controller and repository interaction into a reactive pipeline, which is a redesign the prompt forbids. | The setting looks redundant and could be "tidied away" by someone who does not know both starters are present; removing it would restore the implicitness rather than change behaviour. Carrying both starters means two web stacks are initialised on the classpath even though only one serves requests. |
| DL-031 | The three Twitter drift keys are declared as aliases with nested placeholder defaults pointing at the canonical pair: `api-secret-key: ${TWITTER_API_SECRET_KEY:${TWITTER_API_SECRET:}}`, `consumer-key: ${TWITTER_CONSUMER_KEY:${TWITTER_API_KEY:}}`, `consumer-secret: ${TWITTER_CONSUMER_SECRET:${TWITTER_API_SECRET:}}`. | (a) Declare all five as independent keys with no defaults, so each must be supplied. (b) Declare only the canonical pair and drop the three drift names. (c) Resolve the aliasing in Java, inside `ScannerProperties`. | `backend/app/services/twitter_service.py:L12` reads `TWITTER_API_SECRET_KEY` and `backend/app/tasks/tweet_monitoring.py:L46-47` read `TWITTER_CONSUMER_KEY` and `TWITTER_CONSUMER_SECRET`, while `backend/app/core/config.py:L5-6` declares only the canonical pair — so all three names must resolve for G6 to be met. The aliases are sound because they name the same credentials: X's developer portal labels them "API Key (Consumer Key)" and "API Secret Key (Consumer Secret)". (a) would demand three duplicate secrets. (b) would leave the drift open. (c) would hide the relationship from anyone reading the configuration. The nested-default technique was verified empirically against Spring Boot 3.5.16 on JDK 21 rather than assumed. | An operator who sets only one of an alias pair gets the canonical value silently, which is the intent but is not obvious from an environment listing. Every Twitter key carries an empty-string default so the context refreshes without them, which defers a missing-credential failure to the first API call. |
| DL-032 | Reply generation calls the OpenAI Chat Completions API, replacing `Completion.create(engine="text-davinci-002", ...)` at `backend/app/services/llm_service.py:L19-26`. | (a) The legacy `/v1/completions` endpoint the source used. (b) The Responses API. | The completions-endpoint models the source named were shut down on 2024-01-04, and the stated replacement `gpt-3.5-turbo-instruct` is itself scheduled for retirement, so the legacy endpoint is a dead end for new work. Chat Completions is the surface every currently offered general-purpose model serves, and every parameter of the source call maps across one-to-one: `max_tokens` becomes `max_completion_tokens` (DL-034), while `temperature` and `n` keep both their names and their transcribed 0.7 and 1 values. The Responses API is documented as the successor to and a superset of Chat Completions, but adopting it would change the request and response shape for no behaviour this port needs — the single call site sends one user message and reads one choice. | A later move to the Responses API is a change confined to this adapter. Chat Completions returns a list of choices, so the first is read and any others ignored, which is correct only while `n` is 1; `n` is configuration, so a value above 1 would silently discard output. |
| DL-033 | The OpenAI model is configuration, `scanner.openai.model`, with a documented default rather than a hardcoded identifier. | (a) Hardcode a specific model in `LlmService`. (b) Keep `text-davinci-002`, the identifier the source named. (c) Omit the property and rely on the SDK's default. | OpenAI retires models in dated waves — the GPT-3 and GPT-3.5 completions models were shut down on 2024-01-04, `gpt-3.5-turbo-instruct`, `babbage-002` and `davinci-002` are scheduled for 2026-09-28, and the `gpt-3.5-turbo` and `gpt-4` lines for 2026-10-23 — so any hardcoded identifier has a known expiry and a retirement would require a code change and a redeploy. (b) is not available: the model no longer exists. (c) makes the model invisible and unpinnable. | The default will itself age and must be revisited on OpenAI's published retirement schedule; a stale value produces a runtime API error rather than a startup failure. Different models produce different response quality and cost, so changing the property changes behaviour without any code review. |
| DL-034 | The token-budget property is named `scanner.openai.max-completion-tokens`, carrying the source's value of 150, and is passed as the SDK's `maxCompletionTokens`. | (a) Keep the source's name `max_tokens` and the SDK's older parameter. (b) Drop the cap entirely. (c) Raise the cap for longer replies. | `backend/app/services/llm_service.py:L22` set `max_tokens=150`; `max_completion_tokens` is the modern parameter name in the current API and is what the official SDK's builder exposes, verified by compiling the call during analysis. Keeping the number at 150 preserves behaviour while the name tracks the API. (b) and (c) would change the generated output, which is a business-behaviour change nobody requested. | The property name no longer matches the retired source's name, so a reader diffing the two has to know they are the same knob — which is what this entry records. 150 tokens truncates a long reply, exactly as the source did. |
| DL-035 | The prompt template at `backend/app/services/llm_service.py:L16` is preserved verbatim — segments, punctuation and newlines — as named constants that are concatenated. The unresolvable `{tweet.context}` placeholder is populated deterministically from the post's `ai_tools_mentioned` and `doubt_rating`. | (a) Reword or restructure the prompt. (b) Drop the `Context:` clause, since the value it interpolated never existed. (c) Add a system message describing the assistant. | The prompt is the behavioural contract of this feature, so any edit to it changes the generated text; holding the segments as constants makes that contract explicit and leaves the apostrophe inside the quoted body exactly as the source left it. `{tweet.context}` cannot be resolved as written, because `backend/app/schema/tweet.py:L5-14` declares no `context` attribute, so the placeholder had to be filled from something: the two fields that record why a post was captured at all are the tools it mentions and its doubt rating, both already present on the DTO, which makes the value deterministic and reproducible rather than invented per call. (b) would drop a clause the template itself declares. (c) would send instructions the source never sent. | The `Context:` value is an interpretation of an attribute that never existed, so it is the one part of the prompt that is not a transcription. It is confined to a single private method, so revising it touches one place. |
| DL-036 | `SentimentAnalysisService` exposes `analyzeSentiment(String text)` returning a `double`, invoked with the tweet's content. | (a) `analyzeSentiment(TweetDto)` reading a field, as the source method signature suggests. (b) Add the `analyze(...)` method the controller called, alongside the real one. (c) Return a structured result carrying the score, magnitude and language. | Both sides of this call are broken in the source, in complementary ways: `backend/app/api/tweets.py:L46` calls `sentiment_analysis.analyze(tweet.content)`, a method that does not exist, while the real method `analyze_sentiment(tweet)` reads `tweet.text` at `backend/app/services/sentiment_analysis.py:L14`, a field the schema does not have. What the two agree on is "the text of the tweet", so taking a `String` is the resolution that honours both. (a) would preserve the non-existent field. (b) would ship two names for one behaviour. (c) is a contract expansion — see DL-037. | The service no longer knows which tweet it is analysing, so a log record cannot name the row; the caller holds that context. Magnitude and language are discarded, so a future feature needing them changes this signature. |
| DL-037 | `POST /tweets/{tweetId}/analyze` returns `{"tweet_id": <the path value as received>, "analysis_result": <the sentiment score>}`, and `analysis_result` is the score as a floating-point number. | (a) Return a composite object carrying both the score and the derived doubt rating. (b) Return the doubt rating instead of the score. (c) Return the full Google response. | `backend/app/api/tweets.py:L52-55` returns exactly two keys, and the only analyze method that exists returns a single value, so a single number is faithful to the source contract. (a) and (c) expand a contract IR1 requires preserved. (b) would silently change what the key means. The persisted side effect still sets `tweets.doubt_rating` through the transcribed formula, so nothing is lost — it is simply not echoed. | A client that wants the doubt rating has to read the tweet back. `tweet_id` echoes the path value verbatim, including a value that matched no row, which is deliberate: the handler answers 404 in that case, so the echo is only ever seen for a real row. |
| DL-038 | The pagination envelope keys are `page`, `per_page`, `total` and `total_pages`, derived from Spring Data's `Page` as `getNumber() + 1`, `getSize()`, `getTotalElements()` and `getTotalPages()`. `page` is 1-based on the wire. | (a) Expose Spring Data's own serialised `Page` shape. (b) Use `pageNumber`/`pageSize`/`totalElements`/`totalPages`. (c) Keep the wire 0-based, matching Spring Data. | `backend/app/api/tweets.py:L18-21` and `backend/app/api/responses.py:L17-20` both return a `pagination` object, but the producing methods never existed, so the internal keys are undefined in the source and the frontend types the response as a bare array — there is no evidence either way and the keys must be chosen. `snake_case` follows DL-022, and the four names are the minimum that describes a page. (a) leaks a framework shape and is unstable across Boot versions. (b) is `camelCase`. (c) would contradict `page` defaulting to 1 at `backend/app/api/tweets.py:L12`, which is a preserved query-parameter default. | The off-by-one between the wire and Spring Data is a standing trap on every paginated route; it is converted in one place per controller and covered by a test. Because the keys were invented, a future client contract has to be derived from this entry rather than from the source. |
| DL-039 | `GET /settings` returns a JSON **array** of `{key, value, description}` objects. | (a) A key-to-value map, as `backend/tests/test_api.py:L37-39` expects. (b) A map of key to a nested object. (c) An envelope such as `{"settings": [...]}`. | The array of three-field objects is the only shape that carries all three columns the `settings` table actually has at `backend/app/db/models.py:L40-44`, and it matches `frontend/src/schema/setting.ts:L3-7`, the sole typed contract for this shape. The contrary test evidence is explicitly discounted: that test requests `/settings/` with a trailing slash rather than the registered route, drives FastAPI's `TestClient` against a Flask application, and its sibling `PUT`s to `/settings/`, which is not a route at all — the file cannot execute. A map shape would also silently drop `description`. | A client that wanted `settings["auto_response"]` has to index the array itself. Discounting an in-repository test expectation is a judgement call, recorded here so it reads as a decision rather than an oversight. |
| DL-040 | `SettingsService` seeds exactly three `settings` rows — `tweet_popularity_threshold`, `response_generation_delay` and `stream_keywords` — insert-if-absent on `ApplicationReadyEvent`, each with a `description`, and a key already present is never overwritten. This establishes the precedence contract that a `Setting` row overrides the corresponding configuration default. | (a) Seed nothing and leave the table empty. (b) Make `PUT /settings/{key}` an upsert so an unknown key creates its own row. (c) Seed from a `data.sql` or `import.sql` script. (d) Seed from a `@PostConstruct` method. (e) Seed a wider set including the `auto_response` key `backend/tests/test_api.py:L37-39` names. (f) Overwrite the three rows on every start so configuration always wins. | Nothing in the source tree ever creates a `settings` row: there is no `Base.metadata.create_all()`, no migration, no fixture and no seed path anywhere, so the table starts empty. `backend/app/api/settings.py:L21-22` then returns 404 for any key that is not already present, which makes 404 the *only* reachable outcome of `PUT /settings/{key}` and leaves `GET /settings` rendering an empty array permanently — two of the eleven preserved routes would be preserved in name only and nothing in the system would be runtime-configurable, contradicting the instruction to complete rather than merely port. Insert-only is what lets an operator's edit through `PUT /settings/{key}` survive a restart, which is the behaviour `documentation/Code Structure.md:L1078` describes for `check_popularity_threshold` ("Get popularity threshold from settings") and what the SRS requires at L57 (customizable popularity thresholds), L319 / F005-2 (Threshold Management) and L766 (the glossary's configurable Popularity Threshold). Three keys is the smallest set that covers the two defaulted properties at `backend/app/core/config.py:L10-11` plus the keyword set DL-044 makes operator-editable. (b) would change the `PUT` contract from update to upsert, which the route the prompt orders preserved does not do. (c) moves row content out of the class that owns the key names and out of the transaction the service layer demarcates. (d) is not merely stylistic: `@Transactional` has no effect on a `@PostConstruct` method because the transactional proxy does not exist when it runs, so the seed would execute outside a transaction; `ApplicationReadyEvent` fires after the context is refreshed, and it does not fire in a `@WebMvcTest` or `@DataJpaTest` slice, so test fixtures stay deterministic. (e) is refused for the reasons recorded under DL-039. (f) defeats runtime configurability entirely. | Three rows appear in a database the operator did not populate, and a row deleted deliberately outside the application reappears on the next start. The seeded value duplicates the configuration default at the moment of first start and then diverges silently: a later change to `scanner.popularity-threshold` does not reach a row that already exists. `stream_keywords` is stored as one comma-delimited string, so a keyword containing a comma cannot be expressed. The seed adds three `existsById` reads to every start. Two instances starting at the same moment can both observe a key as absent and both attempt the insert; the loser fails on the primary key, and because the listener runs inside `SpringApplication.run()` that instance does not start — reachable on Cloud Run, whose `scaling_parameters` allow up to ten instances. Measured correction to a common assumption: `ApplicationReadyEvent` is published by `SpringBootContextLoader` too, so a `@DataJpaTest` slice that imports this bean begins from the committed three-row baseline rather than an empty table; a slice asserting on `settings` must expect those rows. Because the seed is insert-only that baseline is deterministic, and a `@WebMvcTest` slice is unaffected because it holds no `DataSource`. Seeding is an addition the source did not perform at all, recorded here so it reads as a decision rather than scope creep. |
| DL-041 | The analytics summary returns `total_tweets`, `total_responses`, `approved_responses`, `pending_responses`, `average_doubt_rating`, `average_like_count` and `tracked_ai_tools`. | (a) The two keys the source test names and nothing more. (b) A larger set including per-tool and per-author breakdowns. (c) A generic `{metric, value}` list. | `total_tweets` and `total_responses` are evidenced by name at `backend/tests/test_api.py:L50-51`. The other five are the complete set of aggregates the four existing tables can answer without a new column: `is_approved` is the only boolean in the schema, `pending` is its complement and supports the human-review workflow the never-auto-post invariant depends on, and `doubt_rating` and `like_count` are the only numeric tweet columns. `tracked_ai_tools` counts the one table nothing else surfaces. (a) leaves an analytics endpoint that answers almost nothing. (b) would need grouping the schema does not index for and would tempt a new index, which is out of scope. (c) makes the response untypable. | Seven aggregates run as separate queries against unindexed columns; at the data volumes the source contemplated that is immaterial, but it would not stay so, and adding an index is out of scope. The key set is now a contract, so removing one is a breaking change. |
| DL-042 | `GET /analytics/trends` returns `{"trends": [ {date, tweet_count, average_doubt_rating, total_likes}, … ]}`, bucketed by day over `tweets.created_at`, across a window of `scanner.analytics.trend-window-days` (default 30). No query parameter is introduced. | (a) Accept `start_date` and `end_date` query parameters, as `backend/tests/test_api.py:L55-59` probes. (b) Bucket by week or month. (c) Return every day since the first tweet. (d) Make the window a `Setting` row rather than a property. | `get_trends()` takes no arguments at `backend/app/api/analytics.py:L14`, so adding a query parameter would change the route contract the prompt orders preserved; the contrary test is itself annotated as possibly needing adjustment and cannot execute. A configured window is therefore the only way to bound the series. Day buckets match `documentation/Technical Specifications.md:L440-441`, which describes the interface as trend visualisations and sentiment over time, and the SRS Feature 7 / F007-1 requirement to aggregate tweet data for trend analysis. (c) makes the response size unbounded. (d) is a reasonable future step but the window is an operator-tuning knob, not a product setting, and DL-040 keeps the seeded set to three product keys. | The window cannot be varied per request, so a client wanting 90 days needs a redeploy or a configuration change. Thirty daily buckets is a fixed response size that will not grow, which is the point, but it also means older data is simply invisible through this route. |
| DL-043 | `SettingsService` is an injected singleton `@Service` whose operations are instance methods; it declares no static member other than its logger and its private constants, and controllers reach it by constructor injection. | (a) Keep the source's call shape literally, as Java `static` methods on the class. (b) A static utility holder that receives a repository handed in per call. (c) A `SettingsService` interface plus a `SettingsServiceImpl`. (d) Field or setter injection instead of a constructor. | `backend/app/api/settings.py:L10,L20` invokes `SettingsService.get_all_settings()` and `SettingsService.update_setting(key, new_value)` statically on the class, and that shape cannot carry the three collaborators the behaviour needs — a repository, a mapper and bound configuration. The conversion is not cosmetic: a `static` method cannot be proxied, so `@Transactional` would not apply to it and the transaction boundary this layer owns would not exist, and it cannot be replaced by a mock in a `@WebMvcTest` slice. TR-14 converts every per-request instantiation and every module-level `get_settings()` read in the source to constructor injection, and this class follows the same rule as the four ported services, so the seven service beans are uniform. (c) would add a type with exactly one implementation and no second caller. (d) hides required collaborators from the constructor signature and permits a partially constructed bean. | The call shape differs from the source, so a reader diffing the Python controller against the Java controller sees an injected field where a class-qualified call stood; the method names and parameter order are unchanged. Any future caller must obtain the bean from the context rather than calling through the class, which is a visible constraint on utility-style reuse. |
| DL-044 | The streaming rule set is the configured base terms in `scanner.ingestion.stream-base-keywords` — `AI coding tool`, `AI code assistant`, `AI generated code`, `GPT-4` — unioned with every `ai_tools.name` row, and the whole set is overridable by the `stream_keywords` setting row. | (a) Keep the source's empty list. (b) Hardcode a keyword list in Java. (c) Derive the set from `ai_tools` alone. (d) Derive it from configuration alone. | `backend/app/tasks/tweet_monitoring.py:L53-55` leaves `keywords = []` behind a TODO and then calls `stream.filter(track=keywords)`; an empty track list is invalid, so ingestion could never start. A keyword set therefore cannot be ported and must be designed. Scanning all four documentation artifacts for named AI coding tools returns only "GPT-4" alongside the generic phrase "AI coding tools", so the base terms are traceable to the documentation's own vocabulary rather than invented. Unioning `ai_tools.name` makes the set grow without a redeploy, which is what that table exists for, and the setting override keeps it operator-editable through the existing `PUT /settings/{key}` route. (c) yields an empty set on a fresh database, reproducing the original defect. (d) makes `ai_tools` decorative. | The default terms are broad, so the stream will match posts that are not about skepticism at all; filtering happens downstream through the popularity gate and the doubt rating. Multi-word terms must be quoted per the X rule syntax, so a term added to `ai_tools.name` with unusual punctuation could produce an invalid rule. A rule set assembled from three sources is harder to reason about than a literal list. |
| DL-045 | Ingestion consumes the X API v2 filtered stream at `GET /2/tweets/search/stream`, with rules registered and reconciled through `POST /2/tweets/search/stream/rules`. | (a) The v1.1 `statuses/filter` endpoint the source's tweepy call targeted. (b) The v2 sampled stream at `/2/tweets/sample/stream`. (c) Polling recent search on a schedule. | v1.1 is legacy and no longer updated, and `statuses/filter` is retired, so (a) is not an option however faithful it would be. The filtered stream is the v2 endpoint that delivers posts matching registered rules, which is exactly what `Stream(...).filter(track=[...])` expressed. (b) delivers a sample of all posts and would need client-side filtering of a firehose. (c) would change the ingestion model from push to pull and multiply request volume. | The filtered stream is a paid-tier capability: the Free tier is suitable only for proof-of-concept work and the Basic tier is the practical production minimum. That is an operational precondition rather than a code concern, recorded here so the constraint is visible. Rule reconciliation is a stateful API interaction this project owns. |
| DL-046 | The stream authenticates with an app-only Bearer Token obtained at runtime by exchanging the consumer key and secret at `POST oauth2/token` under the OAuth 2 client-credentials grant. The OAuth 1.0a user-context properties `scanner.twitter.access-token` and `.access-token-secret` are still declared but are not required for the read path. | (a) OAuth 1.0a user-context request signing, as the source's `OAuthHandler` used. (b) A pre-issued bearer token supplied as its own configuration property. (c) Drop the two user-context properties entirely. | App-only authentication introduces no new mandatory secret, because the consumer key and secret are already declared at `backend/app/core/config.py:L5-6`, and it carries no user context — so it *cannot* publish, which reinforces the never-auto-post invariant at the protocol level instead of only by convention. (a) would sign every request for a capability the read path does not need. (b) would add a secret that can be derived. (c) would reopen the configuration drift those two keys represent, since the source referenced them. | Only one app-only token is valid per application at a time, so two instances exchanging credentials can invalidate each other's token; the client must treat a 401 as a signal to re-exchange. The two user-context properties are declared but unused on this path, which a reader could mistake for dead configuration — they exist to close drift. |
| DL-047 | Deferred work is `@EnableScheduling` plus `@Scheduled(fixedDelayString = …)` on a `@Component`, with no broker and no queue. Delay semantics are fixed **delay**, not fixed rate. | (a) Keep Celery's shape with a real broker such as Redis or RabbitMQ. (b) `spring-boot-starter-integration`. (c) `@Scheduled(fixedRateString = …)`. (d) A `while (true)` loop on a dedicated thread, mirroring the source literally. | The source's scheduler is a `while True` loop that sleeps at the end of each pass at `backend/app/tasks/response_generation.py:L41-50`, so the interval runs from the end of one pass to the start of the next — which is exactly `fixedDelay`; `fixedRate` measures start-to-start and would change the pacing whenever a pass ran long. The Celery application at `:L8` was declared with no broker configured, so `.delay()` could never have dispatched anything; introducing a broker would add infrastructure the prompt forbids. (b) is named by the prompt as an option but adds an integration framework for one periodic call. (d) reintroduces a thread this project would have to manage, and the source's loop blocked application startup outright. | Scheduling is in-process, so two instances both run the pass and can generate duplicate responses; the source had the same property. There is no retry, no dead-letter queue and no visibility into a failed pass beyond its log record. |
| DL-048 | A path identifier that no row matches — including a value that is not a number at all — yields HTTP 404 with the route's own not-found body. Controllers therefore declare `@PathVariable String`. | (a) Declare `@PathVariable Long` and let the framework answer 400 for a non-numeric value. (b) Declare `Long` and add a handler mapping the type-mismatch to 404. (c) Validate the identifier's shape and answer 422. | Flask's default path converter is a string, so `/tweets/<tweet_id>`, `/tweets/<tweet_id>/analyze` and `/responses/<response_id>` all accepted non-numeric input, passed it to the service, received `None` and returned 404. (a) would answer 400 or 500 for the same input, which is a contract break IR1 forbids. (b) reaches the right status through a mechanism that is easy to lose in a refactor, and the mismatch exception would have to be distinguished from every other one. (c) invents a status the source never returned. | A caller cannot distinguish "malformed identifier" from "no such row", which is exactly the source's behaviour and therefore intended. Each controller carries an explicit test for the non-numeric case so the behaviour cannot regress silently. |
| DL-049 | `tweets.id` stays a generated surrogate key and the X post identifier is not carried in it. No natural-key column is declared, so ingestion performs no de-duplication. The same rule governs `responses.id`. | (a) Overload the X post identifier onto the primary key. (b) Add a unique natural-key column holding the X post identifier. (c) De-duplicate by querying for an existing row before each insert, without adding a constraint. | `backend/app/db/models.py:L10` declares an autoincrement-shaped `Column(Integer, primary_key=True)`, and the source persists nothing at all — `backend/app/tasks/tweet_monitoring.py:L29` abandons the constructed object at a TODO — so there is no de-duplication behaviour to preserve, only one that would have to be invented. (a) would change the column's semantics and risk collisions with the generator. (b) is a schema change, and AAP G2 forbids adding a `UNIQUE` constraint or a new column. (c) is a lost-update race without the constraint (b) would add, so it would give the appearance of de-duplication without the guarantee, which is worse than not claiming it. | A post redelivered by the filtered stream — after a reconnect, or because it matches more than one rule — is stored more than once, and the analytics counts of DL-041 include the duplicates. This is an accepted limitation of the parity mandate rather than an oversight. The extension path is a unique natural-key column, which is a schema change and therefore a separate decision. |
| DL-050 | Bean Validation expresses required-versus-optional only. `@NotNull` appears on exactly two request-body components — `CreateResponseRequest.tweetId` and `UpdateSettingRequest.value` — and nowhere else. No `@Size`, `@NotBlank`, `@Min`, `@Max` or `@Pattern` appears on any DTO or entity, and no entity column carries `nullable = false`, `unique` or a bound beyond what DL-068 and DL-069 record. | (a) Add length, pattern and range constraints wherever a value plausibly has bounds. (b) Add `@NotBlank` instead of `@NotNull` on the two components. (c) Drop `spring-boot-starter-validation` and hand-write the two guards. | The Pydantic schemas declare types and optionality only, with no length, pattern or range constraint anywhere, so IR9 makes this validation parity rather than validation improvement. The two `@NotNull` annotations correspond to the only two guards the source actually had: `backend/app/api/responses.py:L40-41` and `backend/app/api/settings.py:L17-18`, both of which test for absence. (b) would reject an empty string, but `backend/app/api/settings.py:L17` tests `is None`, so the source *accepts* an empty value — `@NotBlank` would be a behaviour change. (c) would remove a starter the prompt names and that is genuinely used at those two points. | The API accepts values a stricter contract would reject: an empty setting value, an arbitrarily long content string, a tweet identifier of any shape. That is deliberate parity, and it means input hygiene rests on the persistence layer and on the identifier handling in DL-048. |
| DL-051 | CORS stays fully permissive, reproducing `CORS(app)` with no arguments through a `CorsConfigurationSource` bean. | (a) Restrict allowed origins to a configured list. (b) Restrict allowed methods and headers to what the eleven routes use. (c) Drop CORS handling entirely. | `backend/app/main.py:L20` calls `CORS(app)` with no arguments, which allows every origin, method and header; tightening it would break the frontend, which is out of scope and cannot be adjusted to match, and it is a behaviour change nobody requested. (a) and (b) are what a hardening pass would do — this is not one. (c) would break the browser client outright. | Any origin can call every route from a browser. The mitigating factor is that authentication is now genuinely enforced (DL-021) and the token is a bearer token rather than a cookie, so a permissive policy does not by itself enable a cross-site request with the user's credentials. Tightening it later is a one-bean change. |
| DL-052 | Logging is the SLF4J and Logback baseline that ships with the Boot starter. Structured `INFO`, `WARN` and `ERROR` records are emitted at each translation point, around every external call and on every stream reconnection. No `logback-spring.xml` is added. Credential-bearing values are rendered as the fixed marker `***REDACTED***`. | (a) No logging at all, matching the source exactly. (b) A custom `logback-spring.xml` with a JSON encoder. (c) An audit store, a Cloud Logging sink or a retention policy. (d) A correlation-identifier filter. | The retired backend had no logging framework at all — not one `logging`, `logger` or `getLogger` reference under `backend/` — so (a) would be faithful but would leave a service with external integrations and a background stream completely unobservable. Because Spring Boot bundles SLF4J and Logback with the starter, a baseline costs zero additional dependencies, which is what makes it a translation rather than a new capability. (b), (c) and (d) are each a new capability with its own configuration surface, and the prompt's scope boundary excludes an observability stack. | Log output is plain text on stdout, so a log aggregator has to parse it. Without a correlation identifier, interleaved records from concurrent requests cannot be grouped. The redaction marker is repeated across several types and must stay consistent if it is ever changed — see DL-063 and DL-067. |
| DL-053 | `scripts/deploy.sh` changes one line only: `npm run build`, executed inside `cd backend`, becomes `mvn clean package`. Every other line — the `gcloud builds submit`, the `gcloud run deploy`, the Cloud Function, the Pub/Sub topics, the Firestore rules and the services-describe call — is left exactly as it is. | (a) Rewrite the script to match the Java build end to end. (b) Leave the script untouched. (c) Delete the script. | The prompt instructs that `scripts/deploy.sh` be left structurally alone except where it directly references the backend build or image, and `npm run build` inside `backend/` is precisely such a reference — it was wrong for a Python backend and is wrong for a Java one. (a) exceeds the carve-out. (b) leaves a command that cannot succeed. (c) removes an operational file the prompt keeps. | The script still contains unrelated pre-existing problems, which are deliberately left broken; a reader could mistake that restraint for an oversight, which is why it is recorded. The one changed line assumes Maven is present on the deploying host. |
| DL-054 | `scripts/setup_environment.sh` is left untouched, including the `pip install -r backend/requirements.txt` line at `:L12` and the `cp .env.example .env` line at `:L26`. | (a) Change the `pip` line to a Maven command. (b) Delete the script. (c) Replace the whole script with a Java-oriented equivalent. (d) Add the missing `.env.example` the script copies. | The AAP names the in-scope operations files explicitly and instructs that scripts be left structurally alone except where they reference the backend build or image; this script references neither an image nor the build the CD path uses, so no line of it falls inside that carve-out. Both files it names — `backend/requirements.txt` and `.env.example` — were already absent on the source branch, so the script could not run before this work and its failure mode is unchanged. Alternatives (a) to (c) are edits the AAP scope does not authorise, and (d) would add a configuration file the AAP does not list. | A reader could reasonably expect the `pip` line to change alongside the Dockerfile and the CI workflow, and its retention reads as an oversight unless this row is consulted. The script remains non-functional. |
| DL-055 | `backend/.gitignore` and `backend/.dockerignore` are created, each containing only `target/`. | (a) A single root `.gitignore`. (b) A comprehensive ignore file covering IDE metadata, OS files and logs. (c) No ignore file, relying on committing carefully. | The repository has no root `.gitignore` and the Python tree carried none, so Maven build output — which is new to this repository — would otherwise show as untracked and could be committed. A `.dockerignore` matters specifically because the Docker build context stays `./backend`, so `target/` would be uploaded on every build. Both are scoped to the module rather than the repository root, because the module is what produces the output. (b) would ignore paths this project has no evidence anyone needs ignored. | Two files must stay in step; a build directory renamed in the POM would need both updated. IDE and OS artifacts are still not ignored, so a contributor's local files can appear in `git status`. |
| DL-056 | The Docker build context stays `./backend` and the Dockerfile is named out of context with `-f infrastructure/docker/Dockerfile.backend`. The `COPY` instructions address `pom.xml` and `src` relative to that context. | (a) Move `Dockerfile.backend` into `backend/` so no `-f` is needed. (b) Widen the context to the repository root and address `backend/pom.xml` and `backend/src`. (c) Keep the context at `./backend` and copy the whole tree with `COPY . .` as the source Dockerfile did. | `infrastructure/docker/` is where both Dockerfiles live on the source branch and `cd.yml:L36` already passes `./backend` as the context, so naming the file out of context is the smaller of the two possible edits and leaves the frontend Dockerfile's location undisturbed. Alternative (a) moves a file the AAP lists at its existing path. Alternative (b) enlarges the build context to the whole repository, which would send `frontend/node_modules` to the daemon. Alternative (c) would copy `target/` into the image, which is what `backend/.dockerignore` exists to prevent (DL-055). | The Dockerfile path and the build context are specified in two different places, so a future move of either has to update both. A build run from `backend/` without `-f` finds no Dockerfile. |
| DL-057 | The `flake8` and `mypy` steps are removed from the CI backend job rather than replaced by a Java linter, and `pytest` and `python -m build` are replaced by the single command `mvn -B clean verify`. | (a) Add Checkstyle, SpotBugs or PMD as the Java equivalent of `flake8` and `mypy`. (b) Add `mvn -B compile` as a separate lint-like step before the test step. (c) Keep the Python steps and let them fail. | Java compilation is itself the type check that `mypy` approximated, and `mvn clean verify` runs compilation, tests and packaging in one lifecycle, so a separate step would re-run work already done. Adding a static-analysis plugin would be a new capability with its own rule set to curate, which the prompt's scope boundary excludes. (c) would keep the pipeline red for a tree that has no Python left in it. | Style and bug-pattern analysis is not enforced in CI, so a convention drift is caught only in review. `mvn clean verify` is a single opaque step: when it fails, the log has to be read to learn whether compilation, a test or packaging failed. |
| DL-058 | Code comments carry facts and a pointer only: the source path and line range, a faithful-port or net-new marker, and `see docs/DECISION_LOG.md` with the governing identifier where one is assigned. No comment states a reason, an alternative, a trade-off or a risk. | (a) Follow the prompt's translation-point instruction literally and let each comment explain its own choice. (b) Put rationale in code and reduce this log to an index. (c) Omit the provenance markers entirely, since Rule 1 makes this log the source of truth. | Rule 1 forbids rationale in code comments and declares this log the single source of truth for "why", while the prompt separately asks for a comment at every translation point marking whether a construct is a faithful port or net-new. Both are satisfiable because they govern different content: provenance is a fact, rationale is reasoning. A source pointer plus an identifier is metadata that makes this log *reachable from the code*, which serves Rule 1's own purpose — a reader who lands on a class can find the reasoning — rather than undermining it. (a) would violate Rule 1 outright. (b) inverts it. (c) would discard the traceability the prompt asks for and that `docs/TRACEABILITY_MATRIX.md` is checked against. | Reasoning is one indirection away from the code, so a reader has to open this file to learn why a surprising line exists — which is the trade the rule makes deliberately. An identifier cited in a comment and never populated here is a defect, which is why the completeness invariant at the top of this file exists. |
| DL-059 | The pre-existing divergences between the React frontend and the backend contract are left exactly as they are. The Java service serves the eleven routes unprefixed, names the page-size parameter `per_page` and emits `snake_case` member names, while `frontend/src/services/api.ts:L25` calls `/api/tweets?page=&perPage=`, the Zod schemas type `likeCount`, `createdAt`, `doubtRating`, `quotedTweetId`, `userId`, `aiToolsMentioned`, `generatedAt`, `isApproved` and `tweetId` in camelCase, `frontend/src/services/api.ts:L43` calls a `generate-response` route no backend route declares, and `frontend/src/utils/api.ts:L13-16` imports a module that does not exist. | (a) Adopt the `/api` prefix and `perPage` so the frontend's calls resolve. (b) Serialise camelCase so the Zod schemas parse. (c) Add the `generate-response` route. (d) Repair the frontend instead of the contract. | AAP G1 requires the eleven routes to be served unprefixed with the query-parameter names and status codes the source declares, and 0.2.3 names each of these divergences as explicitly not fixed. Both sides are therefore frozen and no legal edit closes any of them: (a), (b) and (c) each change a contract G1 freezes — (b) is the specific case DL-022 records — and (d) edits files 0.2.3 places entirely out of scope. The divergences also predate this work: they exist between the retired Flask handlers and the same frontend, so leaving them is preservation rather than neglect. | The frontend cannot call this backend successfully: every request misses on the path, and a response that did arrive would fail Zod validation on the member names. That is the state the refactor inherited and it is unchanged by it, so a reader who assumes the two halves interoperate will be wrong. Whoever repairs the frontend later must reconcile the paths, the parameter name and the member naming in one change. |
| DL-060 | The twenty Python files under `backend/app/**` and `backend/tests/**` are deleted, not retained beside the Java tree, and they are deleted in the same change that creates the Java module. | (a) Keep them until the Java tree is complete, then delete. (b) Move them to an `archive/` or `legacy/` directory. (c) Keep the three test files as behavioural documentation. | The Python tree and the Java tree occupy the same directory, and each operational file can reference exactly one of them at any moment: the Dockerfile copies either a Python package or a Maven project, the CI job runs either `pytest` or `mvn verify`. Applying only part of the change would leave a repository where neither backend builds, which is strictly worse than the starting state. The files are also not merely superseded but non-importable — there is no `__init__.py` anywhere, the test files import `fastapi.testclient` against a Flask application and reference modules and symbols that do not exist — so (c) would preserve documentation that never described working behaviour. (b) keeps dead code discoverable at the cost of a directory the prompt does not contemplate. | For the duration of the migration the repository has a Java tree that does not yet compile into a runnable jar, with no Python fallback. Every construct the deleted files carried is accounted for in `docs/TRACEABILITY_MATRIX.md` and recoverable from git history, which is what makes the deletion reversible in practice. |
| DL-061 | `entity/Setting.java` maps the two `settings` columns whose names are reserved SQL words with JPA-standard quoted identifiers — `@Column(name = "\"key\"")` and `@Column(name = "\"value\"")` — keeping the physical names exactly as `backend/app/db/models.py:L42-43` declared them. `settings.description`, the table name `settings` and every `ai_tools` column stay unquoted. `spring.jpa.properties.hibernate.auto_quote_keyword: true` is additionally set in `src/main/resources/application.yml`; the mapping does not rely on it. | (a) Plain `@Column(name = "key")` / `@Column(name = "value")`, relying on `auto_quote_keyword` alone. (b) Rename the columns, e.g. `setting_key` / `setting_value`. (c) Set `hibernate.globally_quoted_identifiers=true` for the whole application. (d) Remove `auto_quote_keyword` and rely on the explicit mapping alone. (e) Append `NON_KEYWORDS=KEY,VALUE` to the H2 JDBC URL. (f) Backtick quoting, ``@Column(name = "`key`")``. | `KEY` is reserved in H2 2.x and in MySQL, and `VALUE` is reserved in the SQL standard and in H2, and (a) fails **even with `auto_quote_keyword` enabled** — measured, not assumed: with the property set to `true`, Hibernate 6.6.53 emits `create table q_plain (key varchar(255) not null, "value" clob, primary key (key))` on H2 and the same shape on PostgreSQL, quoting `value` but leaving `key` bare, and real H2 2.3.232 then rejects that statement with `Syntax error … expected "identifier"`. Only MySQL's dialect quotes both under the property. The explicit JPA-standard form produced correctly quoted identifiers in all six measured combinations of three dialects and the property on and off, and combining it with the property causes no double quoting. (b) breaks the column-name parity mandate (AAP G2 / TR-3) and would falsify the traceability matrix. (c) changes identifier case-sensitivity for all four tables and twenty columns to solve a two-column problem. (d) is defensible on the strength of the measurements, but the property is what covers any future keyword-named column and the mapping is unaffected by it either way, so both are kept. (e) is vendor-specific, fixes only H2, and would have to be threaded through a `DATABASE_URL` that IR4 keeps opaque and vendor-neutral. (f) is MySQL-flavoured, whereas Hibernate renders the JPA-standard form with each dialect's own quote character — measured as `"key"` on H2 and PostgreSQL and `` `key` `` on MySQL — so one mapping covers all three supported vendors. | Quoted identifiers are case-sensitive: on H2 the columns exist as lower-case `key` and `value` while unquoted identifiers are folded to upper case (measured: the created `settings` table has columns `DESCRIPTION`, `key`, `value`). Hand-written native SQL must therefore quote them — `select "key", "value" from settings` — and schema-metadata assertions must expect the lower-case names. Nothing inside the application is affected, because Hibernate emits the quoted form consistently in DDL and DML. Two mechanisms now guard the same two columns, so someone removing `auto_quote_keyword` as redundant would be right about these columns and wrong about any future one. The `not null` that appears on the primary key in the generated DDL is JPA's own `@Id` semantics, not an added constraint. |
| DL-062 | `service/SentimentAnalysisService.calculateDoubtRating` returns `10.0` for a `Double.NaN` score through an explicit guard placed ahead of the transcribed expression; the transcribed lines themselves stay byte-for-byte `(1 - sentimentScore) * 5` followed by `Math.max(0.0d, Math.min(10.0d, doubtRating))`. | (a) Keep `Math.max`/`Math.min` alone, letting NaN propagate. (b) Replace the clamp with ordered conditional expressions that reproduce Python's comparison fall-through across the whole domain. (c) Reject a NaN score with an exception. (d) Substitute a neutral `5.0`. | Parity is measured against the source expression `max(0, min(10, doubt_rating))` at `backend/app/services/sentiment_analysis.py:L32`. Python's built-ins return their first argument when a comparison is false, so `min(10, nan)` is `10` and `max(0, 10)` is `10`: the source yields `10` for a NaN score, confirmed by executing the expression. Java's `Math.min`/`Math.max` instead propagate NaN, so (a) returns NaN from a method contracted to return `0.0`–`10.0` and lets a non-finite value reach `tweets.doubt_rating`. NaN is the *only* input where the two differ — ±0.0 and ±∞ agree — so one guard restores bit-identical behaviour over the full `double` domain (verified against a python3 reference for 18 inputs). (b) is equally correct but discards the two transcribed lines the specification fixes verbatim (min nested inside max, no `Math.clamp`, no reordering), which makes the port harder to diff against its source. (c) and (d) invent behaviour the source does not have. | The bound `10.0` now appears twice in the method — as the upper bound and in the guard — and both must move together if the scale ever changes. A NaN score is mapped to the maximum doubt rating rather than reported as an anomaly; the score returned by the Natural Language API is logged at `INFO` by `analyzeSentiment`, so the input stays observable. |
| DL-063 | `config/DatabaseUrlTranslator.TranslatedDatabaseUrl.toString()` renders all three components — `jdbcUrl`, `username` and `password` — as the single fixed marker `***REDACTED***`, returning the same text for every instance. | (a) The compiler-generated record `toString()`. (b) Redact only the password, printing the URL and username in clear. (c) Emit a sanitised URL with the user-info component and credential-bearing query properties stripped. (d) Render the markers tri-state, distinguishing an absent value (`null`) and an empty value (`""`) from a present one. (e) Drop the override and forbid logging the record by convention. | A value that arrives already in JDBC form is passed through exactly as supplied (DL-064) and may embed credentials in its user-info component or in query properties such as `?user=…&password=…`, so any rendering of the URL text can disclose a credential (CWE-532); (a) and (b) both do, and (b) additionally printed the username, which is credential material. (c) would require a second security-critical parser and would still reveal host and database name for no diagnostic gain. (d) still discloses whether a credential is configured at all, which is the same disclosure that was removed from `config/ScannerProperties` for its fourteen redacted components; using one marker keeps a single convention across both types and cannot leak by construction. (e) depends on every present and future caller remembering, and the record is the natural thing to interpolate into a startup log or an assertion failure. | Someone diagnosing a connection failure cannot read the URL out of a log record that interpolates this record, and cannot tell from that record whether a credential was configured. The values remain available through the `jdbcUrl()`, `username()` and `password()` accessors for code that genuinely needs them, and `translate` still logs the resolved vendor at `INFO` on the translation path. |
| DL-064 | `config/DatabaseUrlTranslator.translate` tests the value exactly as supplied for the literal lower-case prefix `jdbc:` using `String.startsWith`, and returns that same value unchanged. No value is trimmed, case-folded or otherwise normalised anywhere in the class, including on the parse path. | (a) Trim first and match the prefix case-insensitively, returning the trimmed value. (b) Trim for detection only but return the original value. (c) Keep trimming and document it as intended behaviour. | The specified contract is that a JDBC value is returned unchanged and is "not parsed, normalised or stripped" (AAP §0.6.5.1 / IR4), which a trimmed or case-expanded return value does not satisfy — and the class Javadoc already claimed that behaviour while the code trimmed, so the two disagreed. (b) would hand a whitespace-padded URL to the DataSource it had just recognised, converting a clear startup failure into a driver-level one. (c) makes silent correction part of the contract and keeps the class free to alter a value the specification says it must not touch. Consuming the value exactly as supplied also matches SQLAlchemy, which rejects a padded URL rather than trimming it, so failing fast is the source behaviour as well. | A `DATABASE_URL` padded with whitespace, or written with an upper-case `JDBC:` prefix, now fails at startup instead of being silently corrected: padded input raises `DATABASE_URL is not a parseable URL (Illegal character in scheme name)` and an upper-case prefix raises the unsupported-scheme error naming the supported schemes. Both fail fast at boot with an actionable message and neither can produce a half-configured DataSource, but an operator who previously relied on the trim will see a new startup failure. |
| DL-065 | `exception/BadRequestException`, `exception/NotFoundException` and `exception/ResponseGenerationException` each declare `private static final long serialVersionUID = 1L;` and each is `final`. | (a) Carry `@SuppressWarnings("serial")` and declare no identifier. (b) Leave the `javac -Xlint:all` `[serial]` warning in place. (c) Avoid `RuntimeException` so the types are not serializable. | No instance of these types crosses a serialization boundary — each is created, thrown, caught by the error-handling advice in the same JVM and rendered as a JSON body — so any of (a) and a declared identifier are equally sound on the merits, and the tree must be clean under `-Xlint:all`, which rules out (b). A declared identifier was chosen because it is the resolution the review named first, because it removes the warning at its cause rather than hiding the category, and because these three types now expose a closed set of client-visible messages through static factories (DL-066): a fixed identifier makes the wire-literal contract explicit rather than leaving it to a compiler-generated value that changes whenever the class shape does. (c) is not available — an unchecked exception has to extend `RuntimeException`, which is `Serializable`. | The literal `1L` is a value no consumer checks today, so it carries no information; if one of these types is ever genuinely serialised across versions the identifier must be maintained deliberately. Nothing in the service serialises an exception, and introducing that would be an architectural change carrying its own entry. |
| DL-066 | `exception/BadRequestException` and `exception/NotFoundException` declare a private constructor and expose their approved wire literals only through public constants and one static factory per literal (`tweetIdRequired()`, `updateDataRequired()`, `noValueProvided()`, `tweetNotFound()`, `responseNotFound()`, `responseNotFoundOrUpdateFailed()`, `settingNotFound()`); `exception/ResponseGenerationException` carries one fixed message and exposes only `()` and `(Throwable)`. A triggering throwable attaches through `withCause(Throwable)` or the cause constructor and is reachable only through `getCause()`. | (a) Keep a public `String` constructor, as the literal reading of AAP §0.4.1.7 implies, and rely on call sites passing only the approved literals. (b) Validate a supplied message against an allow-list at construction time. (c) Map arbitrary messages to a generic body inside the error-handling advice. | A public `String` constructor lets any caller — including one relaying a database driver message, a vendor API response or a value read from configuration — put arbitrary server-side text into the `error` value of a client response (CWE-209). (a) makes that a convention rather than a guarantee. (b) adds a runtime check for something the type system can make unrepresentable. (c) moves the guarantee into a component that does not exist yet and would still let the wrong text reach a log-and-rethrow path. Closing the constructor makes the eight wire literals the complete client-visible message set by construction, and each factory carries its literal unaltered so every one still round-trips through `getMessage()` character for character, which is what AAP §0.4.1.7 and IR1 actually require. | `new NotFoundException("…")` and `new BadRequestException("…")` no longer compile; call sites must use the factories. No source in the module constructs these types today, so nothing needed changing, but a later checkpoint adding controllers or services must use the factory names. Adding a new client-visible 4xx literal now requires editing the exception type as well as its call site. |
| DL-067 | `dto/LoginRequest` overrides `toString()` to render **both** components as `***REDACTED***` and binds its `password` member write-only (`@JsonProperty(access = WRITE_ONLY)`); `dto/TokenResponse` overrides `toString()` to render `accessToken` as `***REDACTED***` while rendering `tokenType` and `expiresIn` as they are. Deserialization and serialization are otherwise untouched: `LoginRequest` still binds both JSON members and `TokenResponse` still emits `access_token`, `token_type` and `expires_in`. | (a) Rely on a documented "instances of this type are never logged" invariant, which is what the records originally carried. (b) Redact the credential component only, leaving the submitted principal name in clear. (c) Redact without the write-only binding. (d) Omit the credential component from the record and pass it separately. | A record's compiler-generated `toString()` renders every component, so a plain interpolation into a log line, an assertion message or an exception message discloses a submitted password or a live bearer token (CWE-532); (a) makes that a convention that every present and future caller has to remember, which is the fragility the review flagged. (b) still discloses the principal name, which identifies the single configured account. (c) leaves the plaintext able to reach a response body if the record is ever serialised, so the write-only binding is defence in depth; it was measured not to affect deserialization. (d) changes the wire contract of an endpoint the plan fixes. `tokenType` and `expiresIn` stay visible because they are not secret and are what makes a token response diagnosable. | A rendered `LoginRequest` no longer identifies which principal a failed authentication attempt used, so diagnosis relies on the authentication layer's own logging. The marker text `***REDACTED***` is repeated across `ScannerProperties`, `TranslatedDatabaseUrl` and these two records and must stay consistent if it is ever changed. |
| DL-068 | Every character column the source declares unbounded declares `length = Integer.MAX_VALUE`, so generated DDL uses each vendor's unbounded character type instead of JPA's implicit `varchar(255)`. There are ten: `tweets.content`, `tweets.media`, `tweets.quoted_tweet_id`, `tweets.user_id`, `tweets.ai_tools_mentioned`, `responses.content`, `ai_tools.name`, `ai_tools.description`, `settings.value` and `settings.description`. `settings.key` is the single deliberate exception — see DL-069. | (a) Declare no `length` and accept the JPA default of 255. (b) `org.hibernate.Length.LONG32`, the numerically identical Hibernate constant. (c) `@JdbcTypeCode(SqlTypes.LONGVARCHAR)`. (d) `org.hibernate.Length.LONG` (32600). (e) `@Lob`. (f) `columnDefinition = "text"`. | `backend/app/db/models.py:L11,L15,L16,L17,L18,L24,L36-37,L43-44` declare all ten as a bare `Column(String)`, which SQLAlchemy renders as an unbounded `VARCHAR`, so (a) adds a 255-character bound that AAP G2 and TR-3 forbid — and because `ddl-auto=update` generates the schema (DL-026), that bound is written into the physical database rather than merely mis-describing a Java field. It is also a live truncation risk on the largest of them: a post's `content` and its comma-delimited `ai_tools_mentioned` are both free text. Measured against Hibernate 6.6.53: `Integer.MAX_VALUE` yields `clob` on H2 2.3.232, `text` on PostgreSQL 16.14 and `longtext` on MySQL 8.4.11, and every one of the ten columns was then read back from JDBC metadata on H2 and PostgreSQL at a capacity of 2147483647. (b) is exactly the same number and arguably more self-describing, but it would put an `org.hibernate` import into entities that otherwise depend only on `jakarta.persistence`. (c) and (d) both measured as `varchar(32600)` on H2 and PostgreSQL — still bounded, so neither achieves parity. (e) changes the mapping to LOB semantics rather than the DDL type. (f) is not portable across the three supported vendors. | On H2 the columns become `CHARACTER LARGE OBJECT`, which is not a character-varying type: H2 refuses to index a CLOB, so none of these ten columns can carry an index or a unique constraint on H2. Neither is wanted — added indexes and constraints are out of scope — and equality, `ORDER BY`, `LIKE` and `setString` parameters were all measured to work on an H2 CLOB. A raw JDBC read returns such a column through a `Clob` handle rather than a `String`, which `JpaMappingIntegrationTest` accounts for. Because `ddl-auto=update` never widens an existing column, a database created before this change keeps `varchar(255)` and silently diverges from a freshly created one. |
| DL-069 | The `settings.key` primary-key column declares an explicit `length = 255`, held in the named constant `Setting.KEY_LENGTH`, rather than the unbounded mapping the other four character columns use. | (a) `length = Integer.MAX_VALUE`, consistent with DL-068. (b) Declare no `length` and leave the same 255 implicit. (c) `length = 768`, the widest value MySQL can index. (d) Replace the natural key with a generated surrogate and make `key` an ordinary column. (e) Rename the column. | An unbounded primary key is not creatable on two of the three supported vendors — measured: H2 2.3.232 rejects it with `Feature not supported: "Index on column: ""key"" CHARACTER LARGE OBJECT NOT NULL"`, and MySQL 8.4.11 rejects it with `ERROR 1170 … BLOB/TEXT column 'key' used in key specification without a key length` — so (a) would break the test database and a supported production vendor. The bound is therefore unavoidable, and declaring it explicitly is what makes it a deliberate, reviewable decision with a pointer here instead of an invisible framework default, which is why (b) was rejected even though it produces an identical schema. (c) was measured to work at exactly MySQL's 3072-byte limit with utf8mb4 and to fail at 769 characters, so it sits on the boundary and would fail outright under the COMPACT or REDUNDANT row formats; it also changes the physical width, and because `ddl-auto=update` never widens an existing column that change would apply only to newly created databases, silently diverging them from existing ones. (d) and (e) both break the parity mandate that `settings.key` is the primary key and is named `key`. | A setting key longer than 255 characters is rejected at insert time. Every key the service seeds and every key an operator would create through `PUT /settings/{key}` is a short identifier such as `tweet_popularity_threshold`, so the bound is not reachable in practice. This is a documented deviation from the "no added length bound" rule, accepted because the alternative is a table that cannot be created. |
| DL-070 | `entity/AiTool.id` is typed `Long` and `repository/AiToolRepository` is declared `JpaRepository<AiTool, Long>`, matching the literal text of AAP 0.4.1.4 and the identifier type of `entity/Tweet` and `entity/Response`. | (a) `Integer`, the one-to-one type mapping of `Column(Integer, primary_key=True)` at `backend/app/db/models.py:L35`. (b) `Long` on the field with `@JdbcTypeCode(SqlTypes.INTEGER)` to keep a 32-bit column. (c) `Long` plus `columnDefinition = "integer"`. | AAP 0.4.1.4 states `JpaRepository<AiTool, Long>` and TR-3 lists `Integer`→`Integer` and `Integer`→`Long` as both permitted, so `Long` is the mapping the frozen plan specifies; the AAP is authoritative and no entry in this log can override it, which is what (a) — delivered first — attempted on parity grounds. `Long` also makes all three identifiers behave alike, so DL-023's conversion to a `String` at the DTO boundary is uniform, and `AnalyticsService` reads only `count()` from this repository, so no arithmetic depends on the width. Measured: a `Long` identifier generates `bigint` on H2 2.3.232, `int8` on PostgreSQL 16.14 and `BIGINT` on MySQL 8.4.11, with a full Hibernate round trip returning a generated `Long` on each. (b) reaches a 32-bit column but adds a Hibernate-specific annotation to an entity that otherwise uses only `jakarta.persistence`, and leaves the field and the column disagreeing. (c) is not portable across the three vendors. | `ai_tools.id` is a 64-bit column where `backend/app/db/models.py:L35` declared a 32-bit one. It is the only column type in the schema that differs from the source, it is a widening no stored value can fail, and AAP 0.4.1.4 mandates it. Because `ddl-auto=update` never narrows a column, a database created before this change keeps `integer` and diverges from a freshly created one. The wire contract is unaffected: the identifier serialises as a `String` either way (DL-023). |
| DL-071 | `config/DatabaseUrlTranslator` emits a per-vendor JDBC authority prefix held on a private `Vendor` enum: `jdbc:postgresql://`, `jdbc:mysql://` and — for H2 — `jdbc:h2:tcp://`. | (a) The previous uniform `jdbc:<vendor>://` form for all three. (b) `jdbc:h2:ssl://` for H2. (c) Drop `h2` from the supported scheme set and let an `h2://` value be rejected. (d) Require the operator to supply an H2 URL in JDBC form so it takes the pass-through path. | The uniform form is silently wrong for H2, and its failure mode is the dangerous kind: H2 has no `//` connection mode, so it reads `//host:9092/database` as an embedded file name. Measured against a real H2 TCP server, `jdbc:h2://localhost:9092/codeskeptic` reported a successful connection while actually creating a local file database at `/localhost:9092/codeskeptic.mv.db` — it never reached the server, so an operator would have been reading and writing a private, empty database with no error anywhere. `jdbc:h2:tcp://` is H2's documented remote mode and was measured to connect, round-trip a row and materialise the database in the server's own base directory, with no local file created. (b) is the TLS variant and fails the handshake against a plain TCP server, which is the common deployment. (c) and (d) would work but would remove a vendor the POM ships a driver for and that the test profile uses. | H2's default TCP port, 9092, is now implied when the value declares no port; an H2 server on another port must say so. An `h2://` value that was previously "working" was in fact addressing a local file, so the fix changes observable behaviour — an operator relying on that accident will now genuinely reach the server and find different data. Only the plain TCP mode is reachable through translation; `ssl:`, `mem:` and `file:` forms must be supplied in JDBC form and take the pass-through path. |
| DL-072 | `TranslatedDatabaseUrl.jdbcUrl()` never carries credential material. On the parse path, a recognised credential property is removed from the query and fills whichever credential the user-info component left unset; on the `jdbc:` pass-through path a value carrying credential material is rejected outright. One detector — user-info in an authority, plus any `?`, `&` or `;` separated property named `user`, `username`, `password`, `passwd`, `pwd`, `password1`, `password2` or `password3`, matched case-insensitively — enforces the invariant on both paths, and runs again on the reassembled URL as a backstop. | (a) Rely on `TranslatedDatabaseUrl.toString()` redaction alone (DL-063). (b) Strip credentials out of a `jdbc:` value instead of rejecting it. (c) Reject on both paths, extracting nothing. (d) Extract on both paths, parsing the JDBC form too. (e) Match property names by prefix rather than exactly. | Redaction protects this project's own rendering of the record but not a connection pool's or a driver's diagnostics, which receive the URL text itself — so a credential left inside the URL can still reach a log through code this project does not own (CWE-532). (b) and (d) are ruled out by AAP §0.6.5.1 and DL-064, which require a `jdbc:` value to be returned unchanged and never parsed, normalised or stripped; detection is not modification, and the AAP's own algorithm already ends in fail-fast, so extending fail-fast is consistent with it. (c) would reject the SQLAlchemy-style `?user=…&password=…` form that the translation exists to normalise. (e) would match legitimate properties such as `passwordAuthentication` and `userTimezone`; exact matching was verified against five look-alike properties, including `connectionAttributes=user:bob`, which must survive untouched. The behaviour is pinned by forty cases covering the AAP's six, every credential-bearing shape, both no-normalisation shapes and eight previously supported URL forms. | A `DATABASE_URL` in JDBC form that embeds credentials now fails at startup; the remedy is the `scheme://user:password@host/database` form, which is the convention `backend/app/core/config.py:L9` already used, and the failure message names it. A `user` or `password` query property is no longer passed to the driver inside the URL — it is supplied through `username()` and `password()` instead, which is equivalent for both supported drivers but is a change in where the value travels. The recognised-name set is a fixed list, so a driver-specific credential property outside it would not be detected. |
| DL-073 | `service/SettingsService` tolerates degenerate input rather than letting a framework exception escape: a `null` key given to `updateSetting` is reported as absent through `NotFoundException.settingNotFound()` without reaching the repository, and the `stream_keywords` seed value is rendered from the configured terms with an absent `scanner.ingestion` group treated as no terms, each term trimmed, and `null` or blank terms dropped before the comma join. | (a) Call `findById(key)` with whatever arrives, which is the literal reading of the call site. (b) Reject a `null` key with `BadRequestException`. (c) Assert the key with `Objects.requireNonNull`, raising `NullPointerException`. (d) Join the configured terms exactly as written, keeping blanks and failing on a `null` term. (e) Normalise the terms by reusing `util/DelimitedStringListConverter`. | Spring Data asserts a non-`null` identifier, so (a) turns a `null` key into `InvalidDataAccessApiUsageException`, which the error-handling advice renders as HTTP 500 — a key that names no row is exactly the 404 branch of `backend/app/api/settings.py:L21-22`, and the Flask route could never produce a `null` key because its path converter always yields a string, so 500 would be a status the source can never return. (b) would emit the wrong literal, since `No value provided` describes the body rather than the key. (c) would surface as 500 as well. On the seed value, (d) would store a leading or trailing space inside a stream rule and would throw on a `null` element, and the trim-and-drop behaviour matches what `util/DelimitedStringListConverter` already does to every other delimited column in this schema, so one convention governs all of them; (e) was rejected because that converter is a JPA `AttributeConverter` for entity attributes and `settings.value` is a plain column this class writes directly, so reusing it would couple a service to a persistence-mapping type for a five-line join. | Both are deviations from the literal reading of the call sites, recorded here so neither reads as an oversight. The `null`-key branch is unreachable through `PUT /settings/{key}`, so it is defence for direct callers only and costs a branch that no route exercises; a caller that passes `null` by mistake now sees `Setting not found` instead of a null-pointer diagnostic, which is less pointed about the real fault. Dropping blank terms means a keyword list consisting only of blanks silently seeds an empty `stream_keywords` value rather than reporting a misconfiguration. |
| DL-074 | The `actions/setup-node` pin in `.github/workflows/ci.yml` stays at `'14'`, exactly as the pre-refactor workflow declared it, and no frontend step in that workflow is altered. | (a) Raise it to `22.23.2`, the version installed on the build host. (b) Raise it to the lowest release satisfying the platform's Node floor, `20.20.2`. (c) Remove the pin and let the runner default decide. | AAP 0.2.1.6 lists the frontend steps of this workflow as deliberately left untouched and 0.2.3 places `frontend/**` entirely out of scope, so every line of the Node.js step is frozen; the AAP is the authoritative reference and it carves out no exception for the runtime pin. The platform's Node floor governs the toolchain this agent runs while building, not the contents of a workflow the AAP declares untouchable — the backend job reads nothing from the Node toolchain, so the floor and this pin never meet. (a) was delivered first and changed a line the refactor had no authority over, which made it a deviation with no authorising requirement rather than a decision. (b) has the same defect at a lower version. (c) makes the toolchain version implicit and unreproducible. | The workflow's frontend job stays exactly as broken as it was before the refactor — `npm ci` with no committed lock file, and `npm run lint` and `npm run type-check` naming scripts `frontend/package.json` does not declare — which AAP 0.2.3 explicitly requires be left that way. A reader may reasonably take the `14` pin for an oversight; this entry is the record that it is not. |
| DL-075 | `service/AnalyticsService` reports a null aggregate as null. A null `avg(...)` reaches `dto/SummaryDto.averageDoubtRating` and `averageLikeCount` unchanged, and a null `avg(...)` or `sum(...)` reaches `dto/TrendsDto.TrendPoint` unchanged, so "no row in scope carried this measurement" is reported as JSON `null` and never as a measured zero. | (a) Substitute `0.0` for a null mean and `0` for a null total before building the records. (b) Treat an empty table as an error and answer with a non-200 status. (c) Coalesce inside the JPQL with `coalesce(avg(...), 0)`. (d) Omit the key entirely when the aggregate is null. | Zero is a value the data genuinely holds — a doubt rating of 0 is the score of a maximally positive post — so (a), which was delivered first, made "no rated tweet" indistinguishable from "every tweet rated 0", and it contradicted both records' own documentation, which declares the mean components nullable. `TweetRepository` documents the null case and assigns it to the caller; the caller's job is to report it, not to erase it. The same reasoning governs the Notion mirror (DL-090) and the wire null policy (DL-080): a value never measured is not a measurement. (b) invents an error for a legitimately empty database and needs a status no source route defines. (c) would make `TweetRepository`'s documented contract untrue and hide the substitution from every other caller of those queries. (d) makes the DL-041 key set conditional, which contradicts it being a contract. | A client must accept `null` for four numeric fields — the two summary means and the two nullable bucket components — and the frontend Zod contracts type numbers as non-nullable, a pre-existing divergence DL-022 leaves unfixed. A clean checkout starts from an empty schema, so the null path is the first one a first boot and `mvn clean verify` both exercise, which makes it the well-tested path rather than an edge case. |
| DL-076 | `POST /responses` reports exactly two outcomes and never 404. `service/ResponseService.generateResponse` rejects a `null` or empty `tweet_id` with `BadRequestException.tweetIdRequired()` and reports every other failure through the single `ResponseGenerationException` literal: an identifier carrying no number, an identifier naming no `tweets` row, a failure raised inside `LlmService` and a failure raised while saving the row. The 400 guard tests `isEmpty()` and not `isBlank()`. On `/responses/{responseId}` the `GET` and `PUT` 404 bodies remain two different strings, reached through `NotFoundException.responseNotFound()` and `NotFoundException.responseNotFoundOrUpdateFailed()`. | (a) Report an identifier naming no `tweets` row as 404 carrying `Tweet not found`, which is what the tweet routes do. (b) Report an unparseable `tweet_id` as 400, since the value arrives in the request body rather than in the path. (c) Use `isBlank()`, so a whitespace-only identifier is rejected as 400. (d) Back both 404 messages with one constant and emit it from both routes. | `backend/app/api/responses.py:L46-49` branches on the truthiness of the value the service returned and has exactly two arms, 201 and 500. The imported module defined none of the four methods, so the only value it could yield for an identifier it failed to resolve was `None`, which reached `:L49`; the route has no 404 arm to port. (a) and (b) would emit statuses that route can never produce, and (a) would additionally emit a literal belonging to `backend/app/api/tweets.py:L32`. (c) is ruled out by the guard's own semantics: `if not tweet_id` at `:L40` is falsy for the empty string alone, and `'   '` is truthy, so a whitespace-only value passed that guard and failed later as an unresolvable identifier; `isBlank()` would move it from 500 to 400. (d) is ruled out by `api/GlobalExceptionHandler`, which copies the message verbatim into `dto/ErrorResponse`: `:L31` and `:L65` are two distinct wire bodies and one constant would change the `PUT` body. | A client posting an identifier that names no row receives 500, which reads as a server fault rather than a client one, and the body carries nothing distinguishing an unknown identifier from an unset OpenAI credential; the distinguishing detail is preserved only in the log, through the cause-carrying constructor. A reader may see the two near-identical 404 constants as duplication and merge them, silently changing the `PUT` body — the class documentation and this entry record that they are deliberate. A whitespace-only `tweet_id` costs a database round trip before failing. |
| DL-077 | `service/ResponseService` tolerates degenerate input rather than letting a framework exception escape. `getPaginatedResponses` reads a `page` below 1 as 1 and a `perPage` below 1 as 10, the two defaults declared at `backend/app/api/responses.py:L11-12`, instead of letting `PageRequest.of` raise. Identifier conversion applies `Long.valueOf` to the raw value with no trimming, so `null`, the empty string, a whitespace-only value, a value carrying a non-digit and a value beyond `Long` range are all reported as absent. `generated_at` is minted in this class as `LocalDateTime.now()` when the row is built, rather than copied from the `ResponseDto` that `LlmService` returns. | (a) Pass both pagination values through unchanged and let `PageRequest.of` raise `IllegalArgumentException`, which is the literal reading of the call site. (b) Reject out-of-range pagination with `BadRequestException`. (c) Trim the identifier before converting it. (d) Copy `generatedAt` from the `ResponseDto` the generator returned. | Flask coerced both query parameters with `type=int` and supplied 1 and 10 when they were absent, but it bounded neither: `request.args.get('page', 1, type=int)` yields `0` or `-4` unchanged, so the absent service module received them. (a) turns those values into `IllegalArgumentException`, which the catch-all advice renders as 500 — a status `GET /responses` has no arm for — and clamping to the route's own declared defaults is the smallest behaviour that keeps the route answering. (b) would emit a 400 literal that route never declared. (c) is ruled out because the string path converter handed the raw segment straight through and trimming would make `' 5 '` and `'5'` name the same row, a normalisation the source never performed; leaving the value untrimmed keeps every non-canonical form on the single absent path. (d) is ruled out because that component is set when the model call returns, before the row exists, while this class owns the column: taking the value where the row is built keeps the stored timestamp and the insert in one instant and holds even if a future `LlmService` leaves the component at another value. | Clamping means a caller sending `per_page=0` receives ten rows instead of a diagnostic, so a client bug of that shape stays invisible, and a caller sending `page=0` silently receives page 1. An untrimmed identifier means `' 5 '` yields 404 on `GET`, 404 on `PUT` and 500 on `POST` rather than resolving row 5. The stored `generated_at` trails the value the generator recorded by the duration of the model call; no consumer compares the two. |
| DL-078 | `api/AuthController.issueToken` catches `AuthenticationException` inside the handler and returns 401 with an empty body, rather than letting the exception propagate out of the handler method. | (a) Let the exception propagate, as the literal reading prescribes. (b) Add an `@ExceptionHandler(AuthenticationException.class)` to `api/GlobalExceptionHandler` returning 401. (c) Return the 401 with a `dto/ErrorResponse` envelope such as `{"error": "Unauthorized"}`. (d) Clear the security context and re-enter the filter chain so `HttpStatusEntryPoint` handles it. | Propagation was measured, not assumed: because `POST /auth/token` is the chain's one `permitAll` matcher, the request reaches the `DispatcherServlet`, which resolves the exception through `ExceptionHandlerExceptionResolver` into `GlobalExceptionHandler`'s `@ExceptionHandler(Exception.class)` before `ExceptionTranslationFilter` can convert it — so a wrong password, an unknown principal, an empty JSON object and an absent body each returned **500 `{"error":"Internal server error"}`**, reporting a client credential error as a server fault. An empty 401 is byte-for-byte what `HttpStatusEntryPoint(HttpStatus.UNAUTHORIZED)` already returns for every other route, so it puts no new string on the wire, whose error strings are a closed set: the eight per-route literals plus the two envelopes of `backend/app/main.py:L31-37`. (a) ships the observed 500. (b) is asserted against by `api/GlobalExceptionHandlerTest.declaresNoHandlerForAnAuthenticationOrAccessDeniedFailure`, and would change the status of every route rather than this one. (c) would invent a twelfth wire literal. (d) would require unwinding into the filter chain from inside a handler. | The 401 carries no diagnostic body, so a client cannot tell an unknown principal from a wrong password or an absent body — deliberate, and identical to the 401 of every other route. The catch is broad: any `AuthenticationException`, including an `AuthenticationServiceException` raised by an infrastructure fault, is reported as 401 rather than 500, so a misconfigured credential store presents as a rejected credential. Because only the exception's simple class name is logged and never the submitted values, that case has to be diagnosed from the WARN record rather than from the response. |
| DL-079 | The `sub` claim of a token minted by `api/AuthController.issueToken` is `Authentication.getName()` of the authentication the `AuthenticationManager` returned, not `dto/LoginRequest.username()` as submitted. | (a) `request.username()`, the raw submitted string. (b) The `UserDetails` principal cast out of `Authentication.getPrincipal()` and read through `getUsername()`. (c) A claim value derived from `scanner.auth.username` read straight from configuration. | The returned authentication is the credential store's own answer, so its name is the resolved principal and is non-null on every authentication the manager completes — which matters because `security/JwtService.generateToken` rejects a null subject with `NullPointerException`. Against the current `InMemoryUserDetailsManager` the two values coincide, so this is a robustness choice and not an observable change: the minted subject is `admin` either way. (a) would carry a caller-controlled string into a signed claim and would put a `null` subject one absent JSON member away from an unhandled `NullPointerException`. (b) reaches the same value through a cast that couples this class to the store's principal type. (c) would ignore the authentication entirely and mint the same subject for any credential the manager happened to accept. | If the credential store is ever replaced by one that normalises names — lower-casing, or trimming — the `sub` claim will carry the normalised form rather than the submitted form, so a client that compares the claim against what it typed can see a difference. `security/JwtAuthenticationFilter` resolves the claim back through the same store, so the round trip stays consistent. |
| DL-080 | Every scalar component of `dto/TweetDto` is a nullable reference type — `likeCount` is `Integer`, `doubtRating` is `Double`, and `id`, `content`, `createdAt`, `quotedTweetId` and `userId` carry no non-null requirement — and `dto/ResponseDto` requires only `id`, leaving `content`, `generatedAt` and `tweetId` nullable and `isApproved` a `Boolean`. The two list components of `TweetDto` are the exception: a `null` list becomes an empty unmodifiable list and a `null` element is dropped. | (a) Primitive `int` and `double` components with `Objects.requireNonNull` on every other component. (b) Substitute a default for a null column — `0`, `0.0`, the empty string, the epoch. (c) Add `nullable = false` to the columns so the database guarantees what the record assumes. (d) Serialise a null list as JSON `null` rather than `[]`. | `backend/app/db/models.py:L10-18` declares every mapped column as a bare `Column(...)`, so every column is nullable, and `backend/app/schema/tweet.py:L5-14` declares types and optionality only (IR9). A wire record that cannot represent a null column cannot represent a legitimately sparse row: under (a), which was delivered first, a row whose `like_count` was never populated makes the mapper raise inside the read path, so a stored row becomes an HTTP 500 and can never be read again. (b) reports a measurement that was never taken — the same fault as substituting zero for a null aggregate (DL-075) or fabricating a mirror value (DL-090). (c) is forbidden: AAP G2 and 0.8.1 prohibit adding a `NOT NULL` constraint. (d) contradicts DL-024, which fixes both delimited columns as JSON arrays on the wire. No `spring.jackson.default-property-inclusion` is set, so every key is still present in the body and an absent value renders as JSON `null` — measured on `dto/ResponseDto`, which serialises as `{"id":"7","content":null,"generated_at":null,"is_approved":null,"tweet_id":null}` when only its identifier is populated. | A client must tolerate `null` for every scalar of a tweet. The frontend Zod contracts type `likeCount` and `doubtRating` as non-nullable numbers, so a sparse row fails validation there — a pre-existing divergence DL-022 leaves unfixed. Sparse rows are now visible rather than masked, so data-quality gaps surface at the client instead of as a 500 in the mapper. |
| DL-081 | `service/LlmService.generateResponse(TweetDto)` returns the generated text as a `String`. A `ResponseDto` is built only from a stored `responses` row, by `service/mapper/ResponseMapper`, after the insert has assigned an identifier. | (a) Return a `ResponseDto` assembled before persistence, with a `null` `id`, `is_approved` `false` and `generated_at` minted in the adapter. (b) Let `LlmService` persist the row itself and return the mapped record. (c) Introduce an intermediate record carrying the text plus generation metadata. | `dto/ResponseDto` requires a non-null `id` (DL-080) and an identifier exists only once the database has assigned one, so (a), which was delivered first, can be satisfied only by weakening the wire contract — which is the finding that produced DL-080. An integration adapter's contract is what the vendor produced; the component that owns the transaction owns the row and therefore the record built from it. (b) would put a repository inside an adapter whose responsibility boundary TR-13 and 0.3.3 limit to the external system, and would give `LlmService` a transaction. (c) adds a class the AAP 0.3.1 inventory does not contain, for a value one `String` already carries. | The returned text carries no metadata, so the caller must supply `is_approved` and `generated_at`; `service/ResponseService.store` does, at one site. A caller wanting the raw vendor payload rather than the trimmed text has no accessor for it. |
| DL-082 | `dto/UpdateResponseRequest` holds its two components as `com.fasterxml.jackson.databind.JsonNode` and exposes `contentPresent()`, `contentValue()`, `approvalPresent()`, `approvalValue()` and `carriesNoUpdatableField()`. Presence of a key in the request body, not its value, decides whether a column is written, so `{"is_approved":null}` clears the column and an absent key leaves it untouched. | (a) `String content` and `Boolean isApproved` components, where an absent key and an explicit JSON `null` are the same Java value. (b) `Optional<String>` and `Optional<Boolean>` components with `jackson-datatype-jdk8`. (c) A `Map<String, Object>` body. (d) Move the route to `PATCH` with JSON Merge Patch semantics. | `backend/app/api/responses.py:L54` reads `request.json` as a dict and `:L58-59` writes a column when its key is in that dict, so presence is the source's own criterion — and (a), which was delivered first, loses it: `{"content":null}` silently did nothing. Measured on Jackson 2.21.4 through the framework's own `MappingJackson2HttpMessageConverter`, an absent property binds to Java `null` and an explicit JSON `null` binds to `NullNode`, so `{}` carries no updatable field while `{"content":null}` and `{"is_approved":null}` each carry one — the distinction survives with no new dependency and no new class. (b) needs a module registration and puts `Optional` in a record component, which the JDK's own guidance discourages for fields. (c) discards typing and pushes type checks into the service. (d) changes the HTTP method the source declares, which G1 freezes. | The record's component types name a Jackson type, so the DTO is coupled to the JSON library rather than being a plain value object. A body whose `content` is not a string, or whose `is_approved` is not a boolean, is rejected by the compact constructor rather than coerced; Measured through the same converter, Jackson reports that rejection as a `ValueInstantiationException` and the framework surfaces it as an `HttpMessageNotReadableException`, which is answered with 400 (DL-092) and carries no field-level detail. |
| DL-083 | A completion carrying no choice, a choice carrying no content, and content that is blank after trimming are each reported by `service/LlmService` as `exception/ResponseGenerationException`, raised before anything is persisted. The exception carries the wire literal of `backend/app/api/responses.py:L49`. | (a) Return the empty string and let the caller store it. (b) Retry the call. (c) Return `null` and leave the decision to each caller. | The source's 500 branch at `:L48-49` exists for exactly the case where generation produced nothing, and an empty completion is that case rather than a successful one. Under (a), which was delivered first, a blank `responses` row reaches the review queue where `is_approved` is a flag a human sets (0.8.1), so a reviewer would be approving an empty reply. (b) adds a retry policy the source has nowhere, spends a second paid call inside the request on a failure that is usually deterministic, and 0.2.3 excludes new resilience capability. (c) reintroduces a null return every caller must test, and the first caller that forgets stores the blank row anyway. | A completion that is legitimately empty is reported as a failure, so a model configured to answer with nothing makes the route answer 500. The route answers 500 rather than 201 with an empty body — the source's own taxonomy, and intended. |
| DL-084 | Each failure path has exactly one component that writes its error record, and that record carries the operation, the exception class name and, for a failed HTTP call, the status value only. `service/ResponseService` owns the generation path and `service/NotionService.logFailure` owns all three mirror paths; `service/LlmService` writes no error record for a failure it propagates. | (a) Log the exception with its message and stack trace at both the adapter and its caller. (b) Log at the adapter only. (c) Log nothing and let the handler's 500 record be the only trace. (d) Keep the message but redact it by pattern. | A vendor exception message is not ours to store: the OpenAI request carries the tweet text as prompt content, the Notion request carries a database identifier and a page URL, and an HTTP client's message quotes the request and often the response body — so (a), which was delivered first, writes third-party payloads and our own request content into a plain-text log, beyond the DL-052 baseline, which excludes an audit facility precisely because logs are not a record store. Duplication was also real: one failure produced two `ERROR` records from two layers. The caller knows the business operation and the adapter knows the transport, so the reporting layer names both. (b) leaves no record of which business operation failed. (c) removes the signal entirely. (d) is brittle — a redaction pattern must anticipate every vendor's message format. | Less diagnostic detail. The exception class and, for an HTTP failure, the status still separate authentication, rate limiting, timeout and malformed-response classes, but a support case now needs a reproduction rather than a log line. `api/GlobalExceptionHandler`'s catch-all still writes the throwable for a genuine server failure, which is the one place a stack trace remains available. |
| DL-085 | `service/LlmService` closes the cached `OpenAIClient` in an `@PreDestroy` method, clears the field under the same monitor that creates it, and refuses to create a replacement afterwards — a call after destruction raises `IllegalStateException`. A failure to close is logged at `WARN` by exception class and is not propagated. | (a) Leave the client to garbage collection. (b) Declare the client as a `@Bean` and let the container's destroy callback close it. (c) Create a client per call and close it in a try-with-resources. (d) Close it without a destroyed flag. | `com.openai:openai-java` 4.49.0 holds an OkHttp connection pool and a dispatcher executor, and those threads keep the JVM alive until they are shut down, so under (a), which was delivered first, a stopped context leaks a pool and delays shutdown. (b) is the idiomatic form, but the client is created lazily on first use precisely so a missing API key does not fail startup — the key is optional in every profile — and a `@Bean` is created eagerly. (c) discards connection reuse on every request. (d) leaves a race: a request already inside the service can create a replacement after `@PreDestroy` has closed the first, and the replacement is then never closed. | A request in flight during shutdown fails with `IllegalStateException` rather than completing; the container is stopping, so the alternative is a request completing against a closed client. The destroyed flag makes the bean single-use, so a container that destroyed and then reused the same instance would find it unusable — Spring does not. |
| DL-086 | No transaction is open while the OpenAI call runs. `service/ResponseService.generateResponse` reads and maps the subject in one repository-demarcated unit, calls `LlmService` with no transaction open, then persists inside an explicit `TransactionTemplate` unit that re-reads the parent row before inserting. The template is injected rather than obtained by annotating a private method. | (a) One `@Transactional` method spanning the whole flow. (b) `@Transactional(propagation = NOT_SUPPORTED)` around the external call. (c) Annotate the private persistence helper `@Transactional`. (d) Move generation to an asynchronous worker so the request never holds a connection. | A chat completion takes seconds, and under (a), which was delivered first, a pooled JDBC connection is held for all of it — the HikariCP default pool is ten, so ten concurrent generations exhaust it and every other route waits on a database connection for a reason that has nothing to do with the database. (b) still needs an outer transaction to suspend and leaves the connection bound to the thread. (c) is inert: Spring's transaction advice is a proxy, so a self-invocation never passes through it and the annotation silently does nothing — which is why the template is injected. (d) needs a broker or a queue, which 0.2.3 excludes. | The flow is no longer atomic: the parent row can be deleted between the read and the insert, which is why the persistence unit re-reads it and reports the generation-failure literal instead of inserting an orphan. When that happens the generated text is lost and the paid call is not refunded. Reading and writing in separate units also means the mapped subject is a detached entity — only basic and converted attributes are read from it, never the `responses` collection. |
| DL-087 | `repository/ResponseRepository` overrides `findAll(Pageable)` with `@EntityGraph(attributePaths = "tweet")`, so the page query fetches each row's parent tweet in the same statement. The method's name, signature and semantics are unchanged. | (a) Leave the inherited method and accept one secondary select per distinct parent. (b) Make the `@ManyToOne` lazy. (c) Add a differently named `join fetch` JPQL query. (d) Set `hibernate.default_batch_fetch_size` globally. | `backend/app/db/models.py:L28,L30` declares the association with SQLAlchemy's default loading and TR-4 maps it to a plain `@ManyToOne` and `@OneToMany` pair, so the mapping itself must not change — which rules out (b), and (b) would additionally break the detached mapping `service/mapper/ResponseMapper` performs outside a transaction. An entity graph is a fetch instruction on an existing method, so no caller changes and no new public method appears, which (c) would add along with a hand-written count query for pagination. Measured with Hibernate statistics over three distinct parents: the page costs two statements with the graph and grows with the row count without it. (d) is a global tuning property affecting every association in the application. | The page query is now a join, so a page of N rows materialises N parent rows even for a caller that ignores the association — the association is eager either way, so those rows were always read, one statement at a time. A future caller wanting responses without their parents has no un-joined variant. |
| DL-088 | `service/NotionService` writes one Notion property per component of the post it mirrors — nine: `Content`, `Author`, `Timestamp`, `Doubt Rating`, `Engagement`, `Tweet Id`, `Media`, `Quoted Tweet Id` and `AI Tools Mentioned`. Each of the two list components is written as a single comma-delimited rich-text value. A component the post does not carry has its property omitted rather than written as null. | (a) Mirror six properties and drop `media`, `quotedTweetId` and `aiToolsMentioned`. (b) Write each list as a Notion multi-select property. (c) Write one rich-text property per list element. (d) Write an explicitly null property for an absent component. | The mirror is a secondary copy of the relational row (0.6.5.12), and a copy that silently omits three of nine columns is not a copy: a reader of the Notion database cannot tell an absent value from an unmirrored one, which is what (a), delivered first, produced. The delimited form is the form the columns themselves hold (DL-024), so the mirror matches the system of record exactly and `readList` reverses it with the same split `util/DelimitedStringListConverter` uses. (b) requires the target database to declare a multi-select schema with pre-registered options, which this adapter neither manages nor can create. (c) invents property names that depend on the element count. (d) writes a shape the Notion API rejects for several property types and would make an absent value indistinguishable from an empty one. | The nine property names are now part of an implicit contract with a Notion database this code does not create; a database missing any of them rejects the write. The delimited value cannot represent an element containing a comma — the same limitation the column has (DL-024). |
| DL-089 | A 2xx Notion response whose body is empty, or which lacks the structure the call requires, is reported as a failure. `requireResults` raises for an empty body and for an absent or non-array `results`; page creation raises for an empty body and for a created page carrying no identifier. An empty `results` array is not a failure — it is a genuine absence of a match. | (a) Normalise every unexpected shape to a benign value — the empty string, no match, an empty list. (b) Retry the call. (c) Return an `Optional` or a result object from each call so every call site distinguishes the cases itself. | An answer of 200 whose body is not what the API documents is an integration fault, and under (a), which was delivered first, it is indistinguishable from a correct negative answer: `findPageIdByTweetId` reported no match for a broken response, so `updateTweetResponse` then wrote nothing and reported success — a mirror that silently stopped mirroring, which is worse than one that fails loudly. Separating the two is precisely what makes it safe to keep reporting the empty-array case as no match. (b) retries a well-formed exchange that will return the same body. (c) spreads the decision across every call site of an adapter whose purpose is to hide the transport. | A Notion API change that wraps `results` in a new envelope turns a working mirror into a failing one at the first call rather than degrading quietly — intended, and contained: the mirror is secondary (0.6.5.12), so the relational write has already succeeded before the mirror is attempted. |
| DL-090 | Reading a Notion page never substitutes a value. `toTweetDto` returns `null`, and the page is skipped, when the page carries no `Tweet Id` text, so the Notion page UUID is never read into `TweetDto.id`. An absent text property reads as `null`, an absent or unparseable timestamp reads as `null`, and an absent number reaches the boxed component as `null`. Skipped pages are counted and reported once at `WARN`. | (a) Fall back to the Notion page identifier for `id`, and substitute the empty string, the epoch and zero for absent values. (b) Raise on the first unrecognised page. (c) Return a partially populated record carrying the page UUID in a separate component. | A Notion page UUID and the `tweets.id` surrogate key are different identifier spaces (DL-023, IR11), so (a), which was delivered first, handed callers an identifier that resolves to no row and looks entirely valid; its substituted epoch and zero are also indistinguishable from measured values, the same fault as DL-075 and DL-080. A shared Notion database is edited by people, so an unrecognised page is expected rather than exceptional — which is why (b) would let one hand-added row break every read. (c) adds a component to a record whose shape G1 and DL-080 fix. | A page added by hand is invisible to the read rather than reported individually; the count at `WARN` is the only signal and it names no page, deliberately, because a page identifier is a resource identifier DL-084 keeps out of the log. `readTweets` can therefore return fewer records than the Notion query matched. |
| DL-091 | `service/AnalyticsService.getSummary` is `@Transactional(readOnly = true, isolation = Isolation.REPEATABLE_READ)`, so its six aggregate queries read one snapshot. | (a) Leave each query in its own transaction. (b) Compute the six values in one query. (c) Derive the counts in Java from a single `findAll()`. (d) `SERIALIZABLE`. | The six values are reported as one object and two of them are related by construction — `approved_responses` cannot exceed `total_responses` and `pending_responses` is their difference — so under (a), which was delivered first, a row committed between two queries produces an arithmetically impossible body, including a negative `pending_responses`. (b) would need a cross-table query over `tweets`, `responses` and `ai_tools`, two of which have no relationship, so it becomes hand-written SQL and loses the derived-query form the other five use. (c) reads every row of three tables to compute seven numbers. (d) is stronger than a read needs and takes range locks on some vendors. Verified at runtime that the level is accepted for this transaction on H2 2.3.232 and PostgreSQL 16.14; MySQL InnoDB uses it by default. | The snapshot is marginally stale by the time the body is written, which is inherent to any consistent read. A read-only repeatable-read transaction cannot raise a serialization failure on the supported vendors, so the level costs nothing beyond holding one snapshot for the duration of six aggregates. |
| DL-092 | `api/GlobalExceptionHandler` declares an explicit handler for each framework-reported client failure, so the catch-all answers server failures only: 400 `Bad request` for an unreadable body, a missing parameter, header or multipart part and an unconvertible value; 405 `Method not allowed` with an `Allow` header; 415 `Unsupported media type`; 406 `Not acceptable`. | (a) Let the catch-all answer them all as 500. (b) Remove the catch-all so the container's default error page renders them. (c) Enable `spring.mvc.problemdetails` and let the framework emit RFC 9457 bodies. (d) Reuse the single `Bad request` literal for all four cases. | Flask answers a request whose method a route does not declare with 405, and a malformed body with 400, without any application code — so (a), which was delivered first, is not parity: it reports the client's mistake as a server fault and tells the client to retry something that can never succeed. (b) emits the container's HTML error page, breaking the single-key JSON envelope IR1 fixes. (c) replaces every body with a ProblemDetail, contradicting the two envelopes the source defines. (d) would make a 405 and a 415 indistinguishable in the body. The four literals are net-new because the source has no branch for these cases at all; each follows the sentence case of `backend/app/main.py:L33,L37` and each is distinct from the eight per-route literals. | Four wire strings exist that no Python line defines. No correct client depended on the previous text, since these cases previously answered 500 with a different message. A 406 necessarily carries an empty body — a client that refuses `application/json` cannot be served the JSON envelope — so for that one status only the status code conveys the failure. |
| DL-093 | A failed token verification is logged at `WARN` as a category only: the simple class name of the exception jjwt raised, with no message and no throwable. `security/JwtService` writes exactly one such record per rejected token. | (a) Log the parser's message. (b) Log the exception with its stack trace. (c) Log nothing and let the 401 be the only trace. (d) Log a prefix of the presented token. | Every byte a `JwtException` message quotes comes from the presented token, which is supplied by whoever made the request, so (a), which was delivered first, writes caller-chosen text into a plain-text log — a log-injection and log-forging vector, and jjwt's malformed-token messages quote a fragment of the input directly. (b) attaches a stack trace to a routine, expected event. (c) removes the only signal that tokens are being rejected at all, which is the operational question. (d) is worse than (a), since a token prefix can carry a readable header and subject. The exception class already separates the cases that matter: bad signature, expired, malformed, unsupported. | An operator cannot tell from the log which claim or which byte was wrong, only which class of failure occurred, so diagnosing one client requires reproducing its request. This is the same containment DL-084 applies to vendor exceptions. |
| DL-094 | The record written when a request authenticates carries the request method and URI only. `security/JwtAuthenticationFilter` sets the principal on the security context but never writes its name. | (a) Log the token subject. (b) Log a non-reversible identifier such as a salted hash of it. (c) Log nothing on success. (d) Log the subject at `TRACE`. | A principal name in a log is an access record, and IR8 draws the scope line at a logging baseline with no audit facility (DL-052) — an access record is the first half of one, and it accumulates on stdout in plain text with no retention policy to govern it. With a single configured principal (DL-020) the name is also constant, so (a), which was delivered first, distinguished nothing while creating exactly that record. (b) still stores a per-principal identifier and adds a salt to manage, for no diagnostic gain. (c) removes the signal that the filter ran and authenticated. (d) leaves the value in the code, one configuration change away from being emitted. | A successful request cannot be attributed to a principal from the logs alone. Acceptable while the credential store holds one principal; if DL-020's extension path to a persistent user store is taken, this is the decision to revisit. |
| DL-095 | An in-scope test that asserted defective behaviour is rewritten to assert the corrected contract rather than preserved. Four were: the `length() == 255` assertion in `JpaMappingIntegrationTest`, the six-named-properties and six-field round-trip assertions in `NotionServiceTest`, the pre-persistence `ResponseDto` and empty-text expectations in `LlmServiceTest`, and the exactly-six-handlers, seven-handled-types and only-400-404-500 counts in `GlobalExceptionHandlerTest`. | (a) Keep the assertions and shape each fix so they still pass. (b) Delete the assertions without replacement. (c) Mark them `@Disabled` with a comment. | AAP D1 states that a test encoding an outdated or incorrect value is updated to match the AAP, never the reverse, and each of these four assertions pinned exactly the behaviour a finding required changed — the handler-count assertion in particular made adding the 405, 415 and 406 handlers impossible, so (a) would have preserved a defect to keep a test green. (b) would leave the corrected behaviour unguarded, so each assertion was replaced by one over the new contract: physical column type and capacity read from JDBC metadata, one property per mirrored component plus whole-record equality, a failure for absent or blank model output, and a containment assertion over the handled types with the emitted status set widened to six. (c) leaves dead code and no guard. | The count-based assertions did catch an accidentally added handler or property, and the containment assertions that replace them tolerate an unintended handler as long as its status is one of the six. Test-only, and the exchange is what the findings require. |
| DL-096 | No test class outside the sixteen names in AAP 0.2.1.4 is created. Contracts whose owning test class is not yet delivered — the presence semantics of `dto/UpdateResponseRequest`, the log content of `security/JwtAuthenticationFilter`, the analytics aggregates — are covered by the tests that do exist and by recorded runtime evidence. | (a) Create the missing test class now. (b) Fold the assertions into an unrelated existing test class. (c) Leave the contracts unasserted and unrecorded. | AAP 0.2.1.4 enumerates the test inventory exactly, and two of the review's own findings — an unauthorised workflow edit and an artifact changed outside its checkpoint — share the root cause of a change outside the authorised file set, so (a) would repeat that error while fixing others. (b) would place an assertion about one component inside another component's test class, where nobody looking for it would find it and from where it must be moved again when the real class arrives. (c) discards the evidence that the contracts hold. | Three contracts have no dedicated regression guard until their test classes are delivered. Each is guarded indirectly: the presence accessors through `service/ResponseService`'s own update paths, the filter's log content by a source-level check that no statement in the security package names a principal, and the aggregates by runtime verification on two vendors. The gap is recorded here so that it reads as a state rather than an omission. |
| DL-097 | `service/SettingsService.seedDefaultSettings` is not itself transactional. Each key is seeded inside its own `TransactionTemplate` unit configured `PROPAGATION_REQUIRES_NEW` and `ISOLATION_REPEATABLE_READ`, and the insert is flushed inside that unit. | (a) One `@Transactional` method enclosing all three keys. (b) A native `INSERT` with a vendor conflict clause. (c) Make `Setting` implement `Persistable` so Spring Data issues `persist` rather than `merge`. (d) Keep one transaction and catch the conflict inside it. | `SimpleJpaRepository.save` resolves to `EntityManager.merge` for an entity with an assigned `String` identifier and no `@Version`, and `merge` issues its own select — under read-committed that select can see a row another replica committed after the presence test and turn the insert into an update that overwrites an operator's value. That matters here specifically because these rows exist to be edited through `PUT /settings/{key}` (DL-040). Repeatable read puts the merge select on the presence test's snapshot, so `merge` always schedules an insert and every conflict is a duplicate key rather than a silent overwrite. Per-key units also mean a conflict on one key cannot mark the transaction rollback-only and take the other two with it, which (a), delivered first, and (d) both allow. (b) is not vendor-neutral and would hand-write SQL beside reserved column names each vendor quotes differently. (c) changes an entity the review passed and affects every other write to it. | Three transactions at startup instead of one. On PostgreSQL a repeatable-read write transaction can raise a serialization failure; it is caught as a concurrency conflict and reconciled by DL-098. Measured honestly: with read-committed and twelve concurrent replicas, PostgreSQL produced no overwriting update either, because the primary key blocks the second inserter first — so this level buys a property that holds by construction rather than by timing, not a fix for an observed overwrite. |
| DL-098 | A duplicate key or a lock conflict raised while seeding a key is reconciled by re-reading that key: present means another instance inserted it first and the stored row is left exactly as it stands; still absent means the write failed for another reason and the exception propagates. | (a) Let every write failure propagate. (b) Swallow every write failure. (c) Retry with backoff. (d) Insert, then compare the stored row against the default and repair it. | Several replicas start at once and all three keys are seeded on `ApplicationReadyEvent`, so a duplicate key is the expected outcome for all but one replica — measured on live PostgreSQL, (a) makes eleven of twelve replicas log an error for a database that is in exactly the intended state. (b) would hide a genuine outage behind the same path, which is why the re-read rather than the exception type alone is the discriminator. (c) retries a row that is already there. (d) is the overwrite this design exists to prevent: a value an operator changed must survive every subsequent start. | One extra presence read, on the conflict path only. A row another instance inserted with a different value — a different software version's default, say — is accepted and never corrected, which is the same guarantee an operator's own edit relies on. |
| DL-099 | This log is maintained as part of any change that alters a decision, and a superseded decision is rewritten in place under its own identifier rather than appended as a second entry. Seven entries were rewritten this way after the code review — DL-015, DL-017, DL-025, DL-068, DL-070, DL-074 and DL-075 — and DL-059, which an existing entry cited but which had never been populated, was written so that the completeness invariant holds. | (a) Freeze the file and record remediation rationale elsewhere. (b) Append a new entry for each changed decision and leave the superseded one standing. (c) Keep both forms in one row, marked superseded and current. | Rule 1 makes this file the only place rationale may live and forbids it in code, so a decision that changes must be recorded here or nowhere — (a) makes the rule unsatisfiable for any later change and would strand the identifiers the code cites. Every citation in the code names an identifier and not a version, so under (b) a reader following `DL-070` from `entity/AiTool` arrives at superseded text and concludes the code is wrong; this file's own completeness invariant states that a citation is never satisfied by an entry living somewhere else. (c) doubles every rewritten row and makes the current decision harder to find than the abandoned one. The same reasoning does not extend to the rest of the documentation set: `docs/TRACEABILITY_MATRIX.md` is left uncreated, because no requirement in force compels writing it here and creating it would be a change with no authorising need — the boundary is that a file is written when a requirement's resolution cannot be satisfied without it, and not otherwise, which is the same test DL-096 applies to test classes. | A decision's earlier form survives only in version history, so this log states what is delivered rather than how it evolved. An identifier's meaning can therefore change over time; each rewritten row is written to stand alone, so a citation always resolves to the behaviour the code has now. |
| DL-100 | Override the Spring Boot BOM's Netty line with `netty.version` 4.1.136.Final. | (a) Keep the BOM-resolved 4.1.135.Final. (b) Upgrade the Spring Boot parent solely to move Netty. (c) Exclude Reactor Netty. | 4.1.136.Final is the first reviewed patch containing the fixes named by the dependency finding, and the resolved Reactor Netty graph remains compatible under the existing Boot parent. A narrow property override changes the affected runtime without moving every dependency managed by the parent. | The override can diverge from the version combination exercised by the Boot BOM and must be rechecked whenever Boot or Reactor Netty moves. |
| DL-101 | Override the managed Jackson BOM with 2.21.5. | (a) Keep 2.21.4. (b) Override only `jackson-databind`. (c) Upgrade the Boot parent solely for Jackson. | The reviewed authorization and mass-assignment fixes land in 2.21.5. Overriding the Jackson BOM keeps `databind`, core, annotations and datatype modules aligned for Spring MVC, `openai-java` and `jjwt-jackson`. | A Jackson-wide override moves more modules than the directly reported artifact and must be revalidated against every serializer consumer after future Boot upgrades. |
| DL-102 | The CD `workflow_run.workflows` value is the exact CI workflow name `Code Skeptic Scanner CI`. | (a) Rename the CI workflow to `Continuous Integration`. (b) Keep the mismatch. (c) trigger CD directly on pushes. | `workflow_run` compares the displayed workflow name exactly; the mismatch made the deploy job unreachable. Updating the sole consumer preserves the established CI name and restores the intended build-then-deploy chain. | A later rename of either workflow must update the other file in the same change or deployment becomes unreachable again. |
| DL-103 | Third-party workflow code is limited to current official actions pinned by full commit SHA; Google authentication uses `google-github-actions/auth` plus `setup-gcloud`, and Slack notification uses a bounded direct webhook call instead of an archived action. | (a) Mutable major tags. (b) The obsolete `setup-gcloud@v0.2.1`. (c) The archived Slack action. | Immutable revisions make reviewed workflow code reproducible, the Google auth action separates credential acquisition from SDK setup, and a direct `curl` call avoids handing the webhook to an unmaintained repository. | Action and API updates are manual, and the direct webhook payload and timeout are now maintained in this repository. |
| DL-104 | CI has `contents: read`; CD has `contents: read` plus `id-token: write` for Workload Identity Federation; every checkout sets `persist-credentials: false`. | (a) Repository-default permissions. (b) Persist the checkout token. (c) Authenticate Google Cloud with a long-lived JSON key. | Explicit permissions bound the token capabilities visible to jobs, checkout does not leave a reusable credential for later steps, and OIDC exchanges a short-lived identity for cloud access without storing a service-account key. | CD now depends on correctly configured workload-identity-provider and service-account secrets and on their external trust policy. |
| DL-105 | CD checks out `workflow_run.head_sha`, tags the image with that SHA, resolves the pushed immutable digest, deploys the digest, and serializes production deployments without cancelling one already running. | (a) Checkout the default branch. (b) Deploy `latest` or another mutable tag. (c) Allow concurrent deployments. | The deployed bytes must be the commit the triggering CI run tested. Digest deployment removes tag races, and serialization prevents two successful runs from interleaving production mutations. | A queued older run can deploy before a newer one, and a failed digest lookup stops deployment after the image push. |
| DL-106 | Both Docker build stages use reviewed patch tags pinned to registry SHA-256 digests. | (a) Floating Java 21 tags. (b) Patch tags without digests. (c) Vendor a JDK into the repository. | Tags can be repointed; a digest fixes the exact toolchain and operating-system layer used for an attested build while retaining a readable patch tag. | Base-image security updates require an explicit digest refresh, and a removed registry manifest prevents rebuilding until the pin is updated. |
| DL-107 | The runtime image creates UID/GID 10001, copies the jar as root with group-read access, and executes Java as `10001:10001`. | (a) Run as the image's default root user. (b) Use an arbitrary named user supplied by the base image. (c) make the application directory world-writable. | The JVM needs to read the jar and write only to its normal temporary locations; it does not need root or write access to the application artifact. A fixed numeric identity is stable under Kubernetes security policies. | Future features that write beneath `/app` will fail until they receive an explicitly owned data path. |
| DL-108 | JWT configuration, signing and verification accept HS256 only; the JJWT parser's signature registry contains only `Jwts.SIG.HS256`. | (a) Accept HS384 and HS512 with the same key. (b) Keep the parser's default algorithm registry. (c) add asymmetric algorithms. | Minting already produced HS256. Restricting configuration and parser policy to that one algorithm removes algorithm-policy drift and makes a token signed under another same-key HMAC algorithm invalid. | Moving to another algorithm becomes a coordinated configuration, key-management and parser change rather than a property-only change. |
| DL-109 | Accepted JWTs carry exactly `sub`, `iat` and `exp`, with a nonblank subject and `exp` strictly after `iat`; parsing occurs once per validation attempt. | (a) Accept missing or extra claims. (b) Parse once per accessor. (c) normalize a blank subject. | These are the complete claims minted by the service and the only principal and lifetime data its filter consumes. Exact validation prevents indefinite or ambiguous signed tokens and avoids inconsistent results from repeated parsing. | Adding a registered claim such as `iss`, `aud` or `jti` requires updating both minting and the exact accepted-name set. |
| DL-110 | `scanner.jwt.expiration-minutes` must be between 1 and 60 inclusive, and seconds conversion and expiry arithmetic use checked operations. | (a) Permit zero or negative values. (b) Permit values above the documented one-hour maximum. (c) rely on overflowing arithmetic. | The technical specification fixes a one-hour maximum and a positive token lifetime. Startup validation makes invalid policy configuration fail before any token is issued. | Operators cannot use the property to create longer-lived development tokens and must change the policy deliberately to do so. |
| DL-111 | Token rejection and authentication-filter logs use fixed categories at DEBUG and never include token text, claims, JWT parser messages or the principal. | (a) Log parser exceptions at WARN. (b) Log the subject on every accepted request. (c) suppress all diagnostics. | Bearer material and subjects are attacker-influenced or identifying data; fixed categories retain operational signal without log injection, disclosure or amplification. | Debug records cannot identify one rejected token across multiple components without an external request-correlation facility. |
| DL-112 | The JWT filter and security chain share one `RequestAttributeSecurityContextRepository`; the filter saves successful authentication and runs on REQUEST, ASYNC and ERROR dispatches. | (a) Set only the thread-local context. (b) skip redispatches. (c) use an HTTP session repository. | Spring Security authorizes each registered dispatch. Persisting the context on the request keeps authentication available to later dispatches while retaining stateless session policy. | Each dispatch can repeat token validation, and request-attribute persistence does not survive a second HTTP request. |
| DL-113 | The HTTP authentication scheme name `Bearer` is matched case-insensitively and must be followed by one space and nonempty token text. | (a) Match exact case. (b) accept arbitrary whitespace. (c) parse any authorization scheme as a bearer token. | HTTP authentication scheme names are case-insensitive, while a strict separator keeps token extraction deterministic. | Noncanonical headers with tabs or multiple leading spaces are rejected even if another implementation might accept them. |
| DL-114 | Spring Security logout is disabled. | (a) Keep the framework's permit-all `/logout`. (b) implement token revocation. (c) leave logout enabled for POST only. | The service uses stateless custom JWTs and has no revocation store, so the framework logout endpoint cannot invalidate a token and would create a second public route outside the declared surface. | Clients have no server-side logout and must discard their token locally or wait for expiry. |
| DL-115 | Every protected unauthenticated response is an empty 401 carrying `WWW-Authenticate: Bearer`. | (a) A bare 401 without a challenge. (b) a JSON error envelope. (c) a container-generated error page. | The challenge advertises the authentication scheme, and an explicit empty response preserves the closed set of source error strings. | Clients receive no detail distinguishing an absent token from an invalid or expired one. |
| DL-116 | `scanner.auth.password-hash` must be a 60-character `$2a$`, `$2b$` or `$2y$` bcrypt modular-crypt value with cost 10 through 14. | (a) Accept any nonblank text. (b) accept every legal bcrypt cost. (c) validate on the first login. | Shape validation catches misconfiguration at context creation, and the cost range excludes both weak and operationally excessive values without reproducing the hash in diagnostics. | A hash generated outside the accepted cost window cannot be used until policy changes, even though a bcrypt implementation could verify it. |
| DL-117 | `AuthController` maps only credential-rejection exceptions to empty 401; authentication-service and other infrastructure exceptions propagate to the generic 500 envelope. | (a) Catch every `AuthenticationException` as 401. (b) expose provider messages. (c) add a new 503 wire contract. | A broken provider is a server fault, not evidence that the submitted credential is wrong. Keeping the existing generic 500 avoids inventing a new source-incompatible response body. | Clients cannot distinguish a transient authentication backend outage from another internal failure. |
| DL-118 | `POST /auth/token` accepts at most 4096 encoded request bytes and at most 256 characters in each credential field, returning the same empty 401 before authentication when either bound is exceeded. | (a) No bounds. (b) Bean Validation with a 400 envelope. (c) reject only after bcrypt. | The public route otherwise allows unbounded allocation and expensive credential work. A pre-deserialization byte limit and a post-binding character limit contain both costs while preserving the route's generic rejection behavior and validation-parity rule. | The limits are service policy not present in the retired schemas; exceptionally long legitimate credentials cannot authenticate. |
| DL-119 | `util/LogSafe` renders identifiers as `sha256:` plus the first eight digest bytes and renders failures by simple type name; service and error logs emit fixed metadata once without vendor messages or stacks. | (a) Duplicate a private sanitizer in each class. (b) log raw identifiers. (c) use random per-event tokens. (d) log exception stacks at DEBUG. | One utility keeps the five remediated logging surfaces consistent. Stable non-reversible tokens allow same-value correlation without reproducing external identifiers, and type/status metadata is sufficient for the handled error category. | A 64-bit truncated digest can collide, hashing adds small per-log CPU cost, and stack-free handled failures provide less diagnostic detail. |
| DL-120 | Model generation returns an internal `GeneratedResponse` with nonblank content; the public `ResponseDto` requires a persisted identifier and is built only after storage. | (a) Reuse `ResponseDto` with `id = null`. (b) fabricate an identifier. (c) store blank or absent model output as a successful draft. | A database-generated ID does not exist during generation, while the external wire contract requires one. A separate internal type represents the pre-persistence state and makes unusable model output a generation failure instead of a 201 draft. | The internal and wire records must evolve together if response fields change, and blank output now produces a 500 rather than a stored row. |
| DL-121 | Response generation performs the tweet read, OpenAI call and response insert without one surrounding service transaction; repository calls own short transactions and no connection is held during the network call. | (a) Keep one transaction across the whole method. (b) make the OpenAI call asynchronous inside the transaction. (c) manually manage an `EntityManager`. | A slow external request must not reserve a pool connection. The loaded tweet is converted to a DTO before the call and its identifier can be associated with the new response during the later repository transaction. | The tweet can be deleted between read and insert, surfacing as a storage failure, and the detached association depends on the row still existing. |
| DL-122 | Partial response updates acquire a pessimistic write lock on the row before applying content or approval changes. | (a) Save an unlocked stale entity. (b) add a version column. (c) issue field-specific update statements. | Locking preserves both independently writable fields without adding a schema column or duplicating update queries, and it fits the existing transactional service method. | Concurrent updates serialize and can wait, deadlock or fail under database lock timeouts. |
| DL-123 | Tweet and response list operations cap `per_page` at 100 while retaining the 1-based page contract and existing defaults. | (a) Accept `Integer.MAX_VALUE`. (b) reject high values with a new 400. (c) use different caps per route. | A shared upper bound prevents authenticated callers from requesting unbounded database and heap work without adding a new response status or changing parameter names. | Clients requesting more than 100 rows receive a silently capped page and must paginate. |
| DL-124 | Default settings are inserted independently with assigned-key `Persistable` entities and `saveAndFlush`; a duplicate-key race is treated as an already-seeded row. | (a) `existsById` followed by `save`. (b) overwrite with `merge`. (c) one transaction around all defaults. (d) vendor-specific upsert SQL. | `persist` makes an existing assigned key fail instead of overwrite, immediate flush surfaces the race inside the repository transaction, and handling it per key lets concurrent instances both finish startup without vendor-specific SQL. | `DataIntegrityViolationException` is classified as a duplicate based on the fixed valid seed data and sole relevant primary-key constraint; a future additional constraint would require narrower classification. |
| DL-125 | Analytics reads tweet count and both tweet averages in one projection and response total and approved count in one projection; pending is clamped to the nonnegative complement. | (a) Six independent aggregate queries. (b) repeatable-read across every table. (c) one vendor-specific cross-table statement. | Metrics with an internal invariant now come from the same database statement, so approved cannot exceed the response total and tweet means share the tweet count's snapshot. The tracked-tool count is independent. | Tweet, response and tool projections can still observe different commits because they are separate statements, but no returned arithmetic invariant spans those tables. |
| DL-126 | A Notion mirror row without a `Tweet Id` property is skipped and counted in a warning; the Notion page UUID is never substituted as a tweet identifier. | (a) Use the page UUID. (b) emit a DTO with a null ID. (c) fail the entire page read. | `Tweet Id` is the relational correlation contract, while a Notion page UUID identifies a different object. Skipping isolates malformed mirror rows without corrupting IDs or discarding valid siblings. | Malformed rows are absent from the returned list and require log monitoring to detect. |
| DL-127 | Offset-bearing Notion timestamps are normalized to UTC before conversion to `LocalDateTime`; local and date-only values retain their existing parsing paths. | (a) Drop the offset directly. (b) retain `OffsetDateTime` on the DTO. (c) use the host default zone. | Dropping an offset without normalization changes the represented instant. UTC provides one deterministic canonical zone without changing the established DTO type. | The original textual offset is lost and a local timestamp with no offset remains inherently ambiguous. |
| DL-128 | The Notion `RestClient` owns a 5-second connect timeout and a 15-second read timeout; timeout-derived resource failures are translated to the fixed message `Notion request timed out`. | (a) Rely on client defaults. (b) configure per call. (c) expose transport messages. | Shared transport bounds prevent stalled upstream calls from occupying request and scheduler threads indefinitely, and fixed translation prevents vendor or request details from reaching higher error surfaces. | Slow successful Notion operations can be aborted, and timeout classification includes cause types whose simple name contains `Timeout` for cross-client compatibility. |
| DL-129 | OpenAI requests prepend a system instruction that treats the entire source-compatible user prompt as untrusted data, forbids tools or publication claims, and requires a concise human-reviewed draft. | (a) Send only the retired single user message. (b) escape or delete untrusted tweet text. (c) add a publishing tool. | The user prompt must retain its audited literal segments, but tweet text and tool names are attacker-controlled. A higher-priority system message separates trusted constraints from that verbatim data and preserves the structural no-publish boundary. | Model behavior changes from the retired single-message call, and prompt injection is mitigated rather than mathematically eliminated. |
| DL-130 | The lazily created `OpenAIClient` is closed under the creation monitor during bean destruction and cannot be recreated afterward. | (a) Never close it. (b) construct and close per request. (c) close without coordinating with creation. | The client owns transport resources and the singleton already has a synchronization boundary for creation; using the same monitor gives one lifecycle transition and idempotent shutdown. | A call racing with container shutdown can observe the closed state and fail, which is appropriate once destruction begins. |
| DL-131 | Framework request failures have explicit JSON handlers: malformed input and conversion are 400, method mismatch 405, unacceptable response media 406, and unsupported request media 415; unexpected failures retain the generic 500. | (a) Let all reach the catch-all 500. (b) use framework `ProblemDetail`. (c) invent per-exception messages. | These are client request conditions with standard HTTP reason phrases. Routing them through the existing one-key `ErrorResponse` preserves envelope shape and avoids exposing parser or converter details. | The four reason phrases become additional stable response strings even though the retired Flask app registered no equivalent handlers. |
| DL-132 | `GlobalExceptionHandlerTest` uses Spring's `ExceptionHandlerMethodResolver` for assignability; authentication and access-denied exceptions resolve to the declared generic `Exception` handler rather than being described as unhandled. | (a) Compare annotation values by exact type. (b) remove the negative security assertion. (c) add auth-specific advice. | Spring resolves exception handlers by assignability, so an exact-set helper gave a false account of runtime behavior. Resolver-based assertions pin the actual MVC semantics while the security-chain tests separately prove filter failures are answered before MVC. | A future narrowing of the generic handler changes these assertions even if protected-route security behavior remains unchanged. |
| DL-133 | DTO and mapper components backed by nullable source columns use boxed/reference types and preserve null; only externally required persistent identifiers are rejected as null. | (a) Primitive components. (b) add database `NOT NULL` constraints. (c) replace every null with a fabricated zero, false or empty scalar. | The source schema permits those columns to be null. Boxed values and null-preserving mapping prevent unboxing and constructor failures without narrowing the database contract. | Clients must handle null scalar fields, and the mapper still fails when an entity that must already be persisted has no external identifier. |
| DL-134 | The unbounded character mapping of DL-068 also applies to `Tweet.content`, `Tweet.media`, `Tweet.quotedTweetId`, `Tweet.userId`, `Tweet.aiToolsMentioned` and `Response.content`. | (a) Keep JPA's implicit 255 limit. (b) add six vendor-specific `columnDefinition` values. (c) truncate before persistence. | Each corresponding source field is an unbounded `Column(String)`. Reusing `length = Integer.MAX_VALUE` preserves the established portable mapping, names and nullability across all ten source-unbounded fields. | Fresh H2 schemas use CLOB for these columns, so native test queries must cast textual values when they require a JDBC `String`; existing databases are not widened automatically by `ddl-auto=update`. |
| DL-135 | The frontend Node input remains the AAP-frozen value `14`; only the `actions/setup-node` implementation reference is upgraded and pinned. | (a) Raise the Node input during backend remediation. (b) rename or remove frontend steps. (c) leave the action on its old mutable version. | AAP §0.2.1.6 explicitly freezes frontend CI behavior in this checkpoint. Supply-chain hardening of the action reference is separable from changing the frontend runtime contract. | Node 14 is retired and the frontend job can remain broken or unsafe until separately authorized frontend work changes it. |
| DL-136 | The WIF-authenticated deployment syncs frontend assets with `gcloud storage rsync --recursive`, not `gsutil`. | (a) Keep `gsutil -m rsync -r`. (b) authenticate again with a service-account key for the storage step. (c) replace synchronization with individual object copies. | The Google auth action exports credentials for `gcloud`, while its documented compatibility note excludes `gsutil`. Using the current `gcloud storage` surface keeps the whole deployment on the short-lived federated credential established once at the start of the job. | `gcloud storage rsync` output and edge-case behavior can differ from legacy `gsutil rsync`, and the installed Cloud SDK must include the storage command group. |
| DL-137 | The six remaining character columns that `backend/app/db/models.py` declares as a bare `Column(String)` — `tweets.content`, `tweets.media`, `tweets.quoted_tweet_id`, `tweets.user_id`, `tweets.ai_tools_mentioned` and `responses.content` — declare `length = Integer.MAX_VALUE`, the same form DL-068 applies to the four `ai_tools` and `settings` columns. Ten character columns therefore carry it and `settings.key` (DL-069) is the only bounded one. | (a) Declare no `length` and accept JPA's implicit `varchar(255)`, which is what these six columns carried before. (b) `org.hibernate.Length.LONG32`, the numerically identical Hibernate constant. (c) `@JdbcTypeCode(SqlTypes.LONGVARCHAR)`. (d) `@Lob`. (e) `columnDefinition = "text"`. | SQLAlchemy renders a bare `Column(String)` as an unbounded `VARCHAR`, so (a) adds a 255-character bound that AAP G2 and TR-3 forbid — and because `ddl-auto=update` generates the schema (DL-026), that bound is written into the physical database, where a post body or a generated reply longer than 255 characters then fails to persist. Measured against Hibernate 6.6.53, `Integer.MAX_VALUE` generates `text` on PostgreSQL, `longtext` on MySQL and `clob` on H2, and the identifiers stay `integer` on all three; the reported `COLUMN_SIZE` on the test database is 2147483647 for each of the ten columns and 255 for `settings.key` alone. (b) is the same number but would put an `org.hibernate` import into entities that otherwise depend only on `jakarta.persistence`. (c) was measured as `varchar(32600)`, still bounded. (d) changes the mapping to LOB semantics rather than the DDL type. (e) is not portable across the three supported vendors. | On H2 the ten columns become `CHARACTER LARGE OBJECT`, which H2 refuses to index — neither wanted, since added indexes are out of scope — and which a JDBC driver returns as a `Clob` rather than a `String`, so native-SQL readers must normalise the value. Because `ddl-auto=update` never widens an existing column, a database created before this change keeps `varchar(255)` and will silently diverge from a freshly created one. |
| DL-138 | `tweets.id`, `responses.id` and the `responses.tweet_id` foreign key are typed `Integer`, so `TweetRepository` is `JpaRepository<Tweet, Integer>`, `ResponseRepository` is `JpaRepository<Response, Integer>`, `TwitterService` parses a path identifier with `Integer.parseInt` and `ResponseService` with `Integer.valueOf`. Every surrogate key in the schema is now 32-bit, matching `entity/AiTool` under DL-070. | (a) `Long`, which is what DL-025 previously authorised for this entity family and what the literal `JpaRepository<Response, Long>` text of AAP §0.4.1.4 assumes. (b) `Long` on the field with `@JdbcTypeCode(SqlTypes.INTEGER)` to force an `integer` column while keeping the repository type. (c) `Long` plus an explicit `columnDefinition = "integer"`. | `backend/app/db/models.py:L10,L23` declare `Column(Integer, primary_key=True)` and `:L27` declares `ForeignKey('tweets.id')`, and because `ddl-auto=update` generates the schema the Java type decides the physical column type: a `Long` identifier was measured to generate `bigint` on H2, PostgreSQL and MySQL where the source declared a 32-bit column, which AAP G2's requirement of identical column types forbids. DL-070 already applied `Integer` to `ai_tools.id` on exactly this reasoning, so the two entity families are now uniform rather than split. The width of the association partner, which is what DL-025 cited in favour of `Long`, is satisfied either way once both sides are `Integer`. (b) reaches the right column type but leaves the wrong type on the page for a reader and a reviewer, and adds a Hibernate-specific annotation to entities that otherwise use only `jakarta.persistence`. (c) is not portable. | Each of the two tables is capped at 2^31−1 rows, which is what the source declared. The wire contract is unaffected, because every identifier serialises as a `String` (DL-023). An existing database created with `bigint` keys is not narrowed by `ddl-auto=update`, so it keeps the wider column and diverges from a freshly created one; the generated column types are asserted for all three supported vendors so the parity cannot regress unnoticed. |
| DL-139 | The wire records separate the primary key from every other column. `TweetDto.id` and `ResponseDto.id` reject `null`, and the two mappers require the entity to carry an identifier. Every other component maps a column declared without `nullable=False`, so `TweetDto.content`, `.likeCount`, `.createdAt`, `.doubtRating`, `.quotedTweetId`, `.userId` and `ResponseDto.content`, `.generatedAt`, `.isApproved`, `.tweetId` are boxed and nullable and serialise as JSON `null`; `media` and `aiToolsMentioned` stay non-null because `util/DelimitedStringListConverter` never yields `null`. `LlmService.generateResponse` therefore returns the generated text, and `service/ResponseService` builds the `ResponseDto` only from the saved row. | (a) Keep every component non-null, which is the literal reading of TR-6 for a required Pydantic field. (b) Keep the components non-null and substitute a default in the mappers — `0`, `0.0`, `false`, an empty string, the epoch. (c) Make `ResponseDto.id` nullable so `LlmService` can return an unsaved record, which is what it did before. (d) Return an internal generation-result record from `LlmService` instead of a bare `String`. | The columns at `backend/app/db/models.py:L11-18,L24-27` are all declared without a not-null marker and AAP G2 forbids adding one, so a stored row may legitimately leave any of them unset; (a) turns such a row into a `NullPointerException` inside the mapper, which the error-handling advice renders as HTTP 500 for a row the database considers valid. (b) puts values on the wire that no column holds, which misreports the data and cannot be distinguished by a client from a genuine zero or a genuine `false`. (c) contradicts `backend/app/schema/response.py:L5`, which declares `id: str` as required, and it weakens the contract of every stored row to model one transient value. (d) is equally sound, but the only component `LlmService` could populate is the text itself — `backend/app/services/llm_service.py:L32` set `content` and `tweet_id` and nothing else, and DL-077 already assigns `generated_at` where the row is built — so a record would carry one field. | A client must handle a JSON `null` for any of the ten nullable members, which the frontend's Zod contracts do not currently declare; that divergence sits alongside the ones DL-059 records and is not fixed here. `TweetDto.likeCount` and `.doubtRating` are now boxed, so an arithmetic reader must guard for `null` — `service/LlmService` does so when composing the `Context:` clause. Returning a bare `String` means a future second generated value would change the method signature rather than extend a record. |
| DL-140 | `api/GlobalExceptionHandler` declares four further handlers so that a request the framework rejects before or during handler invocation carries a 4xx status rather than the catch-all 500: a body it cannot read, a binding failure and a path or query value of the wrong type answer 400 `Bad request`; a method the matched path does not serve answers 405 `Method not allowed` with an `Allow` header; a request body media type nothing consumes answers 415 `Unsupported media type` with an `Accept` header; and an `Accept` header no producible type satisfies answers 406 `Not acceptable`. The generic 500 `Internal server error` is left for a genuine server failure. | (a) Leave every one of them on the `@ExceptionHandler(Exception.class)` path, which is what the class did before. (b) Extend `ResponseEntityExceptionHandler` and override its protected hooks. (c) Emit the correct status with an empty body, adding no new wire string. (d) Emit the correct status carrying `Internal server error`, reusing an existing literal. | Flask registered handlers for 404 and 500 only (`backend/app/main.py:L31-37`) and rendered its own page for everything else, so the *status* the source returned for these four conditions was 400, 405, 415 and 406 — never 500. Reporting a malformed body or a wrong method as a server fault is therefore both a contract break and a diagnostic lie: it tells a client to retry and an operator to investigate the service. (b) would replace the single-key `{"error": …}` envelope with `ProblemDetail`, which IR1 forbids and which `api/GlobalExceptionHandlerTest` asserts against. (c) is defensible — the source body was HTML, so no JSON literal is being preserved either way — but it would make these the only statuses this service answers with no body, and a JSON client would have to special-case them. (d) would report four distinct client faults under a message that names a server fault. The four new literals are sentence-case, matching `Not found` and `Internal server error`. | Four wire strings exist that no source construct produced, so the service's error-string set grows from ten to fourteen; they are named here rather than derived from the source, exactly as the pagination keys of DL-038 are. A client matching on the `error` value must know them. Because `MethodArgumentTypeMismatchException` is mapped to 400, a future typed `@RequestParam` would answer 400 where Flask's `type=int` coercion returned the parameter default — no such parameter is declared in the module today, and the two paginated routes' `page` and `per_page` defaults are applied in the service layer (DL-077). |
| DL-141 | The algorithm `scanner.jwt.algorithm` names is honoured exactly: it selects the algorithm every minted token is signed with, the JCA name of the key derived from `scanner.jwt.secret` (`HmacSHA256`, `HmacSHA384` or `HmacSHA512`), and the single algorithm the parsing path accepts. A secret supplying fewer bits than that algorithm's key length fails startup with an `IllegalStateException` naming the shortfall. | (a) Sign every token with HS256 whatever the property names, and leave the parser's algorithm set unrestricted — the behaviour delivered before this entry. (b) Reject every value other than `HS256` at startup. (c) Keep `Keys.hmacShaKeyFor(byte[])`, which picks the algorithm from the secret's length. (d) Honour the property when signing but leave the parser unrestricted. | (a) discards operator configuration silently: an operator who sets `ALGORITHM=HS512` gets HS256 tokens with no diagnostic, and because the parser accepts every HS variant of the same key, a token signed HS384 or HS512 with that same secret verifies as well — so the property constrains nothing in either direction. (b) contradicts DL-015, which declares all three HS members supported. (c) derives the algorithm from the secret's length rather than from configuration, so a 64-byte secret yields a `HmacSHA512` key even where HS256 is configured, and a 32-byte secret with `ALGORITHM=HS512` starts successfully and then fails at the first mint. (d) leaves the acceptance side open, which is the half that matters for a presented token. jjwt 0.13.0 supports the restriction directly: `JwtParserBuilder.sig().clear().add(alg)` replaces the standard algorithm registry with the one entry, and a token whose `alg` header names anything else is reported as `SignatureException`, which the parsing path answers with an empty optional. | Raising the configured algorithm now demands a longer `SECRET_KEY`: `ALGORITHM=HS512` over a 32-byte secret fails startup instead of starting and failing at the first mint, which is a louder failure but a failure at a different time than an operator may expect. Changing the algorithm invalidates every token already issued, because the parser no longer accepts the old `alg` — the same client-visible effect as rotating the secret under DL-016. The JWS-identifier-to-JCA-name pairing is a table in `security/JwtService`; a fourth HS member would have to be added in both that table and the algorithm table. |
| DL-142 | `scanner.jwt.expiration-minutes` is validated to 1 … 60 inclusive while `security/JwtService` is constructed, so a value outside that range fails startup, and the conversion to seconds is performed once with `Math.multiplyExact`. | (a) Accept whatever configuration binding produces — the behaviour delivered before this entry. (b) Clamp an out-of-range value into the range silently. (c) Declare `@Min(1) @Max(60)` on the record component and annotate `ScannerProperties` with `@Validated`. (d) Reject only zero and negative values, leaving the documented maximum unenforced. | DL-017 fixes the lifetime's documented maximum at one hour, and a lifetime is only meaningful above zero, so 1 … 60 is the range the two together define. (a) admits `0` and negative values, which mint a token whose `exp` is at or before its `iat` so every request it is presented on is answered 401, and admits values near `Long.MAX_VALUE`, where the minutes-to-nanoseconds conversion inside `Duration.ofMinutes` overflows and the failure surfaces as an arithmetic error at the first mint rather than at startup. (b) hides a misconfiguration, which is the failure mode DL-016 already rejects for the secret. (c) is a working mechanism but splits the `scanner.jwt` group's validation across two files while the secret and the algorithm are already validated in `JwtService`, and it reports a binding failure rather than a message naming the property, the offending value and the accepted range. (d) leaves the documented maximum a comment. | The upper bound is a compile-time constant, so an operator who wants a longer session cannot configure one: the bound and DL-017 must both change. A deployment that had been running with an out-of-range value — nothing in this repository ships one — stops starting after this change rather than continuing with its previous behaviour. |
| DL-143 | A token whose signature verifies but which carries no `exp` claim is refused: the parsing path answers an empty optional, so neither `extractUsername` nor `extractExpiration` returns a value for it. | (a) Accept it, which is what jjwt does on its own because it validates `exp` only when the claim is present — the behaviour delivered before this entry. (b) Accept it and apply the configured lifetime to its `iat` as an implied expiry. (c) Require `iat` as well, and refuse a token whose `iat` lies in the future. | Every token this service mints carries `exp` (DL-018), so a same-key token without one was not minted here. Accepting it would make that token valid for as long as `SECRET_KEY` lives, which is precisely the exposure the bounded lifetime of DL-017 exists to limit, and it would do so with no revocation path. (b) invents an expiry the token does not carry and makes acceptance depend on configuration that the token's issuer never saw. (c) is defensible and would catch a clock-skewed or forged `iat`, but `iat` is informational in RFC 7519 while `exp` is the claim that bounds validity, and adding an `iat` rule would refuse tokens that are otherwise entirely valid under this service's own contract. | A second issuer sharing `SECRET_KEY` and minting tokens without `exp` would find them refused; no such issuer exists in this repository, where `AuthController` is the only mint path. The refusal is recorded as the `EXPIRATION_ABSENT` reason code of DL-087, so the cause is visible only when `DEBUG` is enabled for `security.JwtService`. |
| DL-144 | A verification failure is recorded at `DEBUG` as one of six fixed reason codes — `MALFORMED`, `SIGNATURE_REJECTED`, `EXPIRED`, `UNSUPPORTED`, `EXPIRATION_ABSENT`, `REJECTED` — with the count of records suppressed for that reason since it last produced one. Each reason produces at most one record per minute. No token, claim, principal, request method, request URI, exception type or parser message is written. `JwtAuthenticationFilter` writes one fixed `DEBUG` sentence when a request authenticates and nothing at all when a presented token does not verify. | (a) `WARN` carrying the exception's simple name and message for every failure, and `DEBUG` carrying the principal name and request URI on success — the behaviour delivered before this entry. (b) The same fixed reason codes, but one record per failure with no sampling. (c) A Micrometer counter per reason instead of a log record. (d) Keep the principal name on the success record. | An unauthenticated caller chooses both how many failures arrive and what the token contains, so (a) hands that caller control of log volume at a level that is enabled by default, and jjwt's messages quote the presented value and internal parser detail, which is the CWE-532 exposure the review names; the success record additionally wrote a principal name and a caller-supplied URI, whose control characters a text log preserves verbatim (CWE-117). Moving to `DEBUG` puts the records behind an operator decision, fixed codes keep them free of caller-controlled text, and the per-reason interval bounds volume even once `DEBUG` is on. (b) leaves the flooding half of the finding unaddressed. (c) is the right shape for rate data but needs Micrometer and an actuator endpoint, and an observability stack is outside the scope boundary. (d) is the single most useful field for an operator, and it is exactly the field the finding asks to omit; the reason code plus the container's own access log covers the same ground. | With `DEBUG` enabled, a burst of identical failures appears as one record plus a count, so the timing of individual attempts is not recoverable from the log; a reason's suppressed count is only reported on that reason's next record, so a burst followed by silence leaves its count unreported until the next failure of the same kind. The interval is a compile-time constant. Correlating a failure with a request now requires the container access log, because the record names no request. |
| DL-145 | Only a complete, non-blank reply is accepted from the model. A response carrying no choice, a first choice carrying a non-blank `refusal`, a finish reason other than `stop`, or text that is blank once trimmed is reported under a fixed unusable-output code (`NO_CHOICE`, `REFUSAL`, `INCOMPLETE:<finish_reason>`, `BLANK_TEXT`) and raised as `IllegalStateException`, which `service/ResponseService` reports as the 500 literal of `backend/app/api/responses.py:L49`. Alongside it, `scanner.openai.reasoning-effort` is declared with the default `minimal`, and a blank value omits the parameter. | (a) Return the first choice's text whatever it is, as the source did and as the previously delivered adapter did, so an empty string became a stored draft. (b) Reject blank text only, without inspecting the refusal or the finish reason. (c) Retry once on `LENGTH` with a larger budget. (d) Raise the transcribed `max_completion_tokens` of 150 instead of naming a reasoning effort. (e) Leave the reasoning effort unset and rely on the provider default. | The source's `choices[0].text.strip()` at `backend/app/services/llm_service.py:L29` ran against a completions model with no reasoning phase, no refusal field and a finish reason nobody read, so "whatever came back" was a workable contract. Against the configured chat model it is not: the 150-token budget of DL-034 is shared between hidden reasoning tokens and the visible reply, so a truncated or entirely empty reply is a realistic outcome, and a refusal or a content-filter stop is a normal provider response rather than an exception. Each of those stored a row a human would later review as if it were a draft. (b) leaves a truncated half-sentence acceptable. (c) doubles the provider cost of a failure and would spend a second budget on the same prompt with no evidence the first was too small; the operator can raise the budget instead. (d) changes a transcribed literal the parity mandate fixes at 150. (e) leaves the split between reasoning and visible tokens undocumented, which is exactly what makes the empty reply hard to diagnose; `minimal` keeps the 150 tokens available to the reply while remaining a value the SDK recognises, and a blank value is the escape hatch for a model that rejects the parameter. | Generation now fails where it previously produced a row, so the 500 rate rises for prompts that were silently storing empty drafts; that is the intended change and it makes the condition visible. `INCOMPLETE:<finish_reason>` puts a provider token in a log record — it is an enumerated token, never generated content. `reasoning-effort` is sent to the provider, so a model that does not accept the parameter fails every request until the value is blanked; the accepted values are the SDK's and are checked before the request is built. |
| DL-146 | The OpenAI client carries an explicit request timeout (`scanner.openai.request-timeout-seconds`, default 30) and an explicit retry limit (`scanner.openai.max-retries`, default 2), and `ResponseService.generateResponse` declares no transaction, so the model call sits between two short repository transactions. No circuit breaker is added: a provider failure surfaces as the 500 literal of `backend/app/api/responses.py:L49`. | (a) Keep the SDK defaults implicitly, as the previously delivered adapter did. (b) Set a timeout but leave retries at the SDK default. (c) Keep `@Transactional` on `generateResponse` and accept that a connection is held for the duration of the model call. (d) Mark the method `@Transactional(propagation = NOT_SUPPORTED)` instead of removing the annotation. (e) Add resilience4j and a circuit breaker around the call. | A timeout and a retry count that are chosen by the library rather than by this service are a deployment property nobody reviewed; naming them makes the worst-case wait per attempt arithmetic rather than a guess. The database consequence is the sharper one: with a transaction open across the call, one slow provider response holds a pooled connection for its whole duration, so a handful of concurrent generations can exhaust the pool and stall unrelated requests. (c) accepts exactly that. (d) is equivalent in effect but leaves an annotation on the method that suggests a transactional contract it does not have. (e) is a new capability with its own dependency, configuration surface and failure semantics, and the scope boundary excludes it. | Removing the transaction means the insert commits before the row is mapped, so a failure after the insert leaves a stored draft rather than rolling it back; the only step after the insert reads columns of the row the insert returned. The mapped `Tweet` is detached, so any future mapper change that touched the lazy `responses` collection would fail outside a transaction — the mapping test of DL-082 covers the current reads. Retries multiply the worst-case wait: three attempts at a 30-second timeout is a 90-second ceiling per request. |
| DL-147 | `LlmService` closes its OpenAI client in a `@PreDestroy` method, at most once, and refuses to create another afterwards. | (a) Leave the client to JVM termination, as the previously delivered adapter did. (b) Implement `AutoCloseable` and rely on the container's inferred destroy method. (c) Close without a closed flag, so a late call creates a fresh client during shutdown. | The client owns an OkHttp dispatcher, a connection pool and their threads; leaving them to process exit means a context restarted inside a living JVM — which is what an integration test and a devtools reload both do — leaks one set per restart. (b) works, but `AutoCloseable` on a service invites callers to close it and makes the shutdown contract implicit. (c) would let a request arriving during shutdown resurrect a client that nothing will close afterwards. | A request that reaches the service after shutdown has begun fails with an `IllegalStateException` naming the closed client rather than being served; graceful shutdown drains in-flight requests before `@PreDestroy` runs, so this is the shutdown-race path only. A failure raised while closing is swallowed by type name so that shutdown continues. |
| DL-148 | A provider failure is written to the log exactly once, by the adapter that made the call, and the record carries only the HTTP status, the provider's error `code` and `type`, and the `x-request-id` response header. The throwable is never handed to the logger, in `LlmService` or in `ResponseService`; the cause travels on the exception instead. | (a) Log the throwable with a stack trace at the point of failure and again at the orchestrating layer, as the previously delivered code did. (b) Log the exception's message without the stack trace. (c) Log nothing and rely on the exception reaching a handler. | The OpenAI SDK documents that a service exception's message can carry the provider's response JSON, which for this service means the generated reply and whatever the prompt quoted from a monitored post — a CWE-532 exposure in a log that is not treated as sensitive. Logging it twice doubled both the exposure and the noise. (b) still copies the provider body, because the body is inside the message. (c) removes the diagnostic entirely: the status, the error code and the request identifier are the fields a support request to the provider needs, and none of them is content. | The stack trace is no longer in the log, so an unexpected failure has to be diagnosed from the exception's type name plus the request identifier. `code` and `type` are provider-defined strings; they are enumerated error identifiers rather than content, but they are still values this service does not control. |
| DL-149 | Every caller-supplied identifier is rendered through a `logSafe` guard before it reaches a log record: characters outside printable ASCII become `?` and the value is truncated to 64 characters. | (a) Interpolate the raw path segment, as the previously delivered code did. (b) Log the parsed identifier only, after conversion, and nothing when conversion fails. (c) Add an encoder to the Logback configuration that escapes control characters for every record. | A path segment reaches these methods decoded, so `%0A` in a request URI arrives as a real line break and a text log preserves it verbatim — the caller can forge a record boundary or an entire fabricated record (CWE-117). (b) loses the diagnostic exactly when it matters, because the interesting case is the value that did not parse. (c) is a global fix with a global blast radius: it would rewrite every record this service and every library it embeds emits, and a `logback-spring.xml` is deliberately absent under DL-052. The guard is applied at the four call sites that log a caller-supplied value. | The guard is a private method in each service that needs it rather than a shared utility, because the target file inventory declares no such utility class; a second copy exists wherever another service logs a caller-supplied value, and the two must stay consistent. A value that legitimately carries non-ASCII characters is rendered with `?` in place of them, so a log record is not a faithful copy of the request. |
| DL-150 | The Notion transport carries a bounded connect timeout (`scanner.notion.connect-timeout-seconds`, default 5) and a bounded read timeout (`scanner.notion.read-timeout-seconds`, default 10), applied through a request factory built from `ClientHttpRequestFactorySettings`. A Notion failure reaches the caller as a `RestClientException` and is not retried; the relational store has already accepted the data, so the mirror failing never fails the primary write. | (a) Leave the framework defaults, which are null timeouts — the state delivered before this entry. (b) Set only a read timeout. (c) Add a retry with backoff. (d) Add a circuit breaker so a failing mirror stops being called. | Boot's default request-factory settings carry no timeout at all, so a Notion request that never answers holds its caller — a request thread or, once the task files land, the scheduler thread — for as long as the socket stays open. A secondary mirror must not be able to do that. (b) leaves a connect that hangs on an unreachable host unbounded. (c) and (d) are both defensible for a mirror, but each is a new capability: a retry needs an idempotency story for `pages.create`, which is not idempotent, and a breaker needs resilience4j plus its own configuration surface. The scope boundary excludes both, so the failure policy is stated instead: fail the mirror call, keep the relational write. | Five and ten seconds are chosen without production measurements; a slow but healthy Notion response beyond ten seconds now fails where it previously completed. A caller that treats a mirror failure as fatal would propagate it — no caller exists yet, and the policy above is what the task files must follow when they land. |
| DL-151 | The `Notion-Version` header value is `scanner.notion.api-version`, defaulting to `2022-06-28`, and the adapter keeps using the database-query endpoint `POST /v1/databases/{id}/query`. | (a) Keep the version pinned in code with no property, as delivered before this entry. (b) Move to a current API version and the data-source endpoints it introduces. (c) Send no version header and accept the provider's default. | `2022-06-28` is the version whose request and response shapes the adapter is written against: the property-object shapes, the `results` array and the query filter all match it, and the configuration surface this project declares carries a database identifier (`NOTION_DATABASE_ID`) and nothing else. (b) is the upgrade path and is recorded as such — current Notion versions address a *data source* inside a database, so it needs a new configuration value, a new endpoint set and a re-verified response shape; that is a change of external contract rather than a defect fix, and AAP scope forbids adding the capability here. (c) is not an option: the header is mandatory, and omitting it makes the provider choose a version the code has not been written against. Making the value a property is what lets an operator move a deployment forward, or pin it back, without a rebuild. | The pinned version will eventually be withdrawn by the provider, at which point every Notion call fails until the property is raised and the response mapping is re-verified — the mapping is written against this version's shapes, so raising the property alone is not sufficient. The upgrade plan is: declare the data-source identifier, switch the query path, re-verify the property shapes, then raise the default. |
| DL-152 | Before its first write, the adapter reads the target database once and confirms that it declares all seven properties this mirror writes with the types it writes — `Content` title, `Author` rich_text, `Timestamp` date, `Doubt Rating` number, `Engagement` number, `Tweet Id` rich_text, `Response` rich_text — failing with an `IllegalStateException` that names every absent or mistyped property. The check is remembered on success, so it runs at most once per instance. | (a) Write and let the provider reject the request, as delivered before this entry. (b) Create the missing properties by patching the database schema. (c) Document the required schema and check nothing. (d) Re-check on every write. | Notion rejects a page whose property keys or types do not match the parent database, so an operator who provisioned the database by hand — the only way it can be provisioned here — learns of a naming mistake as a provider validation error on every single mirror attempt, with the property name buried in a body this service is forbidden to log (DL-096). One up-front read turns that into one startup-shaped failure naming exactly what is wrong. (b) has this service own a schema an operator also edits, and a mirror that mutates its target's structure is a larger authority than a mirror needs. (c) leaves the diagnosis where it was. (d) spends a request per write for a schema that changes almost never. | The first write pays one extra request, and a database edited to remove a property after the check succeeded is not noticed until the process restarts. Seven property names are now a contract with the operator's Notion workspace; the four inherited from the source keep their original names, and the three that changed are recorded in the entries covering the property map. |
| DL-153 | A Notion failure is logged once, carrying the HTTP status, the status text, the `code` member of the error body when it matches `[a-z_]{1,64}`, and the `x-request-id` header. The response body is never logged and the throwable is never handed to the logger. | (a) Log the throwable, which renders the method, the URI and the response body, as delivered before this entry. (b) Log the exception message only. (c) Log the whole error body but only at DEBUG. (d) Log the status alone and read nothing from the body. | A `RestClientResponseException` renders the request method, the request URI and the response body, and the body of a failed Notion write echoes the property values that were sent — the mirrored post's text, its author identifier and its identifiers — into a log that is not treated as sensitive (CWE-532). (b) is the same exposure by another route, because the body is inside the message. (c) puts the exposure behind a level rather than removing it, and DEBUG is enabled on exactly the systems where someone is investigating. (d) is safe but discards `code`, which is the field that distinguishes a validation mistake from a rate limit from a revoked token; extracting only that member, and only when it is shaped like an identifier, keeps the diagnosis without the content. | The `code` extraction parses the error body in memory; a body large enough to matter is a provider anomaly and is still never written anywhere. Losing the body means a validation error's specific message is no longer in the log — the schema check of DL-095 is what covers the common cause. |
| DL-154 | A query's `page_size` is held between 1 and 100: a non-positive limit becomes the transcribed default of 10 and a limit above 100 is capped at 100 and reported. | (a) Forward whatever the caller asked for, as delivered before this entry. (b) Reject a limit above 100 with an exception. (c) Page automatically until the caller's larger limit is satisfied. | The provider rejects `page_size` above 100, so forwarding 500 produces a guaranteed validation failure for a request that could have been served. (b) turns a caller's over-ask into an error on a read path whose contract is "up to this many", where returning up to 100 satisfies the contract. (c) is a new capability — several requests per call, an accumulating result set and a partial-failure story — and the cursor parameter already lets a caller page deliberately. | A caller asking for 500 silently receives at most 100 and must use the cursor to continue; the cap is reported at `WARN` so the truncation is visible. |
| DL-155 | Text written to a title or rich-text property is split into items of at most 2000 characters, at most 100 items are written, and a blank value is written as an empty array rather than as an item carrying empty text. A `null` value still omits the property. | (a) Write one item carrying the whole value, including an empty string for a blank value, as delivered before this entry. (b) Truncate to 2000 characters and write one item. (c) Reject a value longer than the limit. | The provider rejects `text.content` of an empty string and rejects an item longer than 2000 characters, so the previous form failed for both a blank author identifier and a post body over the limit; the empty array is the representation the provider documents for clearing a property, which is the closest thing to "present but carrying nothing". (b) throws away up to 198,000 characters of a value the provider would have accepted across items. (c) fails a mirror for content the primary store holds happily, which inverts the mirror's role. | A value beyond 200,000 characters is truncated, with the dropped character count reported and the text itself never logged. Splitting means a single logical value is several items in Notion; reading it back through `readText` returns the first item only, so a very long mirrored body reads back shortened. |
| DL-156 | A Notion response is validated before it is mapped. An absent body, a query response carrying no `results` array, a created page carrying no identifier and a matched page carrying no identifier are each reported under a fixed malformed-response code and raised as `IllegalStateException`; an empty `results` array is an empty result rather than a failure. A page is mapped only when its `Tweet Id` property carries text, the `Timestamp` offset is normalised to UTC before it is dropped, and an `Engagement` number that is fractional or outside the range of the like-count column is reported as absent. | (a) Treat each of those as success or as empty, which is what was delivered: an absent page identifier returned an empty string, an absent body returned an empty list, a page with no `Tweet Id` borrowed the Notion page UUID, an offset date-time silently lost its offset, and a number was narrowed with `intValue()`. (b) Validate the response but keep the page-UUID fallback. (c) Skip a malformed page rather than failing the whole query. | Each previous behaviour produced a value that looked legitimate: an empty page identifier reads as a page that exists, a Notion UUID reads as a post identifier and correlates with nothing, `2026-02-14T08:45+02:00` read back as `08:45` UTC — a two-hour error — and `3000000000` narrowed to a negative like count. A mirror that answers with plausible wrong data is worse than one that fails, because nothing downstream can detect it. (b) keeps the one fallback that manufactures an identifier. (c) is what is done for a page missing `Tweet Id`, which is a page this mirror did not write and cannot interpret; a body or a `results` array of the wrong shape is a different thing — it means the response is not the response this code was written against, and continuing past it would map arbitrary JSON. | A query whose response shape changes — a provider version bump under DL-094, for instance — now fails loudly instead of returning an empty list. Pages written by anything other than this mirror are skipped rather than partially mapped, and the count of skipped pages is one record per query, so which pages were skipped is not recoverable from the log. UTC normalisation means a timestamp read back may differ from the local time originally written, which is the correct instant rather than the original wall clock. |
| DL-157 | `updateTweetResponse` tests its documented no-op — a `null` or blank tweet identifier — before it reads `scanner.notion.database-id`. | (a) Read the configuration first, as delivered before this entry. (b) Read the configuration first but downgrade its absence to a warning on this path. | The method documents that a blank identifier leaves Notion untouched and raises nothing; with the configuration read first, an unconfigured mirror threw `IllegalStateException` for a call that was going to do nothing anyway, so the no-op was unreachable exactly when it mattered least and most: on a deployment with no Notion configured at all. (b) makes an unconfigured mirror silent on a path that does need the database, which hides a real misconfiguration. | None: no other behaviour changes, and a call that does reach Notion still fails when the database identifier is absent. |
| DL-158 | `getSummary()` reads its seven metrics with three statements, one per table: `TweetRepository.findAggregates()` returns `count`, `avg(doubtRating)` and `avg(likeCount)` as a closed projection, `ResponseRepository.findApprovalCounts()` returns the total row count and the approved row count as a closed projection, and `count()` on `ai_tools` returns the remaining metric. `pending_responses` is the difference of the two counts the second statement returned together. The derived `countByIsApprovedTrue()`, `findAverageDoubtRating()` and `findAverageLikeCount()` operations are withdrawn. The documented guarantee is narrowed: metrics one statement returns together read one state of the table it names, and no guarantee spans two statements. | (a) Keep the six independent queries and keep the snapshot wording, as delivered before this entry. (b) Keep the six queries and raise the transaction to `REPEATABLE READ` or `SERIALIZABLE`. (c) Keep the six queries and drop the snapshot wording without changing the code. (d) Compute `pending_responses` with its own `count` predicate instead of subtracting, leaving five reported counts and no derived value. (e) Read all seven metrics in a single statement joining the three tables. | Six independent statements under the isolation level the connection supplies each read the rows committed when they execute, so `total_responses` and `approved_responses` could be read across two states and their difference could be negative; the class documented a snapshot it did not deliver. One statement per table removes that arithmetic hazard at its source and leaves the remaining cross-table looseness stated rather than claimed away. Alternative (b) changes the isolation level, which the AAP places out of scope alongside indexes and constraints, and would serialise a reporting read against ingestion. Alternative (c) leaves the negative-difference outcome reachable. Alternative (d) still spans two statements for `total_responses` and `pending_responses`, so the two could disagree. Alternative (e) needs a cross join or three correlated subqueries and would report zero rows for one table as no row at all. | The two projections are closed interfaces whose accessor names must stay aligned with the select aliases; a rename in one place and not the other fails at runtime rather than at compile time. `sum(case when ...)` yields `null` over an empty table, so the approved total is routed through the same absent-value helper as the trend totals. `total_tweets` no longer comes from `count()`, so a reader looking for that call in `AnalyticsService` will not find it. The statement-count assertions depend on `hibernate.generate_statistics`, which is enabled for the mapping test class only. |
| DL-159 | `seedDefaultSettings()` declares no transaction, so each repository call it reaches demarcates its own. `seedIfAbsent` tests presence with `existsById`, writes with `saveAndFlush`, catches `DataIntegrityViolationException`, re-establishes presence and returns quietly when the key is now taken, and rethrows when the key is still absent. A failure that is not an integrity violation is not caught. | (a) Keep `existsById` followed by `save` inside one transaction, as delivered before this entry. (b) Keep the outer transaction and add the catch inside it. (c) Have `Setting` implement `Persistable<String>` so the write becomes a JPA persist and can never update. (d) Issue a vendor-specific upsert such as `insert ... on conflict do nothing` as a native query. (e) Take an application lock, or a database advisory lock, around the seeding. (f) Drop the seeding and require an operator to insert the three rows. | The check and the write were separate statements, so two instances starting against one database could both observe absence and the loser's insert could be rejected on the primary key while `ApplicationReadyEvent` was still being handled synchronously. Flushing inside `seedIfAbsent` turns that rejection into a `DataIntegrityViolationException` raised where it can be handled, and re-establishing presence distinguishes a key another writer took from a rejection that leaves the key absent. Removing the outer transaction is what makes the recovery reachable: a rejected write inside a wider transaction marks it rollback-only, so the re-check would fail as well and the remaining two keys would never be attempted. Alternative (b) keeps that outcome. Alternative (c) changes an entity to carry transient state and two JPA callbacks for a benefit the catch already delivers. Alternative (d) is not portable across PostgreSQL, MySQL/MariaDB and H2 and would need the reserved-word columns `key` and `value` quoted per vendor, which the AAP's dialect-agnostic rule forbids. Alternative (e) adds coordination machinery the AAP does not carry. Alternative (f) leaves `PUT /settings/{key}` answering 404 for every key on a fresh database. | `settings.key` is an assigned `String` primary key, so the write is a JPA merge and reads the row before inserting. A key taken between `existsById` and that read is therefore found and updated, storing the value and description this call computed; two instances holding the same configuration store identical content, while two holding different configuration resolve to whichever wrote last. The insert-only wording is narrowed accordingly: a key this operation observes as present is left untouched. `saveAndFlush` issues one extra flush per seeded key at startup. |
| DL-160 | Both `FROM` instructions of `infrastructure/docker/Dockerfile.backend` name an immutable OCI image index digest beside the tag: `maven:3.9-eclipse-temurin-21@sha256:c07f7ccfb8ca6c9fa29ee523f00afa7d2ca6132c92f8652c4aebb5ee3491f502` and `eclipse-temurin:21-jre@sha256:8cef5fc7bebe421363ab543a2f4db5caf7d119d8db67d56b0f56c485d2de4d55`. A refresh is a reviewed edit to these two lines. | (a) Keep the mutable tags, as delivered before this entry. (b) Pin the platform-specific manifest digest of the image the build host resolves. (c) Pin a narrower tag such as a full Maven and JDK patch version without a digest. (d) Move the digests into build arguments or an external file so a refresh needs no Dockerfile edit. (e) Add a scheduled workflow that rewrites the digests automatically. | A mutable tag lets one source revision build on different base contents on different days, so a build is not reproducible and a base change cannot be attributed. An index digest is the narrowest immutable reference that still resolves for every published architecture. Alternative (b) is immutable too but pins one architecture and would fail a build on any other. Alternative (c) is still mutable, since a tag can be republished. Alternative (d) hides the pinned value from the diff a reviewer reads, which is the whole point of pinning. Alternative (e) is the automation the finding invites, but the AAP places workflow additions outside the backend build carve-out, so the refresh stays a reviewed edit and the tag is retained beside each digest so a reader can see what is being pinned. | A pinned base receives no security update until the digest is refreshed, so the two lines must be revisited deliberately rather than drifting forward on their own. The digests were resolved and the image was built and run during this work; a digest recorded here and later deleted upstream would fail the build with a manifest-not-found error rather than silently falling back to the tag. |
| DL-161 | The runtime stage creates group and user `scanner` at the fixed identifiers 10001:10001 with no home directory and `/usr/sbin/nologin` as the shell, copies the jar with `--chown=10001:10001`, and declares `USER 10001:10001` before `EXPOSE` and `CMD`. The identifiers are build arguments so they can be overridden without editing the instructions. | (a) Keep the runtime stage without a `USER`, as delivered before this entry, leaving the process as root. (b) Use the base image's own unprivileged account if one exists. (c) Declare `USER` by name only. (d) Let the platform impose a user, for example through a Kubernetes security context or Cloud Run's own sandbox. (e) Create the account with a home directory and a login shell. | The runtime stage had no `USER`, so the JVM ran as uid 0 with every capability the container runtime granted, and a defect reachable through the HTTP surface would have had root inside the container. A fixed numeric identifier is what an external policy — a read-only root filesystem, a `runAsUser` constraint, a volume ownership rule — can be written against, and it stays stable across base image rebuilds. `eclipse-temurin:21-jre` ships no unprivileged account, so (b) is unavailable. (c) resolves to a name the base image may rename or drop and does not satisfy a numeric `runAsUser` policy. (d) leaves the image itself unsafe wherever no such policy is applied, including a plain `docker run`. (e) grants the account interactive access it never uses. | Every path the process writes to must be owned by 10001 or world-writable; only the jar is copied, and the JVM's temporary directory resolves under `/tmp`, which is world-writable in the base image. A future instruction that writes into `/app` will fail unless it is chowned or placed above the `USER` line. Binding a port below 1024 is no longer possible, which the pinned port 5000 does not require. |
| DL-162 | `entity/Response.tweet` declares `@ManyToOne(fetch = FetchType.LAZY)`. `entity/Tweet.responses` keeps the `@OneToMany` default, which is also lazy, and neither side declares a cascade or `orphanRemoval`. | (a) Omit `fetch` and accept the `@ManyToOne` default, which is `EAGER`. (b) `EAGER` stated explicitly, so that a detached `Response` can always read its parent. (c) `EAGER` on the owning side plus an entity graph or a `join fetch` query for the paging path. | `backend/app/db/models.py:L28` declares `relationship("Tweet", back_populates="responses")` with no `lazy` argument, and SQLAlchemy's default loader strategy is `select` — a lazy load on first access — so lazy is the faithful port and (a) silently inverts it. (a) also loads the parent row of every element of a page of responses, which is one additional statement per row unless the provider batches them. `service/ResponseService` and `service/mapper/ResponseMapper` read the association inside the transaction that loaded the row, and `service/mapper/ResponseMapper` reads only the parent identifier, so nothing outside a transaction traverses it and (b)'s premise does not arise; `spring.jpa.open-in-view` is `false` for the same reason. (c) adds a query-shaping mechanism to compensate for a fetch mode the source never asked for. | A `Response` read outside a transaction and then traversed to `getTweet()` raises `LazyInitializationException` rather than returning a row. Every current caller maps inside its own transaction, so no caller is exposed; a future caller that maps outside one would be. `repository/JpaMappingIntegrationTest` asserts the fetch mode and the empty cascade on both sides so that an accidental change to either is a test failure rather than a silent behaviour change. |
| DL-163 | Every component of `dto/TweetDto` and `dto/ResponseDto` is a nullable reference type and neither record rejects a `null` component. `like_count` is `Integer`, `doubt_rating` is `Double` and `is_approved` is `Boolean`, none of them a primitive. `media` and `ai_tools_mentioned` are the two exceptions to "carried verbatim": a `null` list becomes an empty unmodifiable list, so neither is ever held as `null`. `service/mapper/TweetMapper` and `service/mapper/ResponseMapper` unbox nothing and substitute nothing else. | (a) Keep the primitives and the `Objects.requireNonNull` guards, treating the Pydantic contract's absence of `Optional[...]` as a not-null contract. (b) Keep the primitives and have the mappers substitute a default — `0`, `0.0` and `false` — for a `null` column. (c) Add `nullable = false` to the columns so the database cannot hold `null` in the first place. (d) Wrap each component in `java.util.Optional`. (e) Make `media` and `ai_tools_mentioned` nullable too, for uniformity with the other seven components. | `backend/app/db/models.py:L11-18,L24-27` declares every non-key column as a bare `Column(<Type>)` with no `nullable=False`, so a `null` in any of them is a valid stored row — and the source's own read path applied no substitution, because the `to_dict()` that `backend/app/api/tweets.py:L19,L30` and `backend/app/api/responses.py:L18,L29,L47,L63` called was never defined anywhere. (a) makes those valid rows unreadable: a primitive component unboxes a `null` `Integer`, `Double` or `Boolean` into a `NullPointerException`, and a `requireNonNull` guard turns a `null` `content`, `created_at`, `user_id`, `generated_at` or `tweet_id` into one, so `GET /tweets`, `GET /tweets/{tweetId}`, `GET /responses` and `GET /responses/{responseId}` would each return 500 for a row the schema permits — reachable before any write path is exercised, from a row inserted by anything other than this service. (b) reports a value the database does not hold and makes an unset rating indistinguishable from a rating of zero, which the doubt-rating scale gives a meaning to. (c) is a schema change that AAP G2 forbids. (d) is not a Jackson-friendly shape and would change every JSON key's rendering. (e) was rejected because the wire contract at `backend/app/schema/tweet.py:L11,L14` and `frontend/src/schema/tweet.ts:L9,L12` types both as arrays, and `util/DelimitedStringListConverter` already normalises a `null` column to an empty list (DL-024), so an empty array is what the column's own read contract produces. | A client now has to tolerate `null` for `content`, `like_count`, `created_at`, `doubt_rating`, `user_id`, `generated_at`, `is_approved` and `tweet_id`, where the Pydantic model declared those fields without `Optional[...]`. That divergence is inherent: the model and the table disagreed in the source, and the table is what the service reads. Consumers that assumed a non-null number now see `null` instead of a 500, which is the intended trade. `repository/JpaMappingIntegrationTest` persists a row holding `null` in every nullable column and maps it through both mappers, so a reintroduced primitive or guard fails a test rather than a request. |
| DL-164 | `util/DelimitedStringListConverter` escapes the delimiter and the escape character inside each element: a comma is written `\\,`, a backslash is written `\\\\`, and only an unescaped comma separates elements. A read reverses the substitution, and element content is no longer trimmed on the way in or out. The round-trip guarantee is that any list whose elements are non-`null` and not blank is read back element for element with each element byte-for-byte identical. | (a) Leave the delimiter unescaped, as the first implementation did. (b) Store the list as JSON text in the same `String` column. (c) Use RFC 4180 CSV quoting — wrap an element containing the delimiter in double quotes and double any interior quote. (d) Change the delimiter to a character that cannot occur in the data, such as the ASCII unit separator. (e) Reject an element containing the delimiter at write time. | (a) silently corrupts data: `["a,b"]` is stored as `a,b` and read back as `["a", "b"]`, so one element becomes two with no error anywhere — and both affected columns hold free text a human supplies, `tweets.media` holding URLs and `tweets.ai_tools_mentioned` holding tool names such as `Copilot, the assistant`, so the case is reachable rather than theoretical. (b) and (c) are both reversible, but (b) changes what an operator sees in the column from a readable list to a JSON document, and (c) makes the encoding of a value depend on whether it contains a delimiter, so the same element is stored two different ways — backslash escaping is uniform and its decoder is a single pass with no quote state. (d) was already rejected under DL-024 because a comma is what a human editing the column would write, and it would also make existing column values unreadable. (e) turns a legitimate value into an error and would need a new client-visible message, which no source route declares. Trimming was dropped because it is itself lossy: an element with significant leading or trailing whitespace could not survive a round trip while it was applied. | The stored form of an element containing a comma or a backslash is no longer identical to the element, so an operator hand-editing `tweets.media` has to know that `\\,` is a literal comma and `,` is a separator; the read path stays tolerant of a hand-edited value carrying repeated or trailing commas, which it drops as blank elements. Rows written before this change are read identically, because a value containing no backslash decodes exactly as it did before, so no migration is required — but a pre-existing value that happens to contain a backslash now loses it, or absorbs the character after it. `repository/JpaMappingIntegrationTest` covers the round trip for commas, backslashes, a trailing backslash, double and single quotes, non-ASCII text, an emoji, interior and surrounding whitespace and a newline, and asserts the exact stored column text, so a regression in either direction fails a test. |
| DL-165 | `service/AnalyticsService.getSummary` reads `total_responses` and `approved_responses` from a single aggregate projection, `repository/ResponseRepository.findResponseCounts()`, which returns `count(r)` alongside `coalesce(sum(case when r.isApproved = true then 1 else 0 end), 0)` in one statement, and derives `pending_responses` as their difference. The inherited `count()` and the derived `countByIsApprovedTrue()` remain declared and are no longer used to build a summary. | (a) Keep the two separate statements — `count()` then `countByIsApprovedTrue()` — and subtract, as the first implementation did. (b) Keep the two statements and raise the transaction's isolation to `REPEATABLE READ` or `SERIALIZABLE`. (c) Compute `pending_responses` with its own third statement instead of subtracting. (d) Load every `responses` row and count in Java. (e) Drop `pending_responses` from the metric set. | Spring's `@Transactional` leaves isolation at the database default, which is `READ COMMITTED` on PostgreSQL and on MySQL's InnoDB under its own default of `REPEATABLE READ` still differs from a single-statement snapshot in the general case; under `READ COMMITTED` each statement takes its own snapshot, so with (a) a commit landing between the two reads can make `approved_responses` exceed the `total_responses` already read and drive `pending_responses` negative — a value the metric cannot take. One statement removes the window entirely rather than narrowing it, and it does so identically on all three supported vendors. (b) would work on some vendors but pins behaviour to isolation semantics that differ across PostgreSQL, MySQL/MariaDB and H2, and it makes a read-only report the reason for a stricter isolation level. (c) adds a third statement and a third snapshot, so it makes the inconsistency worse. (d) transfers an aggregate the database computes into application memory and scales with the table. (e) changes the metric set that DL-041 fixed. The `case` expression is portable JPQL and names no vendor function, so DL-027's dialect-agnosticism holds. | The two counts are now coupled: a future metric over `responses` that has to share their snapshot must join this projection rather than issue its own statement, or the same class of skew returns. `total_tweets`, the two means and `tracked_ai_tools` are still separate statements inside the same read-only transaction, so a summary can still mix a `tweets` snapshot with a `responses` snapshot — accepted, because no metric is derived by arithmetic across those tables and none can therefore go negative. `repository/JpaMappingIntegrationTest` asserts that the projection agrees with `count()` and `countByIsApprovedTrue()` on quiescent data, that an unset flag counts as pending and never as approved, that an empty table yields two zeros rather than a `null`, and that `approved_responses + pending_responses` equals `total_responses` in the assembled report. |
| DL-166 | `repository/JpaMappingIntegrationTest` verifies the mappings twice over: against live `DatabaseMetaData` on the H2 connection the test slice supplies, and against the `create table` statements Hibernate generates for `PostgreSQLDialect`, `MySQLDialect` and `H2Dialect`. The generated statements are produced in-process by building metadata from the four entity classes with the dialect stated explicitly, `hibernate.boot.allow_jdbc_metadata_access` set to `false` and `UserSuppliedConnectionProviderImpl` as the connection provider, then writing a script through `jakarta.persistence.schema-generation.scripts.action=create`. No database is contacted for the two vendors that are not running. | (a) Assert against live H2 metadata only, as the test previously did. (b) Run a real PostgreSQL and a real MySQL per test class through Testcontainers and read their metadata. (c) Commit an expected DDL script per vendor and compare the generated script to it byte for byte. (d) Use `org.hibernate.tool.hbm2ddl.SchemaExport`. (e) Assert only the Java-side `@Column` facets by reflection and infer the DDL from them. | (a) validates the schema the implementation chose on one vendor rather than the schema the source declares on all of them, so a mapping can pass on H2 while the PostgreSQL or MySQL column type diverges — which is exactly how the `bigint` identifiers and the `varchar(255)` character columns survived an earlier green build. (b) is the strongest option and was rejected for the reason recorded in DL-009: the Testcontainers `junit-jupiter` and `postgresql` modules publish only up to 1.21.4 while the standalone artifact has moved on, and it needs a Docker daemon that the prompt's "no manual intervention" requirement cannot assume. Offline script generation needs neither a daemon nor a new coordinate — `hibernate-core` is already on the classpath — and it exercises the same dialect code that would run against the real server. (c) makes every unrelated Hibernate upgrade a test failure over formatting; asserting the column-type fragments each statement must contain, and the fragments it must not, is stable against that. (d) is not available: that class does not exist in `hibernate-core` 6.6.53, which was confirmed by compiling against it. (e) reasons about DDL instead of reading it, and the whole point is that `length = Integer.MAX_VALUE` renders differently per vendor. | The generated statements are checked for the fragments they must contain plus two negative rules — no `bigint` anywhere, and no `varchar(` in `tweets`, `responses` or `ai_tools` — so a column type that changes to some third value the expectations do not name could pass if it is neither. The positive fragment list covers all twenty columns on all three dialects, which bounds that gap. Script generation builds and immediately closes a `SessionFactory` per dialect, which is three extra boot cycles in the test phase. The scripts are written under `target/`, which `backend/.gitignore` excludes. Verification is still not a real PostgreSQL or MySQL server: a defect that shows up only in the server's own interpretation of valid DDL would not be caught here. |
| DL-167 | `service/LlmService.generateResponse` returns a nested `GeneratedResponse(String content, LocalDateTime generatedAt, String tweetId)` — an internal result describing a reply that has been generated and not yet stored, carrying no identifier and no approval flag — and `dto/ResponseDto` asserts a non-null `id` in its compact constructor alongside its three other required components. The wire record is built only from a stored row. | (a) Return `ResponseDto` with a `null` `id`. (b) Return the generated `String` alone. (c) Keep `ResponseDto` but declare `id` nullable and document the unsaved case. (d) Return the unsaved `entity/Response`. (e) Declare `GeneratedResponse` as its own top-level class under `dto/` or a new `service/result/` package. | `ResponseDto` is the body of all four `responses` routes, and `frontend/src/schema/response.ts:L3-9` types `id` as a required string, so a `null` identifier is a value no consumer of that record can ever receive. (a) and (c) therefore ask every reader of the wire record to handle a state the wire does not carry; (c) additionally removes the one invariant that would catch a genuinely missing identifier at construction, and it removes it for all four routes in order to describe one internal moment. (b) discards `generatedAt`, which the generator is the only place that knows. (d) hands a mutable entity across a service boundary and implies a row exists. (e) is the tidier package layout, and is rejected only because it would add a class the AAP's enumerated file list does not contain: nesting the record in the single class that produces it keeps the delivered file set exactly as specified, and keeps the type out of `dto/`, where every member is a wire contract. `GeneratedResponse` holds its own non-blank invariant, so an instance cannot exist for a reply carrying no text (DL-081). | Two record types now describe a reply and a reader must know which boundary each belongs to; the names and the Javadoc carry that distinction and nothing else enforces it. A caller wanting the generated text and the stored identifier makes two hops rather than one. Because the record is nested, its qualified name is `LlmService.GeneratedResponse`, which is longer at every use site and would have to move if a second producer ever appeared. |
| DL-168 | A model response carrying no usable text is a failed generation, never an empty success, and the rule is enforced at both boundaries. `service/LlmService` raises `GenerationFailedException` for a response with no choice, for a first choice whose message content is absent and for a first choice whose content is blank. `service/ResponseService.generateResponse` re-tests the result before it constructs an entity and raises `ResponseGenerationException`, so `POST /responses` answers 500 with `{"error": "Failed to generate response"}` and writes no `responses` row. | (a) Return empty text through a `NO_GENERATED_TEXT` constant. (b) Raise in `LlmService` only and let the exception travel to the advice. (c) Guard in `ResponseService` only. (d) Store the empty draft and let a human reject it. (e) Retry the model call before failing. | `backend/app/services/llm_service.py:L29` indexed `choices[0]` directly and raised on a response carrying none, so failing is the source behaviour and substituting empty text is the deviation; `backend/app/api/responses.py:L44-49` then reports a falsy generation result as 500 with that exact literal, so both the status and the message are the source's. (a) writes a row whose `content` is empty and whose `is_approved` is `false`, which is indistinguishable from a real draft awaiting review — it makes the one workflow the never-auto-post invariant depends on a human reading meaningless, and it does so silently. (b) is sufficient today and is why the adapter raises at all, but it leaves the boundary that owns the column trusting its collaborator; (c) leaves the adapter free to return a value its own contract forbids. Holding the invariant at both boundaries costs one branch and means neither a future generator nor a test stub can cause an empty row: the guard runs before any entity is constructed, so `repository/ResponseRepository.save` is not reached at all, and that is asserted directly rather than inferred. (d) contradicts the reasoning against (a). (e) adds retry behaviour the source had nowhere and the prompt does not ask for. | A model that legitimately returns whitespace now yields 500 rather than a stored empty draft, and the client cannot tell "the model said nothing" from "the call failed" — which is the contract, because `backend/app/api/responses.py:L49` declares one message for the whole failure class. The same rule is now expressed in two classes and could drift; tests pin both. Consequently `POST /responses` reports exactly two outcomes and never 404 (DL-076). |
| DL-169 | `org.postgresql:postgresql` is held at `42.7.13` by overriding the `postgresql.version` property in `backend/pom.xml`, above the `42.7.11` that `spring-boot-dependencies:3.5.16` manages. | (a) Accept the BOM's `42.7.11`. (b) Take the minimum fixed release, `42.7.12`. (c) Exclude the driver and let each deployment supply one. (d) Pin `<version>` on the dependency instead of overriding the property. | `42.7.11` falls inside the range `42.7.4` … `42.7.11` affected by the silent channel-binding downgrade that pgJDBC fixed in `42.7.12` (CVE-2026-54291): under `channelBinding=require` the driver could fall back from SCRAM-SHA-256-PLUS to plain SCRAM-SHA-256 when the server certificate's signature algorithm carries no `tls-server-end-point` binding hash, and the project states that no driver setting restores enforcement — upgrading is the fix. This service consumes `DATABASE_URL` exactly as supplied (DL-064), so a deployment is free to set `channelBinding=require` and would then be relying on an enforcement the BOM's version does not perform. (a) keeps that gap for the sake of a matched BOM. (b) closes it with a smaller jump but takes the release with the least field exposure; `42.7.13` is the maintenance release that follows it on the same line and carries the same fix. (c) makes the artifact incomplete and moves the decision to whoever assembles the image. (d) is equivalent for this module but would leave the BOM property still naming `42.7.11`, so any other coordinate resolving that property could disagree — overriding the property keeps exactly one number. Update policy: on every Spring Boot upgrade, read the new BOM's `postgresql.version`; the override is removed as soon as it reaches or passes `42.7.13`, and until then it is re-verified rather than assumed. | The driver is one patch release ahead of what Boot 3.5.16 was tested against, so an incompatibility would be found by this project's own build and test suite rather than upstream — narrowed by both versions being `42.7.x` maintenance releases on one line, and by `mvn clean verify` and a boot against PostgreSQL 16 with `ddl-auto=update` both passing on it. The override is invisible unless a reader opens `<properties>`, so a Boot upgrade that would otherwise have moved the driver silently keeps this number: a stale override is the failure mode and the check is manual. Advisories for this coordinate must now be read directly rather than arriving with a framework upgrade. |
| DL-170 | `com.mysql:mysql-connector-j` is held at `26.7.0` by overriding the `mysql.version` property in `backend/pom.xml`, above the `9.7.0` that `spring-boot-dependencies:3.5.16` manages. | (a) Accept the BOM's `9.7.0`. (b) Use MariaDB Connector/J for both MySQL and MariaDB URLs. (c) Drop MySQL support and ship the PostgreSQL driver alone. (d) Pin `<version>` on the dependency instead of overriding the property. | Oracle publishes `26.7.0` as a GA release that supersedes `9.7` and states it is the release recommended for production systems; the numbering moves from `9.x` to a calendar scheme, which makes the jump look far larger than the change it carries — the release notes list connection-correctness fixes, among them a `LOAD DATA LOCAL INFILE` failure path that could leave a connection out of sync for the next command, and an N-character retrieval bug for valid UTF-8 encoding aliases. Taking the vendor's recommended production release is the same policy DL-082 applies to the other driver, and applying one policy to both is what keeps DL-028's "one artifact serves either URL" honest: a supported driver on one side and a superseded one on the other would quietly make the choice of database vendor a choice of maintenance level. (b) would replace a driver the AAP names and would change which URL schemes are recognised. (c) contradicts IR4 and DL-028. (d) is rejected for the reason recorded in DL-082. Update policy: identical to DL-082 — the override is removed once the BOM's `mysql.version` reaches or passes `26.7.0`. | This is the override that carries a real compatibility bound: `26.7.0` is documented for use against MySQL Server 8.4 and later, so a `DATABASE_URL` naming an older server is an unsupported pairing where the BOM's `9.7.0` reached further back. Nothing here detects the server version and the vendor is not resolved until runtime (DL-027), so such a mismatch surfaces at the first connection rather than at startup. MySQL is also not exercised by `mvn clean verify`, which runs against H2 (DL-009), so the evidence for this coordinate is a boot against a real server rather than a build step. As with DL-082, the override must be revisited on every Boot upgrade or it goes stale unnoticed. |
| DL-171 | The embedded servlet container is held at `10.1.57` by overriding the `tomcat.version` property in `backend/pom.xml`, above the `10.1.55` that `spring-boot-dependencies:3.5.16` manages. The property moves every `org.apache.tomcat.embed:*` artifact the web starter brings in, not one of them. | (a) Accept the BOM's `10.1.55`. (b) Override `tomcat-embed-core` alone. (c) Exclude Tomcat and run Jetty or Undertow. (d) Move to the `11.x` line. | The container is the only component in this artifact that terminates untrusted network input, so it is where lagging the current maintenance release costs the most; `10.1.57` is the current release of the `10.1` line and carries what was published after `10.1.55`, including the alignment of DIGEST authentication with RFC 7616. Applying the same patched-release policy here as to the two drivers keeps one rule across the whole overridden set instead of three separate judgements. (b) is the narrower change but would split the Tomcat artifacts across two versions, a combination nobody tests — the `tomcat.version` property exists precisely so they move together. (c) changes the container this project was specified with and is a far larger behavioural change than a patch bump. (d) is not a patch decision at all: the `11.x` line targets a later Jakarta EE baseline than the Servlet 6.0 / Jakarta EE 10 platform Boot 3.5 builds against. Update policy: identical to DL-082 — the override is removed once the BOM's `tomcat.version` reaches or passes `10.1.57`. | The container runs one maintenance release ahead of what Boot 3.5.16 was built and tested against, and it is the component where an untested combination has the widest reach: a regression would appear as a request-handling fault rather than a compile failure, so this project's own `mvn clean verify` and a boot on port 5000 are the only evidence it holds. Being current is not being invulnerable — the Tomcat 10 advisory list carries an entry whose affected range includes `10.1.57`, and although it concerns the examples web application that an embedded container never deploys, it shows that the override buys currency rather than immunity and must be re-read on each upgrade. The property also moves several artifacts at once, so a fault is attributed to a version rather than to a component. |
| DL-172 | The runtime stage of `infrastructure/docker/Dockerfile.backend` creates an unprivileged account `scanner` with an explicit uid and gid of `10001`, copies the executable jar with `COPY --from=build --chown=scanner:scanner`, and declares `USER scanner` ahead of `CMD`. The Maven build stage continues to run as root. | (a) Leave the runtime process as root, which is the behaviour the file had. (b) Reuse a distribution account such as `nobody` or `daemon`. (c) Move to a distroless or `-nonroot` base image that ships a non-root default user. (d) Create the account with `useradd --system`. | Nothing the process does needs privilege: it binds port 5000, which is above 1024 (DL-029), reads one jar, and writes nothing inside the image, so running as root grants capability that is only ever available to an attacker. Docker's own image guidance is to run the process as a non-root user. An explicit numeric uid and gid, rather than a name alone, keeps the identity stable across rebuilds and satisfies a Kubernetes `runAsNonRoot`/`runAsUser` policy without a name lookup inside the image. Doing the ownership change on the `COPY` avoids a second `RUN chown` layer that would duplicate a 170 MB jar purely to change its owner. (b) reuses an account whose uid is shared with unrelated software when no user namespace is in play. (c) would replace the base image the AAP fixes at `eclipse-temurin:21-jre` (§0.8.4). (d) was tried against the pinned image and emits `useradd warning: scanner's uid 10001 is greater than SYS_UID_MAX 999`, since that image is Ubuntu 26.04; a plain account with an explicit high uid avoids both the warning and the distribution's system-uid range, and `--no-log-init` avoids the sparse `lastlog` growth a high uid otherwise provokes. | The account can write nowhere under `/app`, so a future feature that needs a writable working directory must create and chown that directory explicitly; the jar is deliberately not writable by the identity that runs it. A host bind mount owned by a different uid is unreadable to the process. The build stage still runs as root, which is where the network fetch and the compile happen — that stage contributes no layer to the shipped image. Verified by building the image and running it: the process reports `uid=10001(scanner)`, `/app/app.jar` is owned by `scanner:scanner`, and Tomcat starts on port 5000. |
| DL-173 | Both `FROM` instructions carry the readable tag **and** a `sha256` digest: `maven:3.9-eclipse-temurin-21@sha256:eb5b6009…` for the build stage and `eclipse-temurin:21-jre@sha256:8cef5fc7…` for the runtime stage. The tag documents the version; the digest fixes the contents. | (a) Tags alone, which is what the file had. (b) The digest alone, dropping the tag. (c) A fully qualified patch tag such as `maven:3.9.11-eclipse-temurin-21.0.12` with no digest. (d) Delegate the pin to an automated update bot. | A tag is a mutable pointer, so rebuilding the same commit can consume different upstream contents — which makes the build non-reproducible and lets a changed or compromised upstream layer enter the image with no change anywhere in this repository. Docker's guidance recommends digest pinning for exactly this reason. Keeping the tag beside the digest preserves the information a reviewer actually reads: (b) would leave sixty-four hex characters and no indication of which Java or Maven line is in use. (c) narrows the drift but a patch tag is still mutable, and both images are rebuilt whenever their base OS is patched. (d) is the correct long-term process, but this repository carries no bot configuration and adding one is a new capability rather than a translation. Both digests were resolved from the registry as part of this change and both are multi-architecture OCI image indexes, so pinning does not tie the build to a single architecture. | The digests age: a security fix in either base image is not picked up until someone advances the digest by hand, which is a deliberate trade of freshness for reproducibility and belongs in the dependency-update routine alongside DL-010, DL-011 and DL-014. If an upstream digest is ever garbage-collected the build fails loudly instead of silently substituting content. The tag and the digest can drift apart in the file, because Docker resolves the digest and treats the tag as decoration — so the tag has to be updated in the same edit as the digest to stay truthful. |
| DL-174 | `backend/pom.xml` declares `maven-compiler-plugin` — version and release level still inherited from the Spring Boot parent — with `<proc>full</proc>` configured at plugin level, so both `default-compile` and `default-testCompile` receive it. | (a) Leave the plugin undeclared and accept the javac lint message. (b) `<proc>none</proc>`. (c) Declare `annotationProcessorPaths` and `testAnnotationProcessorPaths` naming `spring-boot-configuration-processor` explicitly. (d) Suppress the message with `-Xlint:-options`. | `spring-boot-configuration-processor` sits on the compile classpath to generate `META-INF/spring-configuration-metadata.json` for `config/ScannerProperties`, so javac finds a processor by scanning the class path and reports that a future release may stop doing so unless annotation processing is requested by name, by processor path, or explicitly — the message was emitted on both the main compilation and the test compilation, so a reader saw it twice per build. `-proc:full` states the behaviour the build already depends on, which keeps the metadata generated and removes the dependence on a deprecated default. (b) would silently stop producing the metadata file, which is the contract an IDE reads to complete and validate `scanner.*` keys. (c) is more precise, but it has to name the coordinate, repeat the declaration for the test compilation, and be edited whenever a processor is added or removed, whereas one plugin-level parameter is inherited by both mojos. (d) hides the message rather than resolving it and would also hide unrelated `-Xlint:options` findings. | Annotation processing stays enabled for every processor that reaches the class path, so `proc:full` is an explicit switch and not an allow-list; a processor arriving through a future transitive dependency runs exactly as it would have before. Declaring the plugin adds a second place to look when compiler behaviour is in question, although neither the version nor the release level is duplicated there — both still come from the parent, so a Boot upgrade continues to move the compiler version on its own. |
| DL-175 | `security/JwtService.parseClaims` accepts a token only after four further requirements are met, checked once jjwt has verified the signature: the `alg` header names `HS256`; `sub` is present and not blank; `iat` is present; and `exp` is present and strictly later than that `iat`. Any token failing one of them is answered with `Optional.empty()`, exactly as an unverifiable token is, so `security/JwtAuthenticationFilter` leaves the security context untouched and `security/SecurityConfig`'s `HttpStatusEntryPoint` answers 401. | (a) Verify the signature only, as before, and let a missing claim mean "unlimited" — jjwt checks `exp` when it is present and skips the check when it is absent. (b) Require the claims but throw instead of returning empty, letting `api/GlobalExceptionHandler` classify the failure. (c) Require `exp` alone and accept an absent `iat` or `sub`. (d) Restrict the accepted algorithm through a jjwt parser option rather than a header comparison. (e) Also require `jti`, `iss` or `aud`. | DL-018 states the minted claim set as exactly `sub`, `iat` and `exp`, and DL-017 caps the lifetime at sixty minutes; without this check neither statement is enforced on the way back in. (a) leaves a real hole: a correctly signed token carrying only `sub` never expires, so a token that leaks — or one minted before a lifetime reduction — authenticates indefinitely, which is precisely the condition CWE-613 describes. It also lets a token with no `sub` reach `SecurityContextHolder` as a `null` principal. (b) would let an authentication concern reach the error-translation advice, which owns the eight approved wire literals and no 401 path (DL-078); returning empty keeps the single answer for every rejected token. (c) accepts a claim set this service never mints. (d) is not available in jjwt 0.13.0 as an allow-list, and `verifyWith(SecretKey)` already rejects an asymmetric `alg` and an unsigned token — but it does **not** reject `HS384` or `HS512` signed with the same secret, and comparing the header to the one algorithm `generateToken` uses closes that remainder in one line. (e) would require claims no code path here reads and none the source minted. | A token minted by an earlier revision of this service carries all three claims, so no already-issued token is invalidated by the change; a token minted by any other issuer that happens to share the secret is now rejected if its claim set differs. The `sub`-blank branch is defensive rather than reachable through jjwt: measured against 0.13.0, both the builder and the parser normalise a whitespace-only `sub` to an absent claim, so a hand-crafted payload is needed to exercise it — `security/JwtServiceTest` builds exactly that, signing the payload with `javax.crypto.Mac` so the signature still verifies. The `exp`-later-than-`iat` requirement rejects a zero-lifetime token that jjwt would otherwise have reported only as expired. |
| DL-176 | A token this service refuses is recorded at `DEBUG` with a fixed message and, for a jjwt failure, the exception's simple class name. Neither the presented token, the message of the jjwt exception, nor any claim value is logged, and no throwable is attached. The reason a verified token was rejected is one of five fixed descriptions declared in `requiredClaims`. | (a) Keep the previous `WARN` carrying `JwtException.getMessage()`. (b) Keep the library message but sanitise it by stripping control characters. (c) Log the token, or a prefix of it, to help correlate a failure. (d) Log nothing at all on a rejected token. (e) Attach the exception as a throwable so the stack trace is available. | The presented bearer header is attacker-controlled input, and a jjwt message can echo parts of the token it was constructed from, so writing it to the log turns a rejected request into log-forging and disclosure exposure — CWE-117 and CWE-532. The exception's class name is drawn from a closed set of jjwt types and cannot carry a newline or a claim value, which is why it is the one piece of the failure worth keeping. `DEBUG` is the right level because a rejected token is a routine event on an authenticated API and `WARN` would let an unauthenticated caller flood the log at a level operators alert on. (b) is a sanitiser this project would then own and have to keep correct against library message changes. (c) logs a credential. (d) removes the ability to tell an expired token from a malformed one while diagnosing a client, which the class name preserves at no cost. (e) writes the same library message into the log through the stack trace. | Diagnosing a client now needs `DEBUG` enabled for `com.codeskeptic.scanner.security.JwtService`, and the log names the failure category rather than the offending value — a deliberate trade. `security/JwtServiceTest` attaches a Logback list appender and asserts the exact recorded message, the `DEBUG` level, the absence of a throwable, and that neither the token nor the library message appears, so a future edit that reintroduces either fails the build. |
| DL-177 | `service/LlmService.generateResponse(TweetDto)` returns the generated text as a `String`, and `dto/ResponseDto` requires a non-null `id` again. `service/ResponseService.generateResponse` is the only class that builds a `ResponseDto` for a generated reply, and it builds it from the row `ResponseRepository.save` returns, so the record only ever exists for a row that carries a database-assigned key. An earlier revision had `LlmService` return `new ResponseDto(null, text, now, false, tweetId)` and removed `id` from the record's null checks to allow it; both halves of that are reverted. | (a) Keep returning a `ResponseDto` with a null `id`. (b) Keep returning a `ResponseDto` and fabricate an `id` — a placeholder, a zero or a UUID. (c) Return a second, generation-only record type carrying just the text and the post identifier. (d) Have `LlmService` persist the row itself so it can return a fully populated record. | `backend/app/schema/response.py:L5` declares `id: str` with no `Optional[...]`, and TR-6 requires a required source field to become a non-null contract, so (a) publishes `{"id":null,…}` on a route whose contract says otherwise — the record is the body of three responses on `/responses` and the element type of the paginated envelope, so a nullable identifier is visible on the wire. (b) is worse: it puts a value in `id` that names no row, and the AAP forbids fabricating one. (c) would be defensible but adds a type whose only content is a string the caller already has, and every consumer would immediately unwrap it. (d) would move persistence into the OpenAI adapter, crossing the layer boundary that keeps `LlmService` free of repositories. Returning the text keeps `LlmService` exactly what its name says, leaves the wire record to the layer that owns the transaction, and needs no new type. TR-13 is unaffected: a `String` return carries no vendor type. | The method name `generateResponse` now reads as though it returns a response object; its Javadoc and this entry are what disambiguate it. `service/ResponseService` is the sole caller (verified by grep), so the signature change has no other consumer, but any future caller — the planned `task/ResponseGenerationScheduler` among them — must go through `ResponseService.generateResponse(String)` if it wants a stored row rather than bare text. The requiredness of `id` is enforced in the canonical constructor rather than by an annotation, consistent with DL-050, so a violation surfaces as a `NullPointerException` at construction rather than as a validation failure. |
| DL-178 | A Chat Completions response that carries no usable reply is a generation failure. `service/LlmService.firstChoiceContent` raises `IllegalStateException` when the response carries no choice, when the first choice carries no message content, and when that content is blank once trimmed; each case is also recorded at `WARN`. An earlier revision returned the empty string for the first two cases and accepted blank text. | (a) Keep returning empty text and let the empty draft be stored. (b) Treat no-choice and absent-content as failures but accept blank text. (c) Raise `ResponseGenerationException` here instead of `IllegalStateException`. (d) Retry the request. (e) Return `Optional<String>` and let the caller decide. | The source's `response.choices[0].text.strip()` at `backend/app/services/llm_service.py:L29` would itself have raised on an empty choice list and on absent text, so failing is the faithful behaviour and returning empty text was the deviation. It also matters on the wire: `POST /responses` reports exactly two outcomes — 201 with the stored row, or 500 with `Failed to generate response` (DL-076) — so (a) stored an empty draft, answered 201, and put a row a human must review into the queue with nothing in it. (b) leaves the same defect for the case a model actually produces, which is whitespace rather than an absent field. (c) is forbidden by the boundary this project holds: `service/ResponseService` is the only class permitted to construct that type and its literal, because `api/GlobalExceptionHandler` copies an exception message verbatim into the response body, so a message minted here would leak onto the wire. `IllegalStateException` is a plain runtime failure that `ResponseService` already wraps, which is why the 500 body stays exactly the approved literal and the `@Transactional` method rolls back with nothing saved. (d) adds a retry policy, which is a forbidden new capability. (e) pushes a decision the adapter is best placed to make onto every caller. | A model that legitimately returns nothing now produces a 500 rather than an empty draft, so a client sees a failure where it previously saw a success — which is the intent, and is what the route contract already described. The three messages are diagnostic only and never reach a client; `service/LlmServiceTest` asserts each one and asserts that none of them equals the wire literal, and `service/ResponseServiceTest` asserts that the wrapped failure carries the wire literal, keeps the original as its cause, and never calls `save`. |
| DL-179 | Default-row seeding is duplicate-tolerant. `service/SettingsService.seedDefaultSettings` keeps `@EventListener(ApplicationReadyEvent.class)` but declares **no** `@Transactional`, so it opens no transaction of its own; each row is written with `saveAndFlush`, which runs inside `SimpleJpaRepository`'s own transaction; and a `DataIntegrityViolationException` from that write is caught per row, logged at `DEBUG` and passed over. The `existsById` check stays, and the semantics stay insert-only. An earlier revision wrapped all three rows in one `@Transactional` method and wrote them with `save`. | (a) Keep the single enclosing `@Transactional` and catch the duplicate inside it. (b) Keep the enclosing transaction and inject a `PlatformTransactionManager` or `TransactionTemplate` so each row can run in its own inner transaction. (c) Split the per-row write into a second bean, or self-inject this one, so `@Transactional(propagation = REQUIRES_NEW)` takes effect. (d) A vendor-specific conditional insert — `INSERT … ON CONFLICT DO NOTHING`, `INSERT IGNORE`, `MERGE`. (e) A distributed lock or a leader election so exactly one instance seeds. (f) Leave the race in place, since it only occurs at startup. | Two instances starting together can both see a key as absent and both try to insert it; the loser's write violates the primary key. Under (a) that failure is unrecoverable inside the same transaction — a constraint violation marks it rollback-only and leaves the `EntityManager` unusable, so catching it saves nothing and all three rows are lost — and because the listener runs during `SpringApplication.run`, the exception propagates and the instance fails to start. That is the concrete defect: a routine rolling restart could take an instance down. Removing the enclosing transaction is what makes per-row isolation possible at all, and `saveAndFlush` is what makes the outcome known before the catch, since a plain `save` may defer the statement past the transaction boundary. (b) and (c) both work, but each adds a collaborator or a bean purely to obtain what the repository's own transaction already provides — and a fourth constructor parameter would change every construction site in `service/SettingsServiceTest`. (d) is not portable across PostgreSQL, MySQL/MariaDB and H2, and this project deliberately spells no vendor SQL and declares no dialect (DL-027). (e) is coordination infrastructure the system does not have. (f) is the defect. | The write is `saveAndFlush` on an entity with an assigned identifier and no `@Version`, so JPA merges rather than persists: it issues a SELECT first and, if the concurrent row has already committed, the statement becomes an UPDATE instead of raising a violation. That window is narrow and both instances seed identical configuration-derived values, so the update is a no-op in practice — but it means the loser of a race may rewrite the winner's row with the same content rather than being rejected. An operator's edit is never at risk, because a key that is already present is skipped by `existsById` before any write is attempted. Verified at runtime against PostgreSQL 16: a first boot seeds all three rows, a second boot writes nothing, and after an operator changes `tweet_popularity_threshold` to `250` directly in SQL a third boot logs all three keys as "already present and is left unchanged" and leaves the edited value at `250`. `service/SettingsServiceTest` covers the race by stubbing the duplicate-key failure for one key and for all three, asserting that seeding completes without raising and that the remaining keys are still written. |
| DL-180 | `service/AnalyticsService.getSummary()` declares `@Transactional(readOnly = true, isolation = Isolation.REPEATABLE_READ)`, so all six of its aggregate queries read one snapshot and every metric of the returned `SummaryDto` describes the same instant. `getTrends()` keeps the default isolation. | (a) Leave `getSummary()` at the default isolation, as it was. (b) Read `total_responses` and `approved_responses` in a single aggregate query, adding a projection method to `ResponseRepository`. (c) Compute `pending_responses` with its own `countByIsApprovedFalse()` query instead of by subtraction. (d) Clamp `pending_responses` at zero. (e) `SERIALIZABLE` instead of `REPEATABLE_READ`. | The summary carries a derived metric, `pending_responses = total_responses − approved_responses`, whose operands were read by two separate queries. Under PostgreSQL's default `READ COMMITTED` each statement takes its own snapshot, so a reply approved between them makes `approved_responses` exceed the earlier `total_responses` and the difference goes negative — an impossible value in a field a reviewer reads as a work queue. One snapshot removes the window, and because the transaction is read-only it cannot fail on a serialization conflict. It also covers the whole record rather than the response pair alone, which is what (b) would have fixed; (b) is otherwise sound but `repository/ResponseRepository` is deliberately closed at one declared method, with every member attributable to a named metric, so widening it to fix an isolation problem would trade a transaction-level concern for a permanent interface change. (c) replaces the derivation the metric is defined as (DL-041) and changes its meaning for a row whose `is_approved` is null. (d) hides the inconsistency instead of removing it, and would still report a total and an approved count that disagree. (e) is stronger than needed and, on PostgreSQL, exposes a read-only transaction to serialization failures for no gain. All three supported vendors implement `REPEATABLE READ` — it is MySQL's default, and PostgreSQL and H2 2.x both offer it — so no vendor is excluded, and Spring's `HibernateJpaDialect` applies the level to the JDBC connection or raises `InvalidIsolationLevelException` loudly rather than ignoring it. | The summary transaction now holds one snapshot for the duration of six queries, which on PostgreSQL keeps a slightly older row version visible to those queries; the cost is negligible at this scale and the reported instant is simply the start of the transaction rather than the start of each query. A future metric added to the summary joins the same snapshot automatically. `service/AnalyticsServiceTest` asserts the declared annotation carries `readOnly` and `REPEATABLE_READ`, so removing it fails the build, and the level was verified at runtime against PostgreSQL 16 by reading `Connection.getTransactionIsolation()` inside such a transaction and by calling `getSummary()` against a live schema without an `InvalidIsolationLevelException`. |
| DL-181 | `api/GlobalExceptionHandlerTest` is a `@WebMvcTest` slice that drives every translated exception through the real `DispatcherServlet` with MockMvc against a nested probe handler, and keeps its thirty direct-invocation checks in the same file as supplemental tests. | (a) Keep only the direct-invocation checks, as the class was first written. (b) Add a seventh file in `api/` for the slice. (c) Use `@SpringBootTest` with a live server and `WebTestClient`. (d) Point the slice at a real controller such as `SettingController` and stub its service to throw. | The AAP lists this class in the `api/` group qualified as `@WebMvcTest` plus MockMvc, and direct construction cannot observe five properties the wire contract depends on: resolution of the exception by the `DispatcherServlet`, registration of the advice in the context, Jackson serialisation and the response `Content-Type`, the unmapped-path branch, and suppression of Spring's `ProblemDetail` and Boot's default error body. The last of these matters most: Boot's default body also carries an `error` member, so only an assertion that the object holds exactly one member distinguishes it from the two envelopes of `backend/app/main.py:L31-37`. A nested probe leaves the file count in `api/` unchanged, which is what (b) would have altered. (c) would require a port, a datasource and the security chain for a translation concern. (d) would couple these tests to a controller's service graph and still could not raise `NoHandlerFoundException` or an unparseable body. This was measured rather than assumed: with `@RestControllerAdvice` removed from the handler, exactly the 25 slice cases fail while all 30 direct cases still pass. | The file now loads a Spring context, so it is slower than a pure unit test and depends on `application-test.yml` binding `ScannerProperties`. The probe must declare `@Controller` because `RequestMappingHandlerMapping.isHandler` in Spring Framework 6.2 accepts that stereotype alone; `@TestComponent` keeps it out of the production component scan, and a guard test fails if it is ever registered twice. `controllers` is restricted to the probe, so a controller added later is not exercised by this file and needs its own slice. `addFilters = false` removes the security chain, so this file asserts nothing about the 401 entry point. |
